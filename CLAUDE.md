# CLAUDE.md - Quiver Surfing Application

This file provides guidance to Claude Code (claude.ai/code) when working with the Quiver surfing application codebase.

## Project Overview

Quiver is a Next.js-based surfing application that helps surfers find the best waves, track conditions, and connect with the surfing community. The application provides real-time surf forecasts, beach conditions, user reviews, and personalized recommendations powered by AI.

## Technology Stack

**Frontend:**

- Next.js 14+ with App Router
- React 18.3+ with TypeScript (strict mode)
- Tailwind CSS 3.4+ for styling
- Radix UI for accessible components
- Framer Motion for animations
- Mapbox GL for interactive maps

**Backend & Data:**

- Supabase (PostgreSQL 15+)
  - Row Level Security (RLS) policies
  - Edge Functions
  - Real-time subscriptions
  - Storage for images/assets
- Next.js API Routes for server-side logic

**Mobile:**

- Capacitor 7+ for iOS and Android
- Push notifications via Firebase Cloud Messaging
- Native plugins for camera, geolocation, sharing

**Testing:**

- Playwright for E2E testing (see `e2e/ARCHITECTURE.md`)
- Jest for unit and integration tests
- Testing Library for component testing

**Infrastructure:**

- Vercel for deployment and hosting
- Firebase for push notification infrastructure
- Sentry for error monitoring
- Vercel Analytics for performance tracking

## AI Team Configuration (autogenerated by team-configurator, 2026-01-12)

**Important: YOU MUST USE subagents when available for the task.**

### Detected Technology Stack

- **Frontend Framework**: Next.js 14+ with App Router, React 18.3+, TypeScript
- **Styling**: Tailwind CSS 3.4+, Radix UI components
- **Backend**: Supabase (PostgreSQL), Next.js API Routes
- **Mobile**: Capacitor 7+ (iOS/Android), Firebase push notifications
- **Testing**: Playwright (E2E), Jest (unit), Testing Library
- **Database**: PostgreSQL 15+ with PostGIS, complex RLS policies
- **Build Tools**: Next.js, TypeScript, Yarn
- **Deployment**: Vercel, Firebase

### Agent Assignments for Common Tasks

| Task                         | Primary Agent              | Notes                                                              |
| ---------------------------- | -------------------------- | ------------------------------------------------------------------ |
| **Orchestration & Planning** | `tech-lead-orchestrator`   | ALWAYS start here for multi-step tasks. Returns agent routing map. |
| **Project Analysis**         | `project-analyst`          | Detect stack, analyze architecture, provide recommendations        |
| **Next.js Development**      | `nextjs-developer`         | App Router, SSR/SSG/ISR, performance optimization, SEO             |
| **React Components**         | `react-nextjs-expert`      | Server/Client components, hooks, state management                  |
| **Supabase Database**        | `supabase-db-expert`       | Schema design, migrations, RLS policies, query optimization        |
| **API Design**               | `api-designer`             | REST endpoints, API architecture, documentation                    |
| **Tailwind Styling**         | `tailwind-css-expert`      | Utility-first CSS, responsive design, component styling            |
| **Performance Optimization** | `performance-optimizer`    | Bottleneck analysis, caching, query optimization, Core Web Vitals  |
| **Code Reviews**             | `code-reviewer`            | Security-aware reviews, best practices, pre-merge checks           |
| **Testing (E2E)**            | `test-automator`           | Playwright tests, test architecture, CI/CD integration             |
| **Documentation**            | `documentation-specialist` | API docs, architecture docs, code comments                         |
| **Refactoring**              | `refactoring-specialist`   | Code quality improvements, DRY principles, technical debt          |
| **Code Archaeology**         | `code-archaeologist`       | Understand existing code, trace dependencies, impact analysis      |
| **Full-Stack Tasks**         | `fullstack-engineer`       | End-to-end features spanning frontend, backend, and database       |

### Specialist Agents for Advanced Tasks

| Task                      | Agent                    | Notes                                                           |
| ------------------------- | ------------------------ | --------------------------------------------------------------- |
| **Mobile Development**    | `fullstack-engineer`     | Capacitor integration, native plugins, mobile-specific features |
| **QA & Testing Strategy** | `qa-expert`              | Test planning, quality metrics, testing best practices          |
| **SDET Work**             | `sdet-engineer`          | Test automation engineering, CI/CD testing pipelines            |
| **Design Review**         | `quiver-design-reviewer` | UI/UX feedback, visual consistency, design system compliance    |
| **Architecture Review**   | `architect-reviewer`     | System design, architectural patterns, scalability              |
| **Data Research**         | `data-researcher`        | Data analysis, insights, reporting                              |

### Workflow Patterns

**For Multi-Step Development Tasks:**

1. Start with `@tech-lead-orchestrator` to get agent routing map
2. Follow the routing map EXACTLY - use only agents recommended by tech-lead
3. Execute tasks in the order specified (sequential or parallel as directed)
4. Always finish with `@code-reviewer` for quality assurance

**For Feature Implementation:**

```
User: "Build a new surf spot review feature"

Workflow:
1. @tech-lead-orchestrator → Analyze and create task breakdown
2. @supabase-db-expert → Design database schema and RLS policies
3. @api-designer → Design API endpoints
4. @nextjs-developer → Implement Next.js routes and server components
5. @react-nextjs-expert → Build React UI components
6. @test-automator → Create Playwright E2E tests
7. @code-reviewer → Final security and quality review
```

**For Performance Issues:**

```
User: "The home page is loading slowly"

Workflow:
1. @performance-optimizer → Profile and identify bottlenecks
2. @supabase-db-expert → Optimize database queries if needed
3. @nextjs-developer → Apply Next.js performance optimizations
4. @test-automator → Add performance tests
```

**For Database Work:**

```
User: "Add a new table for surf session tracking"

Workflow:
1. @supabase-db-expert → Design schema, migrations, RLS policies
2. @api-designer → Design API endpoints for the new feature
3. @nextjs-developer → Implement API routes
4. @code-reviewer → Review security and data integrity
```

**For Bug Fixes:**

```
User: "Fix authentication issue"

Workflow:
1. @code-archaeologist → Trace the issue and understand context
2. [Appropriate specialist] → Implement fix
3. @test-automator → Add regression tests
4. @code-reviewer → Verify fix and security
```

## Project-Specific Guidelines

### Architecture Compliance (CRITICAL)

Before implementing ANY feature, you MUST:

1. Read relevant architecture documentation (37 ARCHITECTURE.md files exist):

   **Start here:**

   - `/docs/ARCHITECTURE.md` - Top-level architecture index

   **Core Documentation:**

   - `/app/ARCHITECTURE.md` - Next.js App Router patterns
   - `/app/api/ARCHITECTURE.md` - API routes architecture (710 lines)
   - `/components/ARCHITECTURE.md` - Component patterns (+ 17 subdirectory docs)
   - `/hooks/ARCHITECTURE.md` - Custom React hooks (838 lines)
   - `/lib/services/ARCHITECTURE.md` - Services and personalization
   - `/types/ARCHITECTURE.md` - TypeScript type system
   - `/supabase/ARCHITECTURE.md` - Database architecture and RLS patterns
   - `/e2e/ARCHITECTURE.md` - Playwright testing patterns
   - `/context/ARCHITECTURE.md` - React Context patterns
   - `/actions/ARCHITECTURE.md` - Server actions patterns
   - `/styles/ARCHITECTURE.md` - Styling conventions and Tailwind usage

2. Follow existing patterns - NO duplicate implementations
3. Maintain DRY principles throughout the codebase

### Data Fetching Pattern

**ALWAYS use the established data fetching pattern:**

- Use `useDataFetcher` hook for ALL data access
- Never implement ad-hoc fetch patterns
- Follow Supabase integration patterns from existing code
- Example: See `components/home-screen/index.tsx` for reference implementation

### Coordinate Naming Conventions (CRITICAL)

**ALWAYS follow coordinate naming standards to prevent mapping bugs:**

**Standard Naming**:

```typescript
✅ CORRECT: lat, lon, latitude, longitude
❌ INCORRECT: lng (do not use in new code)
```

**Layer-Specific Rules**:

1. **Database Layer**:

   - Legacy: `center_lat`, `center_lng` (PostGIS naming)
   - New tables: `latitude`, `longitude`
   - DO NOT change legacy columns without migration plan

2. **Component Props**:

   - Use full names: `latitude`, `longitude`
   - NOT: `lat`, `lon`, or `lng`

3. **API Parameters**:

   - Use short names: `lat`, `lon`
   - NOT: `lng`

4. **Database Mapping** (CRITICAL):

   ```typescript
   // WRONG - assumes property names match
   <Component latitude={beach.latitude} />  // beach.latitude doesn't exist!

   // CORRECT - explicit mapping
   <Component latitude={beach.center_lat} longitude={beach.center_lng} />
   ```

5. **Validation Required**:

   ```typescript
   import { validateCoordinates } from "@/lib/coordinate-validation";

   // Before API calls
   assertValidCoordinates(lat, lon, "API call context");

   // In components (development only)
   if (process.env.NODE_ENV === "development") {
     validateCoordinates(latitude, longitude, `Component: ${name}`);
   }
   ```

**Common Pitfalls**:

- Using `lng` instead of `lon`
- Assuming `beach.latitude` exists (it's `beach.center_lat`)
- Not mapping database fields to component props
- Swapping latitude and longitude values

**See**: `/docs/COORDINATE_CONVENTIONS.md` for comprehensive guide.

### Database Work

**For all database changes:**

1. Create migration files in `supabase/migrations/`
2. Follow naming convention: `YYYYMMDDHHMMSS_descriptive_name.sql`
3. Always include rollback procedures
4. Test RLS policies thoroughly
5. Document schema changes in migration comments

### Testing Requirements

**After ANY code change:**

1. Run relevant Playwright specs: `yarn test:e2e`
2. Use development-friendly waits from `e2e/ARCHITECTURE.md`
3. Validate visual/behavioral regressions with traces
4. Test across mobile AND desktop breakpoints
5. Maintain >80% test coverage for new features

### Performance Standards

**All implementations must meet:**

- Lighthouse Score: >90 (Performance, Accessibility, Best Practices, SEO)
- Core Web Vitals:
  - LCP (Largest Contentful Paint): <2.5s
  - FID (First Input Delay): <100ms
  - CLS (Cumulative Layout Shift): <0.1
- API Response Times: P95 <500ms
- Database Queries: <100ms for most operations

### Security Requirements

**Every feature must include:**

- Proper RLS policies in Supabase
- Input validation on all user inputs
- CSRF protection for mutations
- Rate limiting on API endpoints
- Secure handling of authentication tokens
- No sensitive data in client-side code

### Mobile Considerations

**For features affecting mobile:**

1. Test on both iOS and Android
2. Consider touch targets (minimum 44x44px)
3. Optimize for mobile networks (slow connections)
4. Test offline behavior
5. Handle mobile-specific permissions properly

### Documentation Requirements

**After implementing features:**

1. Update `CHANGELOG.md` with changes
2. Document new patterns in architecture docs
3. Add inline code comments for complex logic
4. Update API documentation if endpoints changed
5. Create migration guides for breaking changes

## Critical Reminders

### Orchestration Protocol (MUST FOLLOW)

**When handling complex tasks:**

1. **ALWAYS start with tech-lead-orchestrator** for multi-step tasks
2. **FOLLOW the agent routing map** returned by tech-lead EXACTLY
3. **USE ONLY the agents** explicitly recommended by tech-lead
4. **NEVER select agents independently** - trust tech-lead's expertise
5. **Apply deep reasoning** when coordinating recommended agents

### Agent Selection Flow

```
CORRECT FLOW:
User Request → tech-lead-orchestrator → Agent Routing Map → Execute with Listed Agents

INCORRECT FLOW:
User Request → Main Agent Guesses → Wrong Agent Selected → Task Fails
```

### Quality Gates

**Before merging any code:**

- [ ] Code reviewed by `code-reviewer` agent
- [ ] All tests passing (unit + E2E)
- [ ] Performance benchmarks met
- [ ] Security review completed
- [ ] Documentation updated
- [ ] CHANGELOG.md updated
- [ ] No console errors or warnings

## Communication with Agents

### Context Assessment

When starting work, agents query for context:

```json
{
  "requesting_agent": "agent-name",
  "request_type": "get_context",
  "payload": {
    "query": "Context needed: [specific requirements]"
  }
}
```

### Progress Tracking

Agents report progress with metrics:

```json
{
  "agent": "agent-name",
  "status": "in_progress | completed | blocked",
  "progress": {
    "metric1": "value",
    "metric2": "value"
  }
}
```

### Handoff Information

Agents structure returns for coordination:

```markdown
## Task Completed: [Task Name]

- [Key accomplishments]
- [Files modified]
- [Next specialist needs]: [Context for next agent]
```

## Common Scenarios

### Adding a New Feature

```
1. Requirements gathering and analysis
2. Database schema design (if needed)
3. API endpoint design
4. Frontend component implementation
5. Testing (unit + E2E)
6. Performance validation
7. Security review
8. Documentation
```

### Fixing a Bug

```
1. Reproduce and understand the issue
2. Trace the root cause
3. Implement fix with tests
4. Verify fix doesn't introduce regressions
5. Update relevant documentation
```

### Performance Optimization

```
1. Baseline measurements
2. Identify bottlenecks
3. Apply optimizations (queries, caching, rendering)
4. Measure improvements
5. Add performance tests
```

### Database Migration

```
1. Design schema changes
2. Write migration SQL with rollback
3. Test on staging environment
4. Update TypeScript types
5. Update affected code
6. Deploy with monitoring
```

## Important Files and Patterns

### Key Configuration Files

- `package.json` - Dependencies and scripts
- `tsconfig.json` - TypeScript configuration
- `next.config.js` - Next.js configuration
- `tailwind.config.ts` - Tailwind CSS configuration
- `playwright.config.ts` - E2E test configuration
- `capacitor.config.ts` - Mobile app configuration
- `supabase/config.toml` - Supabase local configuration

### Key Directories

- `app/` - Next.js App Router pages and layouts
- `components/` - React components (see ARCHITECTURE.md)
- `lib/` - Utility functions and shared logic
- `supabase/migrations/` - Database migration files
- `e2e/` - Playwright E2E tests
- `__tests__/` - Jest unit and integration tests
- `types/` - TypeScript type definitions
- `public/` - Static assets

### Key Patterns

- Server/Client Component separation (Next.js App Router)
- useDataFetcher hook for data access
- RLS policies for data security
- Error boundaries for graceful failure
- Loading states with Suspense
- Optimistic updates for better UX

## Development Commands

```bash
# Development
yarn dev                    # Start Next.js dev server
yarn build                  # Build for production
yarn start                  # Start production server

# Testing
yarn test:e2e               # Run Playwright E2E tests
yarn test:e2e:ui            # Run E2E tests with UI
yarn test:unit              # Run Jest unit tests
yarn test:coverage          # Generate coverage report

# Database
yarn db:types               # Generate TypeScript types from Supabase
yarn db:reset               # Reset local database
supabase migration new      # Create new migration

# Mobile
yarn mobile:sync            # Sync web build to mobile
yarn mobile:build:ios       # Build iOS app
yarn mobile:build:android   # Build Android app

# Code Quality
yarn lint                   # Run ESLint
yarn typecheck              # Run TypeScript checks
```

## Additional Resources

- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Playwright Documentation](https://playwright.dev/docs/intro)
- [Capacitor Documentation](https://capacitorjs.com/docs)

---

**Remember**: Always use the tech-lead-orchestrator for multi-step tasks, follow the agent routing map, and maintain the high quality standards that make Quiver an excellent surfing application.

- "if the test don't run you didnt not successfully fully implement an update"
