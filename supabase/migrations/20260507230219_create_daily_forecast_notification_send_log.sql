-- Backfilled from remote supabase_migrations.schema_migrations on 2026-05-11.

CREATE TABLE IF NOT EXISTS public.notification_send_log ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, beach_id uuid NOT NULL REFERENCES public.beaches(id) ON DELETE CASCADE, notification_type text NOT NULL CHECK (notification_type IN ('daily_forecast')), local_forecast_date date NOT NULL, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now(), UNIQUE (user_id, beach_id, notification_type, local_forecast_date) ); COMMENT ON TABLE public.notification_send_log IS 'Durable per-user/per-beach/per-local-date notification send claims for producer idempotency.'; COMMENT ON COLUMN public.notification_send_log.local_forecast_date IS 'Forecast-local date used for daily forecast push idempotency.'; CREATE INDEX IF NOT EXISTS idx_notification_send_log_user_created ON public.notification_send_log (user_id, created_at DESC); DROP TRIGGER IF EXISTS trg_notification_send_log_set_updated_at ON public.notification_send_log; CREATE TRIGGER trg_notification_send_log_set_updated_at BEFORE UPDATE ON public.notification_send_log FOR EACH ROW EXECUTE FUNCTION public.set_updated_at(); ALTER TABLE public.notification_send_log ENABLE ROW LEVEL SECURITY; DO $$ BEGIN IF NOT EXISTS ( SELECT 1 FROM pg_policies WHERE schemaname = 'public' AND tablename = 'notification_send_log' AND policyname = 'notification_send_log_service_role_all' ) THEN CREATE POLICY notification_send_log_service_role_all ON public.notification_send_log FOR ALL USING (auth.jwt() ->> 'role' = 'service_role'); END IF; END $$; CREATE OR REPLACE FUNCTION public.claim_daily_forecast_notification_slot( p_user_id uuid, p_beach_id uuid, p_notification_type text, p_local_forecast_date date ) RETURNS boolean LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$ DECLARE v_claimed_id uuid; BEGIN INSERT INTO public.notification_send_log ( user_id, beach_id, notification_type, local_forecast_date ) VALUES ( p_user_id, p_beach_id, p_notification_type, p_local_forecast_date ) ON CONFLICT (user_id, beach_id, notification_type, local_forecast_date) DO NOTHING RETURNING id INTO v_claimed_id; IF v_claimed_id IS NULL THEN RETURN false; END IF; RETURN true; END; $$; COMMENT ON FUNCTION public.claim_daily_forecast_notification_slot(uuid, uuid, text, date) IS 'Atomically claims a daily forecast notification slot. Returns false when the same user/beach/type/local date was already claimed.'; REVOKE ALL ON FUNCTION public.claim_daily_forecast_notification_slot(uuid, uuid, text, date) FROM PUBLIC; GRANT EXECUTE ON FUNCTION public.claim_daily_forecast_notification_slot(uuid, uuid, text, date) TO service_role; NOTIFY pgrst, 'reload schema';
