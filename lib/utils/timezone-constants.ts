/**
 * Timezone Constants
 *
 * Shared timezone constants used across the application.
 * @module lib/utils/timezone-constants
 */

/**
 * Default timezone used as fallback when coordinates are unavailable.
 * Pacific Time is appropriate for the majority of US West Coast surf spots.
 */
export const DEFAULT_TIMEZONE = 'America/Los_Angeles';
