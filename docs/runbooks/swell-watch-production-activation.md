# Swell Watch — activation runbook draft

Status: **NOT RELEASE READY / NOT ACTIVATED**, 2026-09-05. This is a readiness draft, not an executable production change authorization. Native evidence lives in the companion repository at `docs/release/phase-26-swell-watch-readiness.md`.

## Current audit — supersedes historical implementation gaps below

Production installation draft: [exact candidate migration manifest and no-send boundaries](swell-watch-no-send-install-plan.md). All 13 listed hashes match current files. Fresh read-only queue metadata: 1,548,288 bytes, estimated 1,572 rows, zero observed queue locks/transactions older than five minutes, and still no Swell Watch tables. This is not backup evidence or production approval. The draft identifies remaining backup, commit/review, bounded-runner and approval-token requirements; do not execute it as-is.

### Collection approval separation — 2026-09-05

The existing POST acquisition action now validates the fixed server cohort independently of release policy approval. `SWELL_WATCH_PRODUCER_CONFIG` may contain only `cohort` for raw collection; an optional policy is ignored by acquisition. Completed-batch requests still require the original verified production-approved v2 policy before any database access. Acquisition remains `prototype_unqualified`, never completes a batch, and never calls processing/enqueue. This removes a circular prerequisite; it does not qualify evidence or start the 30-day clock.

Changed this increment: `app/api/cron/swell-watch/route.ts` (only production source), `__tests__/api/cron/swell-watch.test.ts` (six regression cases), and this runbook. Reviewed existing route tests and acquisition storage flow; no browser/native E2E tests changed or run because this increment changes server configuration validation only. Full release E2E remains incomplete. Local review found and fixed Jest array-row expansion in the new negative cases; the first test run failed one timeout and typecheck failed that test signature. Both reruns passed:

- PASS `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only SUPABASE_SERVICE_ROLE_KEY=fixture-only NEXT_PUBLIC_SITE_URL=http://localhost:3000 yarn test:unit --runInBand --silent swell-watch` — 28 suites, 271 tests, 3.943s.
- PASS `yarn typecheck` — 5.74s.
- PASS `node_modules/.bin/eslint --max-warnings=0 app/api/cron/swell-watch/route.ts __tests__/api/cron/swell-watch.test.ts`.
- PASS `git diff --check`.

No production configuration/schema changes, real provider acquisition, notifications (including device tests), commit, push, deployment, or OTA publication. Production no-send installation still needs the exact reviewed migration plan, fresh backup and required approval token; entitlement, qualification and the running scheduler remain unverified. Keep all Swell Watch delivery controls disabled for the full verification period; no automatic activation afterward.

Migration collision resolved locally: renamed the unapplied dedupe migration to `20260904120001_add_swell_watch_v2_enqueue_dedupe.sql` and updated its three PostgreSQL harness references. SQL behavior is unchanged; only an explanatory comment was added. Read-only production SELECT confirms version20260904120001 is absent, the Swell Watch dedupe constraint/index are absent, and the v2 queue contains0rows. Existing Weekend Scout version20260904120000 was not touched. Historical native preflight JSON and dated summaries retain their old version as historical evidence; they must not be reused as a current migration manifest.

Verification: PASS `bash -n scripts/test-swell-watch-provider-run-receipts-postgres.sh scripts/test-swell-watch-event-pipeline-postgres.sh scripts/test-swell-watch-worker-postgres.sh`; PASS `bash scripts/test-swell-watch-worker-postgres.sh` (8/8tests,48.545s; recording-only provider). PASS `git diff --check`. Files changed: renamed migration, three harness scripts, this runbook. No application source/test assertions changed; unit/typecheck/browser/native E2E not rerun for the path-only change. Full release E2E remains incomplete. No production migration, tracking repair, notification, commit, push or OTA publication. Next is the exact no-send migration plan and fresh backup under the repository protocol, not activation.

Operator decision: use the existing production database for30days of verification and send no Swell Watch notifications during that period. This supersedes the non-production environment question below. It does not authorize live-user activation, OTA publication, table deletion, or bypassing `docs/MIGRATION_SAFETY.md`'s fresh-backup/exact-plan approval protocol. Keep notification channels/static delivery disabled and durable control unarmed throughout collection; existing unrelated notifications must remain unaffected. Record actual qualifying coverage, not merely elapsed calendar time. No automatic activation after day30.

Production preflight: `supabase projects list --output json` PASS using existing CLI authentication; `quiverDB` project `vawdnbbgawichorsjiwe` is ACTIVE_HEALTHY. The primary checkout's existing `supabase/.temp/project-ref` matches it; no credentials or `.env` values were opened. Read-only metadata queries through `supabase --workdir /Users/stevenchandler/Desktop/dev/quiver db query --linked ... --output json` PASS. A single JSON inventory query confirms no public `swell_watch_%` tables and no public `%swell_watch%` functions. Of the13local migration versions, production already tracks `20260904120000`; a follow-up SELECT identifies it as `add_weekend_scout_candidate_paging`, not the local Swell Watch dedupe migration. This is a timestamp collision, not an applied Swell Watch migration. Rename the unapplied local migration and update its harness references before any migration plan/application; do not alter the production tracking row. No production business data or schema changed, no backup claimed, no collection started. Only runbook/native checkpoint changed this increment; both `git diff --check` commands PASS. Tests/E2E not rerun for read-only preflight.

Local acquisition is now wired through authenticated POST with `{ "action": "acquire" }`, server-owned cohort/configuration, availability-selected pinned runs and unqualified-only persistence. The separate completed-batch POST invokes attested whole-cohort ingestion, matching, safety accounting and guarded enqueue. Owner qualification is not automated or inferred. Latest Swell Watch units PASS28suites/265tests; real local PostgreSQL worker drill PASS8/8 including metadata-selected acquisition, self-attestation refusal, persisted timing copy and held/stale suppression; typecheck/scoped lint pass. Exact commands are in `swell-watch-local-producer-attestation.md`. No production migration/deployment/configuration or schedule was applied.

Read-only local source audit confirms registry `swell_watch.channels: []` and existing GET cron `0 15 * * *`; these are not deployed-state claims. The bounded provider-name search found no WaveCast/Surfline references in Swell Watch detection/cron/control paths, but is not a complete dependency/base-diff certification. The approved dedicated GFS detector amendment supersedes older detector NWS/Best-Match source fences, not display compatibility or provenance requirements.

Branch-creation reflogs resolve the worktree bases: web `cb6d545da0634b00ad627547352ddf614cdd328e`, native `f2eea026f9db76b1ab6263d1a5485d32d581f55e`; each still equals its current HEAD. Commands: `git reflog show --format='%H %gs' orch/phase-26-swell-web` and native equivalent `orch/phase-26-swell-native`, plus `git log -1 --format='%H %s'`. Initial attempt to combine reflog with `--reverse` failed (unsupported); ordinary reflog succeeded. `git diff HEAD -- package.json yarn.lock` (web) and `git diff HEAD -- package.json package-lock.json` (native) show no dependency changes. Current inventory is62tracked changed files/85untracked(web),29tracked/45untracked(native); full audit must include untracked implementation, not merely git diff. Inspected Swell Watch endpoint literals are Single Runs and marine metadata; legacy advisory adapter uses `api.weather.gov`. No external provider client found within those inspected paths. This is bounded source evidence, not a completed full-diff review.

Latest `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only SUPABASE_SERVICE_ROLE_KEY=fixture-only NEXT_PUBLIC_SITE_URL=http://localhost:3000 yarn deadcode` FAIL exit1: six unused files, unused/unlisted dependencies,559unused exports and other repository-wide findings. No broad unrelated deletion or check bypass attempted. This remains a failed gate, not an accepted waiver. The data-backed production build also remains unverified with usable non-production data.

Still missing: designated operational database/cohort and credential authority; independently supported provider qualification/entitlement; qualifying30observed-day corpus; detector/display compatibility; real provider delivery and once-only analytics canary; full scope/release audit. Pixel local foreground/background/fresh-process routing was already verified and retained artifact hashes were rechecked; do not repeat it as though it were missing provider-delivery evidence. No Android device was attached at the latest audit. Full E2E remains PARTIAL/NOT PASSED. Implementation progress and standing local authorization do not turn these unavailable inputs into passing evidence. OTA and activation remain held.

The synthetic-evidence dispatch bypass is locally fixed and parent-reviewed: the public RPC now requires current genuine destination/supporting evidence from two distinct issuances. Parent independently passed the full-schema worker drill and 80 focused tests. See `swell-watch-release-identity-review.md` for exact commands, hashes and limitations. This is an unapplied local migration, not a production deployment or qualification of the prototype provider.

## Current implementation boundary

Plan 26-07 is partial. The legacy authenticated shadow cron remains intact; the proposed push path stops with `missing_immutable_issuance`. The local receipt store accepts only `prototype_unqualified` inputs, and completion requires a separate current accepted attestation. There is no authorized producer of that attestation, production completed-batch adapter, durable producer orchestration, or end-to-end producer accounting. Test-only composition and private snapshot replay do not provide those capabilities.

The cycle-5 replay timestamp mismatch is resolved after the user resumed work: actual retained `+00:00` timestamps now pass, with explicit timezone/finite validation and unchanged artifact hashes. The latest authorized collection completed with 43,279 new and 55,788 deduplicated observations. Parent verified its CLI twice without output rewrite and validated all six V2 captures (242,965 rows) in both capture orders. This fixes replay, not genuine issuance: candidate/stability/audience outcomes remain unknown.

The companion native `26-07-PRODUCER-CONTRACT-HANDOFF.md` now records Open-Meteo's documented exact-run binding, full-horizon, and unavailable-run semantics. That advances the source contract but does not qualify existing receipts. The existing forecast builder may mix CDIP S1 with wave-point S2, so a row-level NOAA/OM label is insufficient; non-allowlisted/unknown constituents must remain suppressed for Swell Watch without changing legacy forecast serving.

Forecast ingestion must first provide an immutable provider-issued completed batch/evaluation identity, canonical NOAA/Open-Meteo S1/S2 provenance, completeness, and a continuity relation. Mutable `updated_at`, capture time, content hashes, and synthetic fixture IDs are not substitutes. The current code contains only the public Single Runs endpoint and no customer endpoint/API-key integration; no account, environment file or secret was inspected. Commercial production acquisition remains blocked on confirmed Professional-or-higher entitlement and separately approved secret handling. Design and review the additive producer/storage contract before wiring orchestration; do not repurpose face-height adjustment issue IDs.

The registry retains `channels: []`; the existing cron remains `0 15 * * *`. No live environment value was inspected or changed. Static source checks are not proof of deployed state. The eventual cadence must follow genuinely completed evaluations; its schedule is not yet approved.

## Policy and evidence blockers

Cross-repository fixture parity revalidated with Node `assert.deepEqual` on parsed web/native `swell-watch-v1.json` and `swell-watch-v2.json`, then SHA256 of recursively key-sorted JSON: v1 `24b0e30920cc05d8aad66a85d2a3edf5a273574b637fd0583d905066d01f3608`; v2 `41f02bbe0fc84118f52d8d683fcf9ba000b58cdeca3df2c6becc7c77801c94e0`. Read-only command exited0. These are fixture parity hashes, not real corpus/approval hashes. The six unused-file findings (`components/map/map-display.tsx`, `components/map/map-regions.ts`, `components/map/surf-map-prototype.tsx`, `components/ui/check-in-form.tsx`, `hooks/use-personalization-milestones.ts`, `lib/utils/personalization-messaging.ts`) have no diff from the web base/HEAD; no deletion authorized or performed. This does not prove every global Knip finding predates Phase26 or waive the failed gate. No code/test/E2E change or run in this parity increment; documentation only.

Latest bounded provider-fence rerun: PASS `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only SUPABASE_SERVICE_ROLE_KEY=fixture-only NEXT_PUBLIC_SITE_URL=http://localhost:3000 yarn test:unit --runInBand --silent swell-watch-partition-normalizer swell-watch-seam` (2suites/3tests,0.43s), including explicit unknown-provider suppression. No source/tests changed in this audit; only this runbook. `git diff --check` PASS. No E2E run; full scope/runtime qualification still incomplete. Operational environment question remains unanswered; automatic continuation is not a target selection.

Only the provisional fixture policy exists: `9a278ceca6c2fde80358e19258e2f6118e564735b8eb762266bd682c494df2ad`. It is not production approved. Exact production detector, volume, freshness, disagreement, and provider-failure thresholds remain **unapproved**; never copy fixture values into authority records.

Require 30 qualifying observed days, genuine independent completed evaluations, active-scope and seam coverage, reviewed candidate/event/audience distributions, real corpus/manifest/policy hashes, and an explicit operator decision. Retained V2 snapshots currently have zero genuine completed evaluations. Missing outcomes remain unknown, not zero. Collector age cannot unlock activation.

## Future integration and deployment order — approval required

1. Finish and independently review the canonical producer bridge, durable event/control integration, once-only enqueue, and error accounting. The producer calls existing enqueue directly; it must not pre-claim the worker's recipient-announcement receipt. The v2 recipient/regional-event queue uniqueness constraint arbitrates retries; ownerless historical claims remain blocked.
2. Re-run separate web/native gates, canonical v1/v2 fixture equality, SQL/RLS/concurrency probes, dependency/provider scope audit, and installed-app destination/lifecycle verification.
3. Follow `docs/MIGRATION_SAFETY.md`: review the exact migration set and deployed drift, identify the target/role and fresh backup, hash the plan, obtain the required `APPROVE: <sha>`. Do not apply uncommitted migrations or assume the local baseline matches production.
4. With separate authorization, apply reviewed additive migrations before deploying consumers. Preserve old v1 payload support while deploying v2-capable backend and native consumers. Keep channels, static flag, durable control, and cadence disabled/unchanged throughout validation.
5. Complete a separately authorized dedicated non-production drill and device canary. Only then may Plan 10 record its evidence-bound decision. Plan 11 alone owns explicit approved production mutations: registry push channel, completed-batch schedule, deployment/static enablement, reviewed authority binding, and final durable arm. The exact values and mutation hashes must be filled in and reviewed first.

## Hold, reset, and rollback protocol — local database/worker drill passed; physical canary pending

### Local worker drill preparation — 2026-09-04

The reproducible local runner now exists: `bash scripts/test-swell-watch-worker-postgres.sh`. It creates the dedicated `phase26-worker-drill` Supabase database, loads the committed full schema snapshot plus the existing device-trust and four Phase 26 migrations, seeds only local fixture rows, and exposes PostgREST on loopback port 55433. It refuses an existing named drill container and removes its database/REST containers on exit. The shared Quiver stack is not touched. Dependencies must already be available in the worktree.

`swell-watch-worker-postgres.drill.ts` now passes against that real database for static disable, durable hold, reset-to-shadow suppression, and rejection of an armed-era queued event after hold/reset/re-arm. Each blocked case asserts the persisted exact reason, terminal queue state, attempt count, zero recording-transport calls and empty next tick. A fresh synthetic event then reaches the recording transport exactly once with its partition context intact; its sent attempt and provider outcome are persisted, and another tick does not resend. The synthetic authority is local fixture data, not production approval or genuine shadow evidence. External Firebase is prohibited and analytics are mocked. The earlier fetch-mock setup failure was fixed using real HTTP restricted to loopback. Physical delivery and deployed-worker configuration remain unverified.

Worker regression coverage now executes `processPendingEvents` for Swell Watch with the test-local push channel enabled: an eligible v2 payload reaches the recording FCM transport exactly once, preserves its partition context and records the provider outcome; static disable, held control, stale notification binding and unavailable provider evidence record `skipped_disabled` with their exact reason and terminal queue status, with no dispatch on a later tick. `__tests__/notifications/worker.test.ts` reuses the existing in-memory storage fixture. The worker plus release-helper suites passed 78 tests, strict scoped ESLint and typecheck passed. This closes the missing worker-call-site regression coverage; it does not prove real SQL hold/reset integration or physical delivery.

The separate reduced-schema SQL harness remains useful for race tests but cannot prove worker execution. The new full-snapshot runner closes that local integration gap through `processPendingEvents` with an injected recording transport; it does not establish real-provider delivery.

Prepare the drill using the committed schema snapshot and actual notification migrations in a new isolated local stack. Do not reuse or reset the running shared `supabase_*_quiver` stack. Reuse the existing Phase 26 policy/event fixtures; keep synthetic identities explicit. Invoke `processPendingEvents` with the local client and a recording transport that cannot contact FCM, and disable external analytics. Enable the push registry entry only within the test process so an empty channel list cannot make the hold test pass vacuously. First prove an otherwise eligible fixture reaches the recording transport; then use distinct queued fixtures to prove static disable, durable hold, explicit reset and old-binding rejection. Assert persisted delivery attempts, terminal queue state, exact suppression reasons, zero transport calls for each blocked case, and no stale-row delivery on another worker tick. Preserve the independent SQL race tests. This is local integration evidence; the physical-device/provider canary remains a separate gate.

Device preflight: `xcrun devicectl list devices` succeeded but reported the sole paired iPhone as `unavailable`. No device action or notification was attempted. Before a physical canary, resolve an available dedicated device, its installed build/runtime, test account, backend and provider project; the previous generic request for one test notification did not identify those targets. The Phase 26 objective explicitly requires authorization for controlled-device sends and credential access once these targets are concrete.

The existing admin endpoint is `/api/admin/swell-watch-control`. GET reads control; authenticated POST accepts `hold`, `reset_shadow`, or `arm`, an `expectedEpoch`, a bounded `reasonCode`, and an `Idempotency-Key` header. Use existing secret management; do not put credentials in this document or command logs.

For the non-production drill, queue a known eligible fixture row, hold at the expected epoch, then run the worker and prove zero provider sends plus typed suppression. Reset to shadow using an audited reason and idempotency key; prove the old row is discarded, not released as backlog. Re-evaluate a fresh genuine event before any authorized arm. An epoch conflict requires a fresh control read and operator review, not a blind retry with an invented epoch.

The disposable PostgreSQL harness now proves the database portion: provider-failure hold, explicit reset to shadow, explicit re-arm, and rejection of the pre-reset queued binding as `notification_binding_mismatch`. It also proves stale-forecast rejection and stale control-epoch failure. This is synthetic fixture evidence only; it does not run the installed worker or prove zero real provider sends, so the non-production worker drill remains required.

Automatic hold reasons include candidate/recipient/projected-send caps, discontinuity, material disagreement, stale forecasts, and provider-failure rate. The helpers and final worker gate exist, but producer-derived live signals and dashboards are unfinished. Missing authority/control or unverified event binding must fail closed.

After future activation, monitor stage denominators and suppression reasons, regional-event correlations, once-only queue/receipt outcomes, classified provider failures, destination outcomes, and source continuity. Never log recipients, devices, tokens, relationship rows, or raw private snapshots. Stop on cap breaches, missing lineage, duplicates, unexpected fan-out, or destination regression.

Rollback starts with durable hold plus static disable under explicit operational authority; verify zero subsequent sends and stale-row suppression. Restore reviewed compatible application artifacts if needed, preserving append-only evidence and queue receipts. Do not delete evidence, reset dedupe, or re-arm automatically. Retest and obtain new evidence-bound approval before restart.

## Approval record

Operator/reviewer: not recorded. Production policy hash: unavailable. Approved corpus/evidence hash: unavailable. Dedicated-device and kill/reset drill: not run. Activation decision: **pending; prerequisites unmet**. No migration, deploy, schedule/flag/channel change, arm, push send, commit, or OTA publication was performed by this readiness work.
