# Swell Watch exact-north receipt normalization

## Scope and authorization

Steven approved developing, testing, reviewing and applying the coordinated parser/database correction on September 13, 2026. This authorization does not change the cohort, evaluation policy, study authority, 12-hour freshness limit, four-issuance qualifying-day rule, expiry, or disabled sending. No manual production acquisition, acceptance, completion or evaluation is needed.

## Contract

Only a finite numeric raw direction exactly equal to 360 in an otherwise valid tuple becomes canonical 0. The raw response and canonical semantic payload still contain 360; both hashes continue to cover that original evidence. The parsed component retains `rawFieldProvenance.directionNormalization = {rule: "north_360_to_0.v1", rawDegrees: 360, canonicalDegrees: 0}`. This additive rule is versioned independently of the unchanged receipt envelope. Existing valid receipts and identities do not change.

Database canonical directions remain `>=0 AND <360`. The writer independently requires the exact normalization marker, original raw value 360, and positive period before permitting the raw/canonical difference. Missing/forged metadata, arbitrary wrapping, out-of-range directions, strings, nulls, invalid periods and a fabricated unavailable tuple from raw `(0,0,360)` are rejected. Original raw `(0,0,0)` remains unavailable, not a usable swell.

The repeatable production counterexample was measured in Actions run 34769460344 at 2026-09-13T16:44:12.022Z: Rincón source f11ccd59-b778-4ea1-a8ff-88bffb447cd8, requested issuance 2026-09-13T06:00Z, hour146 / 2026-09-19T08:00Z, primary swell, raw .63m / 8.36s / 360 degrees. It is a replay, not the lost original production body.

## Migration and rollback

Migration: `supabase/migrations/20260913192652_normalize_swell_watch_provider_direction.sql`. The development filename was generated by the official Supabase CLI in isolated Actions run 34771127945. After the approved Supabase connector applied and tracked the exact SQL as version `20260913192652`, the repository filename was aligned with that canonical remote version. SQL bytes are unchanged; SHA-256 `edeb9e81973ff165a358b9d6a25d2029df47236ad4d9b00dbdbd1ca7cf1e05ea` matches the retained migration statement.

Only `public.record_swell_watch_provider_run_receipt(jsonb)` changes. Four exact expression replacements are guarded by the complete baseline and result definition hashes. Existing grants, constraints, reads, completion functions, tables and retained data do not change. Lock timeout is five seconds; statement timeout is thirty seconds; all changes are transactional.

- Before definition SHA-256: `26d064b92fb0023f079d399ca062833f7d9a882d53d1f1e9ab55ef1a5726f522`.
- After definition SHA-256: `0374cd88bf8a6edf727a82dea6d90e6b9d4962dbe9d812c0581651a69d4c8dd4`.
- Apply the reviewed migration before deploying the parser. The migration accepts unchanged old receipts; the old parser still rejects raw360 until deployment.
- Rollback artifact: `docs/operations/swell-watch-direction-normalization-rollback.sql`. Reverting the writer leaves all retained normalized components, raw evidence and provenance intact. Do not rewrite/delete evidence or revoke study authority as a normalization rollback.
- Preflight must reconfirm owner role, definition hash, disabled sends and unchanged authority. Retain a fresh affected-function definition and grants as the scoped rollback backup; this is not a full database pg_dump. Reconcile only this migration's tracking with its committed file and verify the post-apply definition hash.

Post-apply read-only verification at `2026-09-13T19:27:21.488237Z` confirmed the exact new definition hash, one tracked migration with matching SQL hash, unchanged function grants and unchanged study-authority/evaluation-policy snapshot hashes. All 698 existing raw responses remained retained. Durable send control remained disabled with zero production approval authorities and zero notification bindings. At that point the application deployment was still pending and study progress remained zero evaluated runs and zero qualifying days.

## Verification meaning

`bash scripts/test-swell-watch-study-postgres.sh` extends the existing disposable PostgreSQL suite. It runs the actual parser, receipt adapter, SQL acceptance/completion, attested loader, TypeScript shadow evaluator and SQL result recorder at the production 12-hour freshness limit. SQL responses are not mocked. It tests canonical storage, exact raw preservation, malformed provenance rejection, migration/retry idempotence and evidence-preserving rollback. The zero-candidate successful case and zero-tuple suppressed case use explicitly synthetic ten-source data in the disposable database only. They do not qualify a production day.

The incident-branch-only provider replay uses one bounded unauthenticated GET, no production database calls and sanitized evidence output. A successful replay is unqualified acquisition evidence, not automatic acceptance or a study evaluation.

After deployment, independently read the actual scheduled invocation and retained lineage. Successful normalization does not resolve missing-partition or unbounded/unclosed episode suppression. An unattended qualifying day requires all four genuine, fresh, complete ten-source evaluations under the unchanged authority; neither this test suite nor a READY deployment substitutes for that evidence.
