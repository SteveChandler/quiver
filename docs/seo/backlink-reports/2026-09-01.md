# Weekly Backlink Scan - 2026-09-01

Run at 2026-09-01 08:40 PDT / 15:40 UTC, from
`fix/seo-outreach-digest-copy-and-beach-column` (dirty working tree, all changes
pre-existing and unrelated to this routine). Audit folder resolved to the UTC
date, `2026-09-01`.

Headline: **both of last week's `[READY TO SHIP]` items are now live in
production and verified this run** — `/water-temp` and `/surf-cams` redirect
instead of 404ing, and beach pages now link to their own water-temp sub-pages
(7 of 7 sampled). The orphan root cause identified on 25 Aug is fixed at the
source. Ahrefs cannot confirm it yet: its latest completed crawl is still
2026-08-24 and predates the deploy, so the health-24 figure is stale evidence,
not a current reading.

The one genuinely new finding is a **duplicate cam page family**: `/cams/{region}`
and `/surf-cams/{region}` both exist, both are `index, follow`, both self-
canonical, and they overlap on Florida and Hawaii. The family earning the traffic
(`/surf-cams/*`, the #3 page sitewide) has **zero** internal links from any hub.

## Run summary

| Command | Outcome | Output |
| --- | --- | --- |
| `yarn seo:technical-audit` | exit 0, 9.79s, **0 recommendations** | `../Brand-Vault/seo-audit/2026-09-01/TECHNICAL-AUDIT.json` |
| `yarn seo:export:vercel` | exit 0, 1.92s, Vercel CLI stored token still valid | `../Brand-Vault/seo-audit/2026-09-01/VERCEL-EXPORT.json` |
| `yarn seo:backlink-proxy` | exit 0, 0.75s, `missing: []` | `../Brand-Vault/seo-audit/2026-09-01/BACKLINK-PROXY.json` |

Run in the documented order, so `referrers` is populated (17 rows) rather than
empty. `missing` is an empty array — the discriminator that matters: the Vercel
export resolved its stored token, and the empty `embedReferrers` is a **genuine
zero**, not a failed read.

## Technical crawl health

`TECHNICAL-AUDIT.json` is `[]`. That is a real clean result, not a blocked crawl:
a blocked crawl emits exactly one "could not reach the site" recommendation. The
site was independently confirmed reachable during this run — `robots.txt` HTTP 200
(0.12s), `sitemap.xml` HTTP 200 (2.01s), homepage HTTP 200 (0.16s).

**State the cap plainly: this is not a full-site guarantee.** `technical-audit.ts`
crawls the homepage plus the first 10 dashboard entries (11 pages), extracts their
internal links, and checks **at most 40 unique links** (`MAX_LINK_CHECKS = 40`,
`scripts/seo/technical-audit.ts:23`) with an **8-second timeout** each
(`LINK_CHECK_TIMEOUT_MS`). The sitemap advertises **1,615 URLs**, so the sample is
roughly 2.5% of the site by link count. Network and timeout errors are
deliberately swallowed as inconclusive (`checkLinkStatuses`, empty `catch`), so a
link that hangs past 8s cannot appear as a finding either.

**No broken links found. No status codes to report.** Zero broken links means zero
broken links *inside that 40-link sample*.

### Last week's orphan root cause is fixed — verified live this run

The 25 Aug report identified two `[READY TO SHIP]` fixes. Both are now in
production. Verified by direct fetch on 2026-09-01, not from a build log:

| Check | 2026-08-25 | 2026-09-01 | Method |
| --- | --- | --- | --- |
| `/water-temp` | HTTP 404 | **HTTP 308 → `/beaches/usa`** | `curl` no-follow |
| `/surf-cams` | HTTP 404 | **HTTP 308 → `/cams`** | `curl` no-follow |
| Beach page → own `/water-temp` sub-page | 0 of 1 sampled | **7 of 7 sampled** | `href` grep on live HTML |

The seven beach pages sampled — Swami's, The Wedge, Venice Beach, 7th Street
Beach NJ, Asilomar, Kill Devil Hills, Tourmaline — each carry exactly one link to
their own `/water-temp` sub-page, and every one of those sub-pages returns 200.
The linking is systematic, not spot-applied. PR #618 (`codex/seo-water-temp-prod`)
is the apparent vehicle.

Beach-level water-temp pages in the sitemap grew **346 → 461** over the week, and
the sitemap overall **1,381 → 1,615**. The new pages arrive already linked, so
they should not join the orphan population.

### The clean local result is still contradicted by a much wider crawl — but that crawl is stale

`AHREFS-SCREENSHOT-INPUT.json` is **not** present in today's folder. The most
recent capture is dated **2026-08-31** and it describes a Site Audit crawl dated
**2026-08-24**. These are not this run's numbers, and the crawl **predates the
internal-linking deploy verified above**:

| Ahrefs Site Audit (crawl 2026-08-24, captured 2026-08-31) | Value |
| --- | ---: |
| Health score | 24 |
| Internal URLs with errors | 246 |
| Orphan pages (no incoming internal links) | 230 (+226 added vs 17 Aug) |
| Total issues | 1,442 (246 errors, 336 warnings, 860 notices) |
| Links found / crawled / uncrawled | 8,901 / 3,668 / 5,231 |
| Crawl maximum reached | yes — counts are floors, not sitewide |

The 08-31 capture also surfaces issues not in last week's list: **291 changed
pages not submitted to IndexNow** (138 changed), **6 pages that became
non-indexable**, **2 URLs with slow server response for AI crawlers**, and **1
page dropped from the top 10**.

**Do not read health 24 as the current state.** It measures the site as of 24 Aug.
The next Ahrefs crawl is the first that can show whether the orphan fix worked;
until then the local 40-link sample and the capped Ahrefs crawl disagree for a
known and now-addressed reason.

### New finding: two competing cam page families, and the wrong one is orphaned

Verified live this run:

- `/cams/{region}` — **8** regional pages (`southern-california`,
  `northern-california`, `central-california`, `pacific-northwest`, `texas`,
  `florida`, `hawaii`, `east-coast`), all linked from the `/cams` hub.
- `/surf-cams/{region}` — **4** pages in the sitemap (`san-diego`,
  `orange-county`, `hawaii`, `florida`), linked from **zero** of the six hub pages
  checked (`/`, `/beaches`, `/forecast`, `/map`, `/cams`, `/tools/water-quality`).

Both families are `index, follow` and each page self-canonicalises. Florida and
Hawaii exist in both, with different titles competing for the same query:

| Path | Title | Canonical | Robots |
| --- | --- | --- | --- |
| `/cams/florida` | Live Surf Cams in Florida — 4 Cameras \| Quiver | self | index, follow |
| `/surf-cams/florida` | Florida Live Surf Cams \| Quiver | self | index, follow |
| `/cams/hawaii` | Live Surf Cams in Hawaii — 8 Cameras \| Quiver | self | index, follow |
| `/surf-cams/hawaii` | Hawaii Live Surf Cams \| Quiver | self | index, follow |

The orphaned family is the one that earns: **`/surf-cams/san-diego` is the #3 page
sitewide this week at 58 visits**, and `/cams/san-diego` returns **404** — the
region has no equivalent in the linked family. `/surf-cams` now 308s to `/cams`,
which links to none of the four `/surf-cams/*` pages, so the redirect currently
routes users away from the better-performing family.

This is a consolidation decision, not a defect with an obvious correct repair, so
nothing was changed. See `[NEEDS DECISION]` below.

### Observation: beach-level `/tide` pages are live but unlisted

`/{state}/{city}/{beach}/tide` returns 200 (checked The Wedge, Swami's) but **zero**
such URLs appear in the sitemap, versus 461 for `/water-temp`. Beach pages link to
neither. This may be the sitemap indexability predicate correctly excluding them
for want of data; it is recorded as an observation, not a finding, because this
run did not establish intent.

## Backlink and referrer signals

### Data-source status

- **`referrers`** — working source, populated by `yarn seo:export:vercel`. 17 rows.
  Traffic-side, mostly search engines. **Not a backlink index**; do not report the
  list as backlinks.
- **`embedReferrers`** — live Supabase read of `embed_impressions`, last 7 days.
  Empty array with nothing in `missing`, so this is a **genuine zero**: no embed
  impressions in the window. Fourth consecutive week at zero.
- **`manualExports`** — **empty. No data source connected.** This is not a finding
  and it is not "zero backlinks" — it means no file was there to read. Last week's
  folder contained a hand-captured `REFERRING-DOMAINS.csv` (214 rows); imports are
  per-folder and it was not carried into `2026-09-01/`. The named files that would
  populate this are `AHREFS-WEBMASTER-TOOLS.csv` (per-domain backlink index) and
  `GSC-LINKS.csv` (Google-reported inbound links), droppable in
  `../Brand-Vault/seo-audit/2026-09-01/` or `docs/seo/backlink-reports/`.
  Filenames must match that folder's README exactly.

### Aggregate backlink numbers — one day old, and flagged as such

The 2026-08-31 capture **does** carry a `summary.siteExplorer` block, which closes
last week's `[DROP EXPORT]` ask for one. Captured 2026-08-31, reported here as
last-known, explicitly **not** as a new measurement:

| Ahrefs Site Explorer (captured 2026-08-31) | Value | 30d change |
| --- | ---: | ---: |
| Domain Rating | 0 | — |
| **Referring domains** | **188** | **+39** |
| Organic keywords | 242 | +142 |
| Organic traffic | 1,200 | +779 |
| Organic traffic value | $438 | — |
| Total visitors | 9,000 | +5,100 |

So an aggregate referring-domain count **is** available: 188 as of 31 Aug. Note it
reads *lower* than the 224 recorded on 2026-08-18 while the 30-day change is
*positive* (+39) — consistent with Ahrefs pruning its index rather than Quiver
losing links, but this run did not verify that and the two figures come from
different capture sessions. The capture's own notes state it is a dashboard
snapshot, not a backlink index or API export, and that Brand Radar was still
setting up so **no AI-citation metrics were available this week**.

What the still-missing CSV adds is the **per-domain and per-target breakdown**
needed to act on the 188. That remains unavailable.

### Referrers, 7 days (2026-08-25 → 2026-09-01)

| Referrer | Visits | Prev | Note |
| --- | ---: | ---: | --- |
| google.com | 500 | 419 | search |
| duckduckgo.com | 66 | 70 | search |
| search.yahoo.com | 25 | 18 | search |
| bing.com | 21 | 51 | search |
| **chatgpt.com** | **5** | 4 | AI assistant referral |
| search.brave.com / yandex.com | 3 / 3 | 2 / 0 | search |
| google.com.au / ecosia.org | 2 / 2 | 0 / 2 | search |
| accounts.google.com | 2 | 4 | auth bounce, not a referrer |
| **reddit.com** | **2** | 6 | genuine referring domain |
| **com.reddit.frontpage** | **1** | 0 | Reddit app |
| **facebook.com** | **1** | 0 | genuine referring domain |
| **l.instagram.com** | **1** | 1 | genuine referring domain |
| **tiktok.com** | **1** | 0 | genuine referring domain |
| **gemini.google.com** | **1** | 0 | AI assistant referral |
| statics.gov.teams.microsoft.us | 1 | 0 | Teams link unfurl |

Non-search referring sources total **12 visits**: Reddit 3 (web + app), ChatGPT 5,
Instagram 1, Facebook 1, TikTok 1, Gemini 1. Flat against last week's 12 in a
different mix. Gone since 25 Aug: `copilot.microsoft.com` (1), `startpage.com` (1),
`yandex.ru` (2), `com.google.android.gm` (1).

### Traffic context

| | 2026-08-18 → 08-25 | 2026-08-25 → 09-01 |
| --- | ---: | ---: |
| Raw page views | 2,233 | 2,401 |
| Adjusted (bot-filtered) | 2,074 | 2,237 |
| **Unique visitors** | **839** | **1,103** (+31%) |
| Bounce rate | 70% | 76% |
| US visits | 2,101 | 2,204 |
| Singapore visits | 82 | 89 |

The 18 Aug Singapore burst has stayed gone for a second week (89 visits). Unique
visitors up 31% on flat page views means shallower sessions, which the 6-point
bounce-rate rise agrees with; desktop share rose from 59% to 67%. Not diagnosed
this run.

## Confirmed Targets

All five re-fetched live on 2026-09-01. Every one still returns 200, still
references MagicSeaweed at an unchanged count, and still contains **no** reference
to Quiver. Unchanged for a fourth consecutive week. Contacts carried forward from
the 2026-08-18 report.

| Target | Source URL | Status | HTTP | Contact | Next action |
| --- | --- | --- | ---: | --- | --- |
| Odyssey Surf School | https://www.odysseysurfschool.com/blog/surfing/msw-the-way-to-find-out-how-the-surf-condition-is/ | confirmed | 200 | info@odysseysurfschool.com | Still live, 14 MagicSeaweed references, unchanged. Densest MSW page in the set; pitch the updated surf-report reading guide as the replacement resource. Gmail draft exists, still unsent. |
| Surf Sistas surf-report guide | https://www.surfsistas.com/surf-better/how-to-read-a-surf-report-a-beginners-guide/ | confirmed | 200 | none in raw HTML | Still live, 10 MSW references, unchanged. Find a contact route manually; education angle only, do not claim UK spot coverage. |
| Whatever Charters | https://whatevercharters.com/magicseaweed | confirmed | 200 | freddy@foxwatersports.com | Still live, 6 MSW references, unchanged. Dead MSW landing page, cleanest replacement pitch. Gmail draft exists, still unsent. |
| UMass Lowell Campus Recreation | https://www.uml.edu/campusrecreation/programs/outdoor-adventure/surfing.aspx | confirmed | 200 | OutdoorAdventure@uml.edu | Still live, 7 MSW references, unchanged. Gmail draft exists, still unsent. |
| Surf Sistas winter surfing guide | https://www.surfsistas.com/cornwall/survive-winter-surfing/ | confirmed | 200 | none in raw HTML | Still live, 4 MSW references, unchanged. Pair with the Surf Sistas guide pitch above. |

## Unverified Targets

| Target | Source URL | Status | Next action |
| --- | --- | --- | --- |
| Surfrider Foundation Central Texas | https://www.surfrider.org/chapters/central-texas | unverified | Still HTTP 404 on re-check 2026-09-01, fourth consecutive week. No live page with the claimed MagicSeaweed reference has ever been found. Keep out of outreach. |
| swimy.org | https://www.swimy.org | unverified | Homepage returns 200 but carries no Quiver reference, and the domain is **absent from the referrer list for a second consecutive week** (0 visits). The 25 Aug report set the condition "drop from tracking if absent again next week" — that condition is now met. Recommend dropping; not a backlink. |

The carry-forward parser returned 5 confirmed + 2 unverified, so the chain is
intact. This report preserves the literal `## Confirmed Targets` /
`## Unverified Targets` headings and the `Target` / `Source URL` / `Next action`
columns the parser requires. Renaming them silently drops every target.

## Outreach tracker state

59 targets tracked: **20 sent** (+4), 17 queued (−2), 12 drafted (+2), 6 follow-up,
3 responded, 1 declined. Responded: Surf Simply, Stab Magazine, Hawaii Public
Radio. Declined: Honolulu Civil Beat.

Movement this week, the first since 18 Aug:

- Four new targets entered as `sent`: Moment Surf Co, OBX Surf School, Oregon Surf
  Adventures, Rincon Surf School.
- Nor Cal Surf Shop and Hans Hedemann Surf School moved `queued` → `drafted`.

No target moved into `responded`. The August total recorded in workspace memory
(34 sends, 0 replies) is unchanged in outcome: **sends are still converting at
zero.**

## Blocked checks

- **No native backlink export.** `manualExports` is empty; last week's manual
  214-row Site Explorer table was not carried into this folder. No page-level link
  graph exists, so the 188 referring domains cannot be attributed to target URLs.
- **No fresh Ahrefs crawl.** The latest completed Site Audit is still 2026-08-24,
  captured 2026-08-31. It predates the internal-linking deploy, so **it cannot
  confirm or refute the orphan fix.** The next crawl is the first meaningful read.
- **No AI-citation metrics this week.** Brand Radar was still setting up its report
  at capture time, so the ChatGPT / AI Overviews / Perplexity citation counts
  reported on 2026-08-18 have no update.
- **Ahrefs crawl remains capped.** 8,901 links found, 3,668 crawled, 5,231
  uncrawled. 230 orphans and 1,442 issues are floors.
- **Whether Google has indexed the previously orphaned pages** is unknown — needs
  Search Console coverage data, unavailable to this routine.
- **Whether the `/cams` and `/surf-cams` families are cannibalising each other in
  ranking** cannot be measured here; it needs GSC query-level data. The duplication
  and the orphaning are verified; the ranking impact is inferred.
- **The bounce-rate rise (70% → 76%) was not diagnosed.**

## Action list

- `[NEEDS DECISION]` **Consolidate `/cams/{region}` and `/surf-cams/{region}`.**
  Two indexable, self-canonical families cover the same regions; they overlap on
  Florida and Hawaii. The orphaned family earns the traffic
  (`/surf-cams/san-diego`, 58 visits, #3 sitewide) while `/cams/san-diego` 404s.
  Pick one canonical family and 308 the other, the way `/water-temp` and
  `/surf-cams` were just fixed. Do not guess: the choice determines which URLs keep
  their existing rankings.
- `[APPLY FIX]` **Link the four `/surf-cams/*` pages from the `/cams` hub**, or
  from wherever the winning family lives once the decision above is made. Zero hub
  links today, and `/surf-cams` now redirects to a page that does not link to them.
  This is the same orphan pattern that cost 70 health-score points, on the site's
  third-highest-traffic page.
- `[APPLY FIX]` **City-level `/water-temp/*` pages are still near-orphaned.** All 60
  are in the sitemap; the six hub pages checked link to exactly one of them
  (`/water-temp/san-diego`) and `/map` links to none. The beach-level fix landed;
  the city level did not.
- `[DROP EXPORT]` Ahrefs Webmaster Tools **backlink** export (free tier) to
  `../Brand-Vault/seo-audit/2026-09-01/AHREFS-WEBMASTER-TOOLS.csv`. Fourth week
  running, and still the real missing native input — it is what turns 188 referring
  domains into an actionable per-domain list.
- `[DROP EXPORT]` Google-reported inbound links to `GSC-LINKS.csv` in the same
  folder, for a second independent link view. Also the only route to answering
  whether the orphaned pages are indexed and whether the two cam families are
  cannibalising.
- `[DROP EXPORT]` Re-drop the manual `REFERRING-DOMAINS.csv` into this week's
  folder if the 214-domain inventory should persist. Manual imports are per-folder
  and do not carry forward on their own.
- `[SEND EMAIL]` **Odyssey Surf School** — `info@odysseysurfschool.com`. Draft has
  sat unsent for two weeks; target re-verified live today, 14 MSW references
  unchanged. Densest target in the set.
- `[SEND EMAIL]` **Whatever Charters** — `freddy@foxwatersports.com`. Draft unsent
  for two weeks; dead MSW landing page re-verified 200 today. Cleanest replacement
  pitch.
- `[SEND EMAIL]` **UMass Lowell Outdoor Adventure** — `OutdoorAdventure@uml.edu`.
  Draft unsent for two weeks; NH/RI destination coverage already verified.
- `[NEEDS DECISION]` **Surf Sistas, 2 pages, no machine-readable contact.** Carried
  unchanged for three weeks. Worth five manual minutes given it is the second-
  densest target, or drop both pages and stop re-checking them weekly.
- `[NEEDS DECISION]` **Drop `swimy.org` from tracking.** The condition the 25 Aug
  report set — absent from referrers a second week — is now met.
- `[NO ACTION]` **Ahrefs health score 24 is stale, not current.** It measures the
  24 Aug crawl, which predates the orphan fix deployed since. Do not treat it as
  this week's state and do not revert `eaeda9671`.
- `[NO ACTION]` **Last week's two `[READY TO SHIP]` items are shipped and verified
  live.** `/water-temp` and `/surf-cams` redirect 308; beach → water-temp links
  present on 7 of 7 sampled pages.
- `[NO ACTION]` **Technical crawl — 0 findings** in the 11-page, 40-link sample.
  Nothing broken inside the sampled set. Not a sitewide guarantee: the sitemap
  carries 1,615 URLs.
- `[NO ACTION]` **`embedReferrers` empty** — genuine zero for the window, source
  connected and reading. Fourth week at zero.
- `[NO ACTION]` **Surfrider Central Texas** — still 404, still unverified, still
  excluded from outreach.
- `[NO ACTION]` **Beach-level `/tide` pages absent from the sitemap** — recorded as
  an observation; intent not established this run.

## Guardrails

Nothing was committed or pushed. Nothing in production was mutated. No code was
changed. All external checks were read-only GETs against production and seven
third-party URLs. No numbers from a previous run are reported as new: every Ahrefs
figure is dated at the point it appears (Site Explorer captured 2026-08-31, Site
Audit crawl 2026-08-24), and the live production checks are labelled as fetched
2026-09-01.

Any future report in this folder must keep the literal `## Confirmed Targets` and
`## Unverified Targets` headings with `Target` / `Source URL` / `Next action`
columns, or the carry-forward chain breaks silently.
