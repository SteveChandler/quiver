---
phase: 17-limited-session-intelligence-pilot
plan: 17-04
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - components/home-screen/index.tsx
  - components/home-screen/session-intelligence-module.tsx
  - __tests__/components/home-screen/session-intelligence-module.test.tsx
  - e2e/home.spec.ts
autonomous: true
requirements:
  - SI-04
  - SI-07
---

# 17-04: Add Homepage Compact Module

<objective>
Add a compact "Find your next best surf window" module to the authenticated
homepage using existing discovery recommendations and the existing location
fallback chain.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add the compact homepage module</name>
  <files>components/home-screen/session-intelligence-module.tsx</files>
  <read_first>
    - components/home-screen/ARCHITECTURE.md
    - components/home-screen/index.tsx
    - components/session-intelligence/best-surf-windows.tsx
    - lib/recommendations/session-intelligence-surface-adapters.ts
  </read_first>
  <action>
    Create a compact `SessionIntelligenceModule` that accepts discovery
    recommendations, maps up to three through the Phase 17 adapter, and renders
    copy headed `Find your next best surf window`. Include CTA copy for
    `Browse best surf windows` and preserve `Open in app` through the shared
    app-link CTA.
  </action>
  <verify>
    <automated>rg -n "SessionIntelligenceModule|Find your next best surf window|Browse best surf windows|BestSurfWindows" components/home-screen/session-intelligence-module.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The module accepts existing discovery recommendations as props.
    - The module does not fetch.
    - Populated state renders at most three windows.
    - The expected CTA labels are present.
  </acceptance_criteria>
  <done>The homepage has a compact Session Intelligence module.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the module into HomeScreen</name>
  <files>components/home-screen/index.tsx</files>
  <read_first>
    - components/home-screen/index.tsx
    - components/home-screen/hero-recommendation.tsx
    - components/home-screen/top-spots-carousel.tsx
  </read_first>
  <action>
    Render the compact module using `discovery.recommendations`. Place it where
    it complements the existing hero and top-spots feed without replacing them,
    preferably near the header/top-spots transition. Do not add another
    `useSurfDiscovery()` call.
  </action>
  <verify>
    <automated>rg -n "useSurfDiscovery|SessionIntelligenceModule|HeroRecommendation|TopSpotsCarousel" components/home-screen/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - There is still one `useSurfDiscovery()` invocation in `HomeScreen`.
    - Existing hero recommendation and top-spots carousel remain rendered.
    - The module receives data from existing discovery state.
  </acceptance_criteria>
  <done>The compact module is wired without duplicate discovery work.</done>
</task>

<task type="auto">
  <name>Task 3: Guarantee no-location and empty states</name>
  <files>components/home-screen/session-intelligence-module.tsx, components/home-screen/index.tsx</files>
  <read_first>
    - hooks/use-surf-discovery.ts
    - components/home-screen/index.tsx
    - components/home-screen/forecast-outlook-card.tsx
  </read_first>
  <action>
    Rely on the existing seed-location fallback chain
    browser GPS -> home beach -> IP location -> San Diego. If discovery is
    empty, render a small fallback CTA to `/forecast` instead of a broken or
    blank module.
  </action>
  <verify>
    <automated>rg -n "DEFAULT_LOCATION|seedDiscoveryLocation|/forecast|empty|fallback" components/home-screen/index.tsx components/home-screen/session-intelligence-module.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Browser location denial does not make the module unusable.
    - Empty discovery state has a visible fallback.
    - No anonymous landing page behavior changes.
  </acceptance_criteria>
  <done>The homepage pilot works without explicit location.</done>
</task>

<task type="auto">
  <name>Task 4: Add homepage tests</name>
  <files>__tests__/components/home-screen/session-intelligence-module.test.tsx, e2e/home.spec.ts</files>
  <read_first>
    - __tests__/components/home-screen/hero-recommendation.test.tsx
    - __tests__/components/home-screen/top-spots-carousel.test.tsx
    - e2e/home.spec.ts
  </read_first>
  <action>
    Add component tests for populated recommendations, empty fallback, and CTA
    labels. Extend auth E2E coverage so the module can render on `/` without
    removing existing homepage sections.
  </action>
  <verify>
    <automated>yarn test:unit __tests__/components/home-screen/session-intelligence-module.test.tsx --runInBand</automated>
    <automated>npx playwright test e2e/home.spec.ts --project=auth</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if CTA labels disappear.
    - Tests fail if the module renders duplicate discovery fetch state instead of props.
    - E2E confirms existing homepage sections are still available.
  </acceptance_criteria>
  <done>Homepage pilot behavior is covered.</done>
</task>

</tasks>

<verification>

```bash
yarn test:unit __tests__/components/home-screen/session-intelligence-module.test.tsx --runInBand
npx eslint --max-warnings=0 components/home-screen/index.tsx components/home-screen/session-intelligence-module.tsx __tests__/components/home-screen/session-intelligence-module.test.tsx e2e/home.spec.ts
npx playwright test e2e/home.spec.ts --project=auth
```

</verification>

<success_criteria>

- Authenticated homepage includes a compact Session Intelligence module.
- It works when browser location is unavailable.
- It does not add another discovery request.
- Existing home hero, top spots, 7-day outlook card, coast pulse, and bottom nav
  remain available.
</success_criteria>
