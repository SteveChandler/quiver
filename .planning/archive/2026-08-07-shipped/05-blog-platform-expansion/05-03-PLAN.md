# Plan 05-03: Blog Hub Presentation

## Goal

Update the blog hub only where needed for the larger finite post list.

## Scope

- Switch `/blog` from raw `blogPosts` access to helper-backed `allPosts` and `featuredPost`.
- Keep the existing latest/all-post visual presentation.
- Avoid unrelated layout redesign before launch content exists.

## Verification

- Add app-level tests that the hub renders the featured post and all-post list from the helper-backed data.
