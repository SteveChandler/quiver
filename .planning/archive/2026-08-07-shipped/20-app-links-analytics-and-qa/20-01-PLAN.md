---
phase: 20-app-links-analytics-and-qa
plan: 20-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/.well-known/apple-app-site-association/route.ts
  - app/.well-known/assetlinks.json/route.ts
  - app/app/spot/[slug]/page.tsx
  - lib/recommendations/surf-window-links.ts
  - __tests__/app/well-known-app-links.test.ts
  - __tests__/lib/recommendations/surf-window-links.test.ts
  - docs/session-intelligence/phase-20-app-link-verification.md
autonomous: true
requirements:
  - SI-07
must_haves:
  truths:
    - "/app/spot/:slug?window=:id is represented in web link generation and app-link manifests."
    - "Canonical web URLs remain separate from app/universal links."
    - "App Store fallback behavior is documented and locally testable."
    - "Placeholder team IDs or certificate fingerprints are not accepted as live evidence."
  artifacts:
    - "App-link route/link tests."
    - "Phase 20 app-link verification doc."
---

# 20-01: Align App-Link Route, Manifests, And Fallback

<objective>
Make Session Intelligence window links route through the Phase 20 app handoff
path while preserving canonical web URLs and App Store fallback behavior.
</objective>

<context>
@AGENTS.md
@docs/ARCHITECTURE.md
@app/ARCHITECTURE.md
@.planning/phases/20-app-links-analytics-and-qa/20-RESEARCH.md
@.planning/phases/20-app-links-analytics-and-qa/20-PATTERNS.md
@app/.well-known/apple-app-site-association/route.ts
@app/.well-known/assetlinks.json/route.ts
@lib/recommendations/surf-window-links.ts
@lib/constants/app-store.ts
@__tests__/app/well-known-app-links.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app handoff path coverage to manifests</name>
  <files>app/.well-known/apple-app-site-association/route.ts, app/.well-known/assetlinks.json/route.ts, __tests__/app/well-known-app-links.test.ts</files>
  <read_first>
    - app/.well-known/apple-app-site-association/route.ts
    - app/.well-known/assetlinks.json/route.ts
    - __tests__/app/well-known-app-links.test.ts
  </read_first>
  <action>
    Add `/app/*` or `/app/spot/*` to required AASA paths. Preserve existing
    required paths. Add tests proving the app handoff path is present and that
    obvious placeholder certificate fingerprints are not accepted as live
    validation evidence. Keep Android assetlinks package/fingerprint behavior
    env-driven.
  </action>
  <verify>
    <automated>yarn test:unit __tests__/app/well-known-app-links.test.ts --runInBand</automated>
    <automated>rg -n "/app/\\*|/app/spot|placeholder|ANDROID_SHA256_FINGERPRINTS|APPLE_TEAM_ID" app/.well-known __tests__/app/well-known-app-links.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AASA includes the app handoff path and all existing required paths.
    - Tests cover configured Android fingerprints and placeholder rejection/documentation.
    - No secret or certificate value is hard-coded into source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Move surf-window app links to /app/spot</name>
  <files>lib/recommendations/surf-window-links.ts, __tests__/lib/recommendations/surf-window-links.test.ts</files>
  <read_first>
    - lib/recommendations/surf-window-links.ts
    - types/session-intelligence.ts
    - __tests__/lib/recommendations
  </read_first>
  <action>
    Update surf-window link generation so `appDeepLink` and `universalLink`
    use `/app/spot/{slug}?window={windowId}` when a slug is available. Keep
    `canonicalWebUrl` on the canonical beach URL and keep `window=` out of
    canonical URLs.
  </action>
  <verify>
    <automated>yarn test:unit __tests__/lib/recommendations/surf-window-links.test.ts --runInBand</automated>
    <automated>rg -n "/app/spot|window=|canonicalWebUrl" lib/recommendations/surf-window-links.ts __tests__/lib/recommendations/surf-window-links.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - App/universal links include `/app/spot/` and `window=`.
    - Canonical URLs do not include `window=`.
    - Existing no-slug fallback remains safe.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add web fallback for app handoff</name>
  <files>app/app/spot/[slug]/page.tsx, docs/session-intelligence/phase-20-app-link-verification.md</files>
  <read_first>
    - app/app/page.tsx
    - app/ARCHITECTURE.md
    - lib/constants/app-store.ts
  </read_first>
  <action>
    Add a lightweight noindex web fallback for `/app/spot/[slug]` that exposes
    the App Store CTA and a canonical web fallback link. Preserve the `window`
    query for analytics/fallback context without making it a canonical URL.
    Document the route, manifest paths, local checks, and native verification
    caveat in `phase-20-app-link-verification.md`.
  </action>
  <verify>
    <automated>rg -n "noindex|IOS_APP_STORE_URL|window|canonical|/app/spot" app/app/spot docs/session-intelligence/phase-20-app-link-verification.md</automated>
  </verify>
  <acceptance_criteria>
    - Browser fallback is useful when the native app is not installed.
    - The fallback page does not create SEO canonical churn.
    - The doc states native app handling remains a release verification gate.
  </acceptance_criteria>
</task>

</tasks>

<verification>

```bash
yarn test:unit __tests__/app/well-known-app-links.test.ts __tests__/lib/recommendations/surf-window-links.test.ts --runInBand
npx eslint --max-warnings=0 app/.well-known/apple-app-site-association/route.ts app/.well-known/assetlinks.json/route.ts app/app/spot lib/recommendations/surf-window-links.ts __tests__/app/well-known-app-links.test.ts __tests__/lib/recommendations/surf-window-links.test.ts
yarn typecheck
```

</verification>

<success_criteria>

- `/app/spot/:slug?window=:id` is covered locally.
- App Store fallback is present.
- Canonical web URLs are not polluted by app-link parameters.

</success_criteria>

<output>
Create `.planning/phases/20-app-links-analytics-and-qa/20-01-SUMMARY.md`.
</output>
