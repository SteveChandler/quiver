# Plan 04-03: Verified CTA Path

## Goal

Wire the pricing CTA to the verified Phase 2 public path: waitlist/interest through account signup, not checkout.

## Scope

- Add an anonymous visitor CTA that opens the existing `UnifiedAuthModal`.
- Attribute CTA views/clicks with pricing source and surface metadata.
- Suppress pre-auth CTA tracking for authenticated users.
- Show signed-in users an opt-in status message instead of another signup prompt.

## Verification

- Unit tests cover anonymous view tracking, click tracking, modal opening, return path, and signed-in suppression.
