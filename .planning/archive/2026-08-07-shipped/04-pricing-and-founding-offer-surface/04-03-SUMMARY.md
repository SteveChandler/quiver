# Summary 04-03: Verified CTA Path

## Completed

- Added `FoundingAccessCta`.
- Anonymous visitors can join the founding access waitlist through the signup modal.
- CTA events use `source`, `surface`, `cta_type`, and `cta_copy_variant` metadata.
- Signed-in users do not trigger pre-auth view/click events and see beta-protection copy.

## Result

The pricing CTA matches the verified waitlist-only mechanics from Phase 2.
