# Plan 09-04: Allowlist And Guard Validation

## Goal

Validate that launch instrumentation stays inside existing event allowlists and
does not break pre-auth/authenticated guard behavior.

## Scope

- Verify no new event types were added.
- Verify anonymous-safe events are used for public launch surfaces.
- Verify pricing waitlist pre-auth events remain guarded by the existing CTA
  component.
- Verify iOS CTA reporting still uses generic internal CTA events.

## Verification

- Scoped ESLint passes for launch analytics files and tests.
- Unit tests cover page metadata, blog click metadata, pricing CTA guards, and
  iOS CTA metadata.
