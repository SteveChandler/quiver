---
phase: 20.1-durable-beach-follow-and-surf-call-retention-loop
plan: 20.1-02
type: execute
wave: 2
depends_on:
  - 20.1-01
files_modified:
  - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/event-taxonomy.ts
  - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/web-context.ts
  - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/ios-app-cta-tracking.ts
  - /Users/stevenchandler/Desktop/dev/quiver/types/beach-follow.ts
  - /Users/stevenchandler/Desktop/dev/quiver/lib/beach-follow/*
  - /Users/stevenchandler/Desktop/dev/quiver/supabase/migrations/*beach_follow*.sql
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/lib/analytics.ts
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/types/*
autonomous: true
requirements:
  - BFR-01
  - BFR-03
  - BFR-04
  - BFR-06
  - BFR-09
  - BFR-11
  - BFR-12
must_haves:
  truths:
    - "Intent, beach ownership, exact handoff, watched-call, outcome, and analytics contracts are typed and reuse the ownership decisions from Plan 20.1-01."
    - "Unknown coastal visitors remain unknown; a single water-temperature view cannot qualify surfing."
    - "Anonymous follow works without a server round trip, and account merge is versioned, idempotent, and lossless."
    - "New analytics metadata is safe, allowlisted, anonymous-compatible where intended, and non-blocking."
    - "Any database migration is additive, reviewed, and unapplied until explicit production approval."
  artifacts:
    - path: "/Users/stevenchandler/Desktop/dev/quiver/types/beach-follow.ts"
      provides: "Shared web/API beach-follow, topic, and intent evidence types or the audited equivalent path."
    - path: "/Users/stevenchandler/Desktop/dev/quiver/lib/beach-follow/"
      provides: "Pure normalization, merge, qualification, and handoff helpers."
    - path: "/Users/stevenchandler/Desktop/dev/quiver/lib/analytics/event-taxonomy.ts"
      provides: "Allowed BFR web/cross-surface events and safe property enums."
    - path: "/Users/stevenchandler/Desktop/dev/quiver-native/src/lib/analytics.ts"
      provides: "Allowed BFR native events or extension of the audited native taxonomy."
  key_links:
    - from: "anonymous local follow"
      to: "signed-in ownership"
      via: "versioned idempotent merge helper"
      pattern: "merge.*follow|hydrate.*follow"
    - from: "web handoff token/context"
      to: "native first-open/context resolution"
      via: "one typed expiring contract"
      pattern: "handoff.*token|first_open.*join"
---

# 20.1-02: Define Beach Follow, Intent, Handoff, Watch, Outcome, And Measurement Contracts

<objective>
Turn the Phase 20.1 reuse audit into one typed cross-repository contract before product surfaces are implemented.

Purpose: Make later web and native work independently executable while keeping identity, merge, handoff, watched-call, outcome, experiment, and analytics semantics consistent.

Output: Pure helpers, types, safe event taxonomy, tests, and an approval-gated additive migration only if Plan 20.1-01 proves existing storage cannot satisfy the contract.
</objective>

<execution_context>
@/Users/stevenchandler/.codex/get-shit-done/workflows/execute-plan.md
@/Users/stevenchandler/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-REUSE-AUDIT.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-PATTERNS.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure beach-follow and anonymous-to-account merge contracts</name>
  <files>/Users/stevenchandler/Desktop/dev/quiver/types/beach-follow.ts, /Users/stevenchandler/Desktop/dev/quiver/lib/beach-follow/*, focused tests</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-REUSE-AUDIT.md
    - /Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-PATTERNS.md
    - all audited favorite/follow/profile storage and hydration files
  </read_first>
  <behavior>
    - "A canonical beach can be followed with one or more bounded topics without requiring surf intent."
    - "Anonymous state is versioned, deduped, bounded, resilient to corruption, and does not block the utility page."
    - "Account merge unions anonymous additions with server state, preserves explicit removals according to the audited policy, and is idempotent."
    - "Repeated merge or retry cannot duplicate beaches or topics."
  </behavior>
  <action>
    Write failing tests first. Extend the audited ownership model rather than creating a parallel favorite system. Define the smallest typed beach-follow/topic state, local persistence envelope, normalizer, merge input/output, and deterministic keys. If account persistence needs a new table, author an additive migration with RLS, uniqueness, timestamps, topic validation, and rollback notes, but do not apply it. Keep anonymous first save local-only. Do not store raw search text, arbitrary URLs, free-form intent, or notification content.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/lib/beach-follow/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover create, topic update, remove, duplicate input, version migration, corrupt storage, retry, signed-in merge, and explicit-removal policy.
    - No first anonymous follow requires authentication or a network write.
    - Existing favorites/home beaches remain readable and are not silently overwritten.
    - Any migration is committed but explicitly unapplied.
  </acceptance_criteria>
  <done>One reusable ownership and merge contract exists for web implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement conservative intent qualification</name>
  <files>/Users/stevenchandler/Desktop/dev/quiver/lib/beach-follow/intent.ts, /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/web-context.ts, focused tests</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/web-context.ts
    - /Users/stevenchandler/Desktop/dev/quiver/types/implicit-preferences.ts
    - current page-type and CTA analytics tests
    - 20.1-CONTEXT.md D-11 through D-14
  </read_first>
  <behavior>
    - "Explicit intent wins over inferred intent."
    - "No evidence produces unknown, not surfing."
    - "One city or beach water-temperature page view does not produce surf qualification."
    - "A bounded set of high-intent surf actions or multiple surf-specific signals can produce inferred surfing."
    - "Non-surf explicit intent suppresses surf-specific default CTA while preserving optional surf discovery."
  </behavior>
  <action>
    Write characterization and failing tests first. Add a pure classifier over safe enumerated signals and explicit choices. Keep thresholds in named constants and output evidence source plus a bounded reason enum suitable for analytics. Do not use raw page URLs, search terms, text embeddings, or a probabilistic/ML model. Add the explicit intent choice vocabulary only after checking existing profile/implicit preference semantics so unrelated native surf preferences are not polluted by general web intent.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/lib/beach-follow/intent.test.ts __tests__/lib/analytics/web-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Utility-only, single surf-page, multi-surf-signal, explicit surf, and explicit non-surf cases are pinned.
    - The output is explicit/inferred/unknown with safe reason enums.
    - No inferred state is written as an immutable identity label.
  </acceptance_criteria>
  <done>Web can qualify surf intent conservatively without misclassifying utility traffic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define exact handoff, watched-call, and lightweight outcome identities</name>
  <files>audited web handoff helpers/types, audited native link/types, focused tests</files>
  <read_first>
    - existing `/app/spot/:slug?window=:id` generation and fallback code
    - native exact-window routing and initial-link handling
    - native recommendation/session attribution types
    - alert seed/rule types and notification payloads
  </read_first>
  <behavior>
    - "One expiring handoff context identifies the beach, window, source, and recommendation context without trusting stale client data as live truth."
    - "A watched call references an existing recommendation/window and alert identity rather than creating a second forecast record."
    - "A lightweight 1-5 outcome can exist without a full session and can later link to one."
  </behavior>
  <action>
    Extend the audited exact-link and recommendation attribution types. Define safe versioned handoff context, expiry/fallback behavior, watched-call identity, update reason enums, and outcome payload. Keep high-cardinality tokens out of ordinary public event properties. Ensure native can resolve current server truth while retaining the prior web/watched context for explanation. Do not implement UI or outbound delivery in this task.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/lib/**/*handoff*.test.ts __tests__/lib/**/*beach-follow*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Valid, expired, malformed, removed-window, and beach-only fallback cases are tested.
    - Watched-call identity reuses existing alert/recommendation primitives.
    - Outcome validation accepts 1-5 and rejects unsupported values without requiring full session fields.
  </acceptance_criteria>
  <done>Plans 20.1-04 through 20.1-06 share one exact context and outcome vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add safe analytics and experiment contracts across web and native</name>
  <files>/Users/stevenchandler/Desktop/dev/quiver/lib/analytics/event-taxonomy.ts, /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/ios-app-cta-tracking.ts, /Users/stevenchandler/Desktop/dev/quiver-native/src/lib/analytics.ts, event allowlist tests/migration if required</files>
  <read_first>
    - current web and native analytics taxonomy/allowlist files
    - current anonymous-event and `user_events` constraints
    - existing first-open attribution events
    - 20.1-VALIDATION.md analytics section
  </read_first>
  <action>
    Add the minimum BFR event family from research, safe property enums, stable experiment assignment contract, and test-account/emulator filterability. Reuse current generic interaction events when their semantics are strong; create new events only where a funnel step or identity boundary would otherwise be ambiguous. If the Supabase event constraint needs an additive migration, author and test it without applying production changes. Make analytics non-blocking for follow, link, watch, and outcome actions.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/api/events-taxonomy-characterization.test.ts __tests__/lib/analytics/*.test.ts &amp;&amp; cd /Users/stevenchandler/Desktop/dev/quiver-native &amp;&amp; npm test -- --runInBand --runTestsByPath src/lib/__tests__/analytics*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Events distinguish general utility, surf-qualified web, existing web user, joined native, watched-call, and outcome cohorts.
    - Properties contain no PII/raw search/coordinates/notes/token values.
    - Anonymous-allowed and pre-auth behavior is explicit.
    - Failed analytics cannot fail the product action.
  </acceptance_criteria>
  <done>Every later plan can be measured with one safe cross-surface taxonomy.</done>
</task>

</tasks>

<verification>
- Focused web/native contract tests pass.
- Typecheck and scoped lint pass for changed contract files.
- Any migration is additive, locally tested, documented, and unapplied.
- No web UI, native UI, production config, deploy, or outbound delivery is changed.
</verification>

<success_criteria>
- Beach ownership and merge semantics are implementation-ready.
- Intent qualification is conservative and test-pinned.
- Handoff, watch, change reasons, and outcome identities are shared.
- Analytics and experiment contracts can support causal measurement.
</success_criteria>

<output>
After completion, create `20.1-02-SUMMARY.md` with final type/schema/event decisions, migration status, tests, open risks, and the exact inputs required by Plans 20.1-03 through 20.1-06.
</output>
