# Plan 20.1-01 Summary — Inflight Truth, Cleanup Audit, and Cleanup-Train Execution

**Executed:** 2026-08-24
**Session shape:** controller Claude Fable 5 (PM/review/commits) + exactly three Codex `gpt-5.6-sol`
(medium) workers — (1) branch freeze + TD verification, (2) TD-01/TD-02, (3) TD-03/TD-05. Workers
ran no mutating git; the controller reviewed every diff and independently reran every gate.
**Evidence bundle:** `dev/handoffs/phase-20-1-td/` (audit report, raw scan log, two slice reports,
session ledger).

## Audit branch identity (frozen)

- `claude/tech-debt-audit-codex-973g1t` exists ONLY as `origin/claude/tech-debt-audit-codex-973g1t`
  on `quiver-native` (absent in `quiver` and `seaside`). The planning docs' "not visible as a
  pushed ref" note is obsolete — the branch has since been pushed.
- Head `fc2fe1c3` ("docs: point remaining TODO and handoff entries at their tracking issues",
  2026-08-24T19:01Z); merge base with `origin/main` = `01b43900` (#156 merge); 2 ahead / 4 behind.
- **Worktree state: none.** All registered worktrees of all three repos plus
  `~/codex-worktrees/*` and `.quiver/worktrees/*` were swept — no checkout uses the branch, so
  there is no uncommitted work to capture. Nothing was deleted; the remote ref is untouched.
- Unique commits are documentation retirement only (`TODO.md`, `docs/handoffs/README.md`, deleted
  `docs/handoffs/map-spot-payload-metrics-20260806.md`; reference issues #180/#181/#182/#35). No
  app config, dependency, release, or native-fingerprint change vs merge base.
- Provenance: the branch's unique commits contain **no TD inventory**; candidate identity came from
  branch-tree history and was proven by direct reference scans against current `origin/main`.
- Disposition: **reference only + cleanup slices executed from current main.** Never merged.

## Release / source truth

- `quiver`: #605 MERGED 2026-08-24T20:27Z; `origin/main d528663af`...`origin/prod` = 2/90.
  #606 (this phase's planning) merged; live planning branch `planning/phase-20-1-cleanup-gates`
  (3 ahead of main) is the working copy and was NOT merged.
- `quiver-native`: `origin/main` = `1323c2cb` (local `main` checkout was stale/behind — recorded).
  `origin/main...release/ota-build16-main-parity-20260822` = 57/36. Week Scout stability reads
  `EXPO_PUBLIC_WEEK_SCOUT_STABILITY_ENABLED` (`src/lib/week-scout/stability-flag.ts`), env/build-
  profile driven. Installed-device build/runtime/channel/update-id/flag truth remains OPEN (N-01)
  and blocks release lanes only. #131 remains open/draft/binary-gated (N-02: hold).
- Phase 21: trusted-forecast engine/builder/tests/migrations are ON `origin/main` and promoted
  dark to prod (PR #522); the `p21-03/p21-04` worktrees no longer exist. The 21-03/21-04 summaries'
  "uncommitted worktree" status is stale — nothing left to preserve (W-03 resolved).
- La Jolla Shores migration (`20260823160000`) is on `origin/main`; production application +
  credentialed audit + device checks remain #156 closeout gaps (N-04, approval-gated).

## Cleanup-candidate dispositions (X-04) and files reserved

| Slice | Disposition | Commit (branch) | Files reserved/owned |
|---|---|---|---|
| TD-01 | **LANDED** | `f7a536b7` (`cleanup/td-01-nearest-beach`) | `src/lib/geo/nearest-beach.ts` (deleted) |
| TD-02 | **LANDED** | `e9ea878c` (`cleanup/td-02-startup-prefetch`) | `src/lib/prefetch-critical.ts` (deleted); `use-home-location.test.ts`, `app-boat-onboarding-hierarchy.test.tsx` (edited) |
| TD-03 | **LANDED**, e2e deferred | `e753837b` (`cleanup/td-03-payoff-card`) | `payoff-value-card.tsx` + test (deleted); 3 boat/Maestro-contract tests (edited) |
| TD-04 | **SUPERSEDED/NO-OP** | — (main removed it in `f9e15d47`, 2026-08-23) | none — live stack is `beach-hero`/`forecast-timeline`/`looking-ahead-card`/`synced-forecast-chart-stack`/`swell-field-hero` |
| TD-05 | **LANDED** | `ec566292` (`cleanup/td-05-pmns-policy`) | 4 PMNS policy modules + ranking/explanation tests (deleted); `beach-detail-best-window.test.ts`, `recommendation-hold-semantics.test.ts` (edited) |

Combined verification (`cleanup/td-integration-20260824`, + docs commit `abf928ed`):
**18 suites / 445 tests PASS, typecheck PASS.** Per-branch gates also rerun by the controller:
td-02 6/6, td-03 109/109, td-05 150/150, all typechecks green, all after-scans zero-hit.
Rollback per slice: `git revert <commit>` on its branch.

## Current blockers / open items

1. **N-01 device truth** — physical-device build/runtime/channel/update-id/flag capture before any
   native release lane (does not block cleanup or contract drafting).
2. **TD-03 `npm run e2e:boat:gate`** — documented safe-environment deferral (no reserved
   simulator/backend lane this session; builds out of scope). Run on the next approved E2E lane.
3. **Web follow/anon-ownership deep sweep** (Plan 20.1-01 Task 1 remainder) — required before
   20.1-02 contracts / 20.1-03 implementation.
4. **Mock-by-mock mapping** against post-cleanup main — required before 20.1-05.
5. **W-02** `feat/redeem-success-page` retire-vs-cherry-pick decision owed by the 20.1-03/04 lane.
6. **N-03** unique branches (`dazzling-chaplygin` ×4, `blissful-torvalds` ×1) re-judge before
   20.1-05 Home/Looking-Ahead work.
7. Cleanup branches are LOCAL (worktree `quiver-native-phase-20-1`); PR creation/merge awaits
   operator go-ahead. Phase 20.1 native implementation starts only after the train merges.

## Exact commands and searches run

Recorded verbatim in `dev/handoffs/phase-20-1-td/codex1-scan-log.txt` (audit),
`codex1-audit-report.md` (per-slice scans + planned commands), `codex2-slices-report.md` and
`codex3-slices-report.md` (before/after scans, test outputs, typecheck), and the controller's
per-branch reruns summarized in `LEDGER.md`. Canonical shapes: `git grep -n -E '<symbols>' --
':!node_modules' ':!.expo' ':!coverage'`, `npm test -- --runInBand --runTestsByPath <paths>`,
`npm run typecheck`, `git rev-list --left-right --count A...B`.

## Behavior confirmation

No application or production behavior changed: no deploy, migration application, flag flip, OTA,
binary, store action, notification send, payment, or entitlement change. All deletions are
dead/retired code and stale test references, verified by reference scans and green focused suites.
Quick log/session logging is untouched shipped baseline (BFR-11/X-03); no new outcome/session
surface exists anywhere in this work. The planning branch and the audit branch were not merged; no
worktree was deleted.
