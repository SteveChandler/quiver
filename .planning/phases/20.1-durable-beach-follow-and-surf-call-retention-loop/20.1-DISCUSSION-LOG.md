# Phase 20.1: Durable Beach Follow And Surf-Call Retention Loop - Discussion Log

**Date:** 2026-08-24
**Status:** Product direction corrected and captured

## Conversation Sequence

### 1. Three-Month Product Review

The initial review combined web growth with native product health. Steven corrected that framing: web should not be used to make the native release appear healthier. Web users and native users need separate behavior and retention analysis.

Decision:

- Web is measured as acquisition/utility unless the visitor is signed in or otherwise joined to product identity.
- Native is measured as the surf product.

### 2. What Users Actually Do

The review found:

- Web is dominated by one-off search utility, especially beach and water-temperature information.
- Native's repeated behavior centers on Home, Explore, and Beach Detail.
- Alerts and sessions have too little adoption to claim that either currently drives the business.

Decision:

- Do not force web and native into one undifferentiated DAU/retention metric.
- Build repeat-use loops around the surfaces people already use.

### 3. Recommendation Continuity Correction

A proposed Planning -> Decision -> Execution model did not match the code. Native uses `Now`, `Best`, and `My spots`; there is no persisted planned-session commitment in this flow.

Code review found:

- Home remounts with `Now` selected.
- `Now` and `Best` answer different questions.
- Foreground/hourly refreshes rerank recommendation queries.
- Cold start can issue a location-only request and then refine after saved/excluded context settles.
- Best alternatives may come from different windows and are not necessarily same-window runner-ups.
- Week Scout stability exists behind a build-time flag.

Decision:

- Preserve the existing modes.
- Persist or explicitly expire the selected mode.
- Explain why a recommendation changed.
- Enable Week Scout stability for all production users.
- Track continuity in `quiver-native#155` and stability rollout in `quiver-native#76`.

### 4. Stickiness Strategy

The first stickiness strategy assumed many web visitors were prospective surfers. Steven corrected that assumption: most may not be surfers.

Decision:

- Web serves broad coastal intent.
- Native remains surf-specific.
- A water-temperature page view alone never qualifies surf intent.
- Conversion is measured against surf-qualified visitors, not all web visitors.

### 5. AllTrails Analogy

The discussion examined how AllTrails handles broad, episodic outdoor intent:

- public pages provide useful answers before account or payment;
- the durable trail can be saved and organized;
- exact trail context syncs to native;
- native performs an outing-specific job;
- premium value is confidence, safety, convenience, and advanced planning.

Correct Quiver translation:

- Trail -> beach.
- Save trail -> Follow this beach.
- Lists -> My Coast.
- Hit the Trail -> Open this exact call in Quiver.
- On-route condition monitoring -> remember the accepted surf call and notify only for a meaningful change.

The post-activity recording analogy is not Phase 20.1's retention bet because Quiver already has quick log and one-tap session behavior.

Decision:

- The beach is durable; the surf call is temporary.
- Web establishes beach ownership before native install or subscription.
- Native remembers what it told the surfer, stays quiet when nothing meaningful changed, and earns a return with exact prior/current context.

### 6. GSD Phase Request

Steven requested a GSD phase for the strategy.

Decision:

- Add urgent decimal Phase 20.1 to the active `quiver` Go-Live Campaign milestone.
- Place it after completed Phase 20 app-link/analytics foundations and before Phase 21.
- Treat it as cross-repository work across `quiver` and `quiver-native`.
- Make it executable in small waves with explicit measurement and approval gates.
- Include low-fidelity mocks and an audit of in-flight web/native work.

### 7. One-Tap Session Correction

Steven identified that one-tap sessions are already implemented in native and did not move the needle.

PostHog confirms the existing event/funnel surface. For physical-device users excluding test accounts from 2026-06-01 through 2026-08-23:

- 9 users tapped Home quick log.
- 4 submitted a session within one day.

This does not prove quick log caused return, and broader retention/sales did not materially improve.

Decision:

- Remove the proposed watched-call 1–5 outcome, learning receipt, post-window session reminder, and session-schema work from Phase 20.1.
- Preserve quick log, one-tap rating, session prefill, reminders, offline support, and full SessionForm as existing baseline/non-regression behavior.
- Do not use session starts/submits as the Phase 20.1 north-star metric.
- Replace the outcome mock with a watched-call return mock covering no-change silence, meaningful update, exact reopen, and manual reopen.
- Define the native retention hypothesis as:

> Quiver earns a return because it remembers the accepted call, notices a decision-relevant change, and explains it clearly.

## Locked Product Language

Preferred universal web action:

> Follow this beach

Preferred qualified web-to-native action:

> Open this exact call in Quiver

Preferred native action:

> Watch this call

Preferred update categories:

- Still on.
- Call changed.
- Better nearby.

Labels/actions to avoid without supporting evidence:

- Calling every web visitor a surfer.
- Calling cross-window alternatives runner-ups.
- Presenting a mode reset or location change as a forecast shift.
- Claiming Quiver learned when no persisted personalization update occurred.
- Reintroducing `How was it?` as a new Phase 20.1 retention mechanism.

## Non-Goals Reaffirmed

- No generic beach native app.
- No Planning / Decision / Execution state machine.
- No new ML model.
- No duplicate alert composer, favorite store, link router, analytics transport, quick log, outcome prompt, or session log.
- No worldwide expansion or localization in this phase.
- No generic daily streak or engagement push strategy.

---

*Phase: 20.1-durable-beach-follow-and-surf-call-retention-loop*
*Discussion corrected: 2026-08-24*

---

# 2026-08-24 — Plan 20.1-01 execution session (audit + cleanup train)

Execution shape: controller Claude Fable 5 (PM/review, commits); exactly three Codex `gpt-5.6-sol`
(medium) workers — (1) branch freeze + TD verification, (2) TD-01/TD-02 edits, (3) TD-03/TD-05
edits. Workers never ran mutating git; every diff was reviewed and every gate rerun by the
controller before commit.

Decisions recorded this session:

- Planning source = `planning/phase-20-1-cleanup-gates` (newest cleanup-train branch, directly on
  `origin/main`). The planning branch is NOT merged.
- "Current quiver-native/main" = `origin/main` `1323c2cb`; the stale local `main` checkout is not
  release truth (it was behind).
- The tech-debt branch is now a pushed remote ref; there is no local worktree and no uncommitted
  work to preserve. Its two unique commits are docs-only; TD candidate identity was recovered from
  branch-tree history and proven by direct scans against current main. It stays reference-only.
- X-04 dispositions: TD-01 LAND, TD-02 LAND, TD-03 LAND (e2e deferral documented), TD-04
  SUPERSEDED/NO-OP (`f9e15d47` already removed it on main), TD-05 LAND.
- W-01 resolved (#605 merged); W-03 resolved as stale (Phase 21 work landed on main + prod-dark,
  worktrees removed); W-02/N-01/N-02/N-03/N-04 recorded with owners and next actions in the
  inflight-audit refresh.
- TD-05 retention rule honored: the live alert-prefill assertion moves to
  `beach-detail-best-window.test.ts` before the mixed explanation test is deleted.
- No production mutation, deploy, build, OTA, store, notification, payment, or entitlement action
  occurred. Quick log/session behavior untouched (baseline, per BFR-11).
