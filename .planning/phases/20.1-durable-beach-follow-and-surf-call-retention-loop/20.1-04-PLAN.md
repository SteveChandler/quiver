---
phase: 20.1-durable-beach-follow-and-surf-call-retention-loop
plan: 20.1-04
type: execute
wave: 3
depends_on:
  - 20.1-02
files_modified:
  - /Users/stevenchandler/Desktop/dev/quiver/components/session-intelligence/app-deep-link-cta.tsx
  - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/ios-app-cta-tracking.ts
  - /Users/stevenchandler/Desktop/dev/quiver/app/app/spot/[slug]/page.tsx
  - /Users/stevenchandler/Desktop/dev/quiver/app/api/*handoff*/*
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/navigation/*
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/lib/*link*
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/lib/*attribution*
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/screens/beach-detail.tsx
  - /Users/stevenchandler/Desktop/dev/quiver-native/src/screens/home.tsx
autonomous: true
requirements:
  - BFR-01
  - BFR-06
  - BFR-12
must_haves:
  truths:
    - "Only surf-qualified web visitors receive a primary exact surf-call handoff; unknown/non-surf visitors keep relevant beach ownership actions."
    - "The exact beach, window, source surface, and prior recommendation context survive universal link, App Store fallback, and joined first open."
    - "Native resolves current server truth while retaining prior context for explanation and attribution."
    - "Existing app-link routing, fallback, and attribution primitives are extended rather than duplicated."
    - "Expired or unavailable windows produce a truthful beach/current-call fallback instead of a broken route or stale promise."
  artifacts:
    - path: "/Users/stevenchandler/Desktop/dev/quiver/components/session-intelligence/app-deep-link-cta.tsx"
      provides: "Intent-aware exact-context handoff CTA using the existing component or audited equivalent."
    - path: "/Users/stevenchandler/Desktop/dev/quiver/app/app/spot/[slug]/page.tsx"
      provides: "Noindex installed/not-installed fallback that preserves the handoff context."
    - path: "/Users/stevenchandler/Desktop/dev/quiver-native/src/navigation/"
      provides: "Existing router extended to resolve the handoff context exactly."
  key_links:
    - from: "surf-qualified web recommendation/window"
      to: "native Beach Detail or Home exact context"
      via: "existing universal/app link plus expiring handoff identity"
      pattern: "/app/spot/.*window="
    - from: "App Store fallback"
      to: "native first open"
      via: "anonymous attribution join"
      pattern: "first_open.*join|install.*attribution"
---

# 20.1-04: Preserve Exact Beach And Surf-Call Context Through Web-To-Native Handoff

<objective>
Turn surf-qualified web intent into an exact native first-value experience rather than a generic app-download dead end.

Purpose: Let a visitor move from a specific beach/window on web through installed-app, App Store fallback, or first open and see the same resolvable context in native, while measuring every boundary.

Output: Intent-aware CTA, versioned handoff creation/resolution, fallback preservation, native routing, joined attribution, tests, and signed-device verification instructions/evidence.
</objective>

<execution_context>
@/Users/stevenchandler/.codex/get-shit-done/workflows/execute-plan.md
@/Users/stevenchandler/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-REUSE-AUDIT.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-PATTERNS.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-VALIDATION.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/archive/2026-08-07-shipped/20-app-links-analytics-and-qa/20-VALIDATION.md
@/Users/stevenchandler/Desktop/dev/quiver/.planning/phases/20.1-durable-beach-follow-and-surf-call-retention-loop/20.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the web handoff CTA intent-aware and context-complete</name>
  <files>existing AppDeepLinkCTA/app CTA callers, handoff helper/API, iOS CTA tracking, tests</files>
  <read_first>
    - audited exact app-link generator and every active caller
    - /Users/stevenchandler/Desktop/dev/quiver/lib/analytics/ios-app-cta-tracking.ts
    - /Users/stevenchandler/Desktop/dev/quiver/app/app/spot/[slug]/page.tsx
    - Plan 20.1-02 handoff and intent contracts
  </read_first>
  <behavior>
    - "Unknown/non-surf utility visitors do not receive a dominant surf-app CTA."
    - "Surf-qualified visitors can open this exact call in Quiver with beach/window/source context."
    - "The handoff token/context is bounded, expiring, versioned, and not leaked into ordinary high-cardinality analytics."
    - "A page with beach but no defensible live window produces a beach-level handoff, not an invented call."
  </behavior>
  <action>
    Write failing tests first. Extend the existing app CTA/link helper and callers to accept the typed Phase 20.1 context. Add a bounded server-backed or audited equivalent handoff identity only if existing query params cannot survive the fallback/first-open boundary safely. Gate primary surf copy on explicit/inferred qualification. Keep a general follow/My Coast action primary for unknown intent. Track safe source/page/audience/window-presence metadata and handoff start without emitting token values. Do not change canonical page URLs.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/components/**/*deep-link*.test.tsx __tests__/lib/analytics/ios-app-cta-tracking.test.ts __tests__/app/app/spot/*.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Unknown, explicit surf, inferred surf, explicit non-surf, beach-only, exact-window, expired, and missing-data cases are covered.
    - Existing universal-link path remains the one public route.
    - App-not-installed fallback retains sufficient context for first-open join.
    - No generic CTA claims personalization that cannot be resolved.
  </acceptance_criteria>
  <done>Web creates an exact, honest, measurable handoff for surf-qualified visitors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend native routing and context resolution without a second router</name>
  <files>current native initial-link/universal-link router, route params/types, Home/BeachDetail context handling, tests</files>
  <read_first>
    - all current `/app/spot/:slug` and `window` route parsing
    - current RootNavigator/route params
    - Beach Detail selected-window hydration and cache seeding
    - recommendation availability/hold and exact-window fallback code
    - Plan 20.1-02 handoff types
  </read_first>
  <behavior>
    - "Installed-app links open the exact resolvable beach/window."
    - "Native fetches current truth and labels prior web context rather than rendering stale client data as current."
    - "Unavailable/expired windows fall back to the beach's current context with an explanation."
    - "Logged-out preview remains useful and save/watch actions use existing auth gates."
  </behavior>
  <action>
    Extend the existing route parser and navigation params with the minimum Phase 20.1 context. Resolve canonical beach identity, then exact live window/recommendation when available. Seed existing query caches only with trustworthy context and reconcile against the current server response. Present a calm fallback when the window passed, disappeared, or is blocked. Emit resolution success/failure reason without token or PII. Do not introduce a new navigation stack or duplicate Beach Detail screen.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver-native &amp;&amp; npm test -- --runInBand --runTestsByPath src/navigation/__tests__/*link*.test.ts src/screens/__tests__/beach-detail*.test.tsx src/screens/__tests__/home*.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Exact, beach-only, logged-out, expired, blocked, missing beach, missing window, and stale recommendation cases are pinned.
    - Existing routing callers remain compatible.
    - Current server truth controls the displayed call.
    - The user sees why exact context could not be restored.
  </acceptance_criteria>
  <done>Native opens or truthfully degrades the exact web context through the existing route stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Join App Store fallback to native first open</name>
  <files>web attribution write/read path, native install/first-open attribution join, PostHog/Supabase events, tests</files>
  <read_first>
    - current `native_open_attribution_joined` or equivalent implementation
    - install CTA storage/cookie/token path
    - native first-open and identity initialization ordering
    - event allowlist and RLS policies
  </read_first>
  <behavior>
    - "An eligible uninstalled iOS visitor retains the handoff identity through App Store fallback and first open when the platform path supports it."
    - "The join is idempotent, expires, and cannot attach one visitor's context to another user."
    - "Failure does not block onboarding/Home and is measurable by reason."
  </behavior>
  <action>
    Extend the audited attribution join rather than inventing a parallel install tracker. Preserve the handoff identity using the safest existing first-party mechanism and consume it exactly once after native identity/auth hydration permits a safe join. Protect against replay, cross-user attachment, stale tokens, and duplicate joins. Keep context recovery best-effort and non-blocking. Document any iOS platform limitation that prevents deterministic post-App-Store recovery and define the measurable fallback rather than claiming full coverage.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:unit --runInBand --runTestsByPath __tests__/**/*attribution*.test.ts &amp;&amp; cd /Users/stevenchandler/Desktop/dev/quiver-native &amp;&amp; npm test -- --runInBand --runTestsByPath src/**/*attribution*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Success, replay, expired, cross-user, duplicate, missing, and network-failure cases are tested.
    - Join coverage and context-resolution coverage can be queried separately.
    - No handoff failure blocks the native first useful screen.
  </acceptance_criteria>
  <done>Web-to-native acquisition can be measured through first open and exact context resolution.</done>
</task>

<task type="auto">
  <name>Task 4: Validate exact HTTPS handoff on web, simulator, and signed physical path</name>
  <files>phase evidence only plus minimal test fixes</files>
  <read_first>
    - Phase 20 AASA/assetlinks verification artifacts
    - current web/native build profiles and associated domains
    - 20.1-VALIDATION.md physical-device matrix
  </read_first>
  <action>
    Run automated web fallback and route tests plus simulator app-scheme checks. Prepare and, only with explicit release approval, perform the signed TestFlight/production-channel HTTPS universal-link and App Store fallback/first-open validation. Capture the exact URL class, installed/uninstalled state, destination screen, resolved beach/window, attribution events, timestamps, and screenshots/video. If the App Store breaks deterministic context recovery, document the platform boundary and the actual measurable fallback. Do not publish a build or deploy web changes without approval.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/quiver &amp;&amp; yarn test:e2e:guest --grep "app link|exact call|app store fallback" &amp;&amp; cd /Users/stevenchandler/Desktop/dev/quiver-native &amp;&amp; npm run e2e:shard -- discovery</automated>
  </verify>
  <acceptance_criteria>
    - Web installed/not-installed fallbacks pass automated checks.
    - Simulator scheme opens exact context.
    - Signed HTTPS evidence is captured or explicitly blocked by an approval/platform dependency.
    - Live manifests are rechecked before any production claim.
  </acceptance_criteria>
  <done>The phase has honest, reproducible evidence for every supported handoff boundary.</done>
</task>

</tasks>

<verification>
- Web/native focused tests, typecheck, scoped lint, and guest/native E2E pass.
- Exact links retain canonical web behavior and current App Store fallback.
- Joined attribution and context resolution are queryable as separate stages.
- No deploy, build publication, or production configuration change occurs without explicit approval.
</verification>

<success_criteria>
- Surf-qualified visitors can open the exact call they saw.
- Unknown/non-surf visitors are not pushed into an irrelevant app funnel.
- Native resolves current truth and explains fallback.
- Handoff effectiveness can be measured through first open and context resolution.
</success_criteria>

<output>
After completion, create `20.1-04-SUMMARY.md` and an exact-handoff evidence document containing supported paths, limitations, events, test/device artifacts, rollout status, and rollback steps.
</output>
