# Phase 20.1 Reuse Audit

**Audited:** 2026-08-24 (execution session; controller Claude Fable 5, audit worker Codex `gpt-5.6-sol`)
**Anchors:** `quiver` `origin/main` `d528663af` · `quiver-native` `origin/main` `1323c2cb`
**Companion evidence:** `dev/handoffs/phase-20-1-td/codex1-audit-report.md` (full verbatim reference scans), `codex1-scan-log.txt`

Status: the cleanup-train inventory (TD-01..TD-05), native continuity/watch/session ownership, and
release-truth rows below are verified against current code. The exhaustive web follow/intent-storage
capability sweep (Plan 20.1-01 Task 1) and mock-by-mock pixel review are **partially complete**;
open rows are marked `OPEN` rather than guessed.

## Cleanup candidates (TD-01..TD-05) — exact inventory and dispositions

| Slice | Exact files (current `origin/main`) | Production refs | Test/Maestro refs | Live replacement | File owner | Disposition | Planned test command |
|---|---|---|---|---|---|---|---|
| TD-01 | `src/lib/geo/nearest-beach.ts` | none (self only) | none | `src/hooks/use-nearest-beach.ts` (`get_nearby_beaches` RPC, 100-mi ceiling); local add-spot dedupe lives in `AddSpotSheet.tsx` | cleanup train | **LAND** | reference scan + `npm run typecheck` |
| TD-02 | `src/lib/prefetch-critical.ts`; stale refs in `src/__tests__/use-home-location.test.ts` (import + call), `src/__tests__/app-boat-onboarding-hierarchy.test.tsx` (jest.mock) | none — App.tsx call removed by `2be70d16` | test-only leftovers (in-scope) | profile: `use-profile.ts` via `root-navigator.tsx`; location: `use-current-location.ts` via `use-home-location.ts` | cleanup train | **LAND** | both focused suites + typecheck |
| TD-03 | `src/components/onboarding/boat/panels/payoff-value-card.tsx`; `src/__tests__/payoff-value-card.test.tsx`; stale negative assertions in `boat-completion-overlay.test.tsx` (~431-432), `maestro-boat-onboarding-flows.test.ts` (~162, ~448), `maestro-boat-android-contract.test.ts` (~68) | none | stale negatives only (in-scope) | `boat-completion-overlay.tsx` (payoff station/hold/handoff); `panels/review-panel.tsx` → `steps/payoff-step.tsx` (visible review payoff) | cleanup train (boat-only) | **LAND** (e2e gate: documented deferral) | 5 focused boat suites + typecheck; `e2e:boat:gate` deferred — no reserved safe simulator/backend lane |
| TD-04 | `src/components/forecast-chart.tsx`, `src/components/chart-error-boundary.tsx`, `src/lib/forecast-parsers.ts`, 2 dedicated tests | already absent | already absent | Home: `beach-hero.tsx` + `forecast-timeline.tsx`; Beach Detail: `looking-ahead-card.tsx` + `synced-forecast-chart-stack.tsx`; model/source-honesty: `synced-forecast-charts.ts`, `forecast-row-availability.ts`; swell: `swell-field-hero.tsx` + `src/lib/swell-field/**`; errors: `error-boundary.tsx` | n/a | **SUPERSEDED/NO-OP** — removed on main by `f9e15d47` (2026-08-23) | none (nothing to do) |
| TD-05 | delete `src/lib/plan-my-next-session/{candidate-sources,rank-recommendations,reason-mapping,source-flags}.ts`, `src/__tests__/plan-my-next-session-ranking.test.ts`; migrate 1 live assertion out of `plan-my-next-session-explanation.test.ts` then delete it; remove dead ratchet entry in `recommendation-hold-semantics.test.ts` (~96) | none — closed test-only island (`rank-recommendations` imports `candidate-sources`; both in-scope) | ranking/explanation tests (in-scope) + ratchet line (in-scope) | live PMNS: `beach-detail-best-window.ts`, `recommendation-explanation.ts`, `alert-prefill.ts` (+ `alert-rule-seed.ts`), components `recommended-session-card.tsx` / `why-this-call-sheet.tsx`; hold: `recommendation-hold.ts`; ranking: `home/my-surf-list-ranking.ts`, `week-scout/*` | cleanup train | **LAND** (retain live alert-prefill assertion at `beach-detail-best-window.test.ts`) | focused PMNS-UI/hold/Beach-Detail/week-scout-stability suites + typecheck |

Preserved and untouchable during the train: `alert-prefill.ts`, `beach-detail-best-window.ts`,
`recommendation-explanation.ts`, `forecast-window-proof.ts`, `src/components/plan-my-next-session/**`,
`src/screens/beach-detail.tsx`, `src/lib/recommendation-hold.ts`, week-scout libs, all quick-log/session code.

## Native continuity / watch / session primitives (Plan 20.1-01 Task 2, verified rows)

| Capability | Existing files | Current behavior | Similarity | Reuse decision |
|---|---|---|---|---|
| Nearest-beach resolution | `src/hooks/use-nearest-beach.ts` | server RPC `get_nearby_beaches`, 100-mi ceiling | strong | **Reuse** (TD-01 deletes the dead local twin) |
| Startup data warmup | `use-profile.ts` + `root-navigator.tsx`; `use-current-location.ts`/`use-home-location.ts` | query-driven; no imperative prefetch | strong | **Reuse**; TD-02 deletes the dead helper |
| Boat onboarding completion/payoff | `boat-completion-overlay.tsx`, `review-panel.tsx`, `payoff-step.tsx` | live payoff station + review payoff | strong | **Preserve**; TD-03 deletes the retired card only |
| Home/Beach Detail forecast rendering | `beach-hero.tsx`, `forecast-timeline.tsx`, `looking-ahead-card.tsx`, `synced-forecast-chart-stack.tsx`, `swell-field-hero.tsx` | canonical post-`f9e15d47` stack | strong | **Reuse**; mocks must map ONLY to these (TD-04 confirms no prototype remains) |
| PMNS recommendation/explanation | `beach-detail-best-window.ts`, `recommendation-explanation.ts` + 2 components | live Beach Detail recommendation card | strong | **Reuse/extend** for watched-call explanation copy; dormant policy stack deleted (TD-05) |
| Alert prefill | `alert-prefill.ts` → `beach-detail-best-window.ts` → `alert-rule-seed.ts` → beach-detail screen | live one-tap alert seeding | strong | **Extend** for `Watch this call` (BFR-09) — this is the watch seam; no new alert builder |
| Recommendation hold / Week Scout stability | `recommendation-hold.ts`; `week-scout/{canonical-week-scout,select-home-best,stability}.ts`; flag `stability-flag.ts` (`EXPO_PUBLIC_WEEK_SCOUT_STABILITY_ENABLED`) | canonical hold + env-gated stability | strong | **Reuse**; BFR-08 rollout is a release/config lane, not new code ownership |
| Quick log / sessions | Home quick log, SessionForm, rating/prefill, reminders | shipped baseline; 9 users tapped / 4 submitted (2026-06-01..08-23); did not move retention or sales | n/a | **Preserve as baseline** — prohibited as Phase 20.1 surface or metric (BFR-11) |
| Home ranking | `home/my-surf-list-ranking.ts`, `week-scout-home-adapter.ts` | canonical sourcing/dedupe/hold | strong | **Reuse** for BFR-07 continuity work |

## Web rows verified this session

| Capability | State | Evidence |
|---|---|---|
| `main -> prod` promotion | #605 MERGED 2026-08-24; `origin/main...origin/prod` = 2/90 | gh + rev-list |
| `feat/redeem-success-page` | 4 unique commits, 165 behind; overlaps utility/app-CTA surfaces | rev-list; disposition: retire by default, named cherry-pick if 20.1-03/04 claims one |
| Phase 21 trusted-forecast | engine/builder/tests/migrations ON `origin/main`, promoted dark (PR #522); worktrees gone | ls-tree + log; summaries' "uncommitted worktree" status is stale |
| Web follow/favorites/anon-state/intent storage deep sweep | **DONE 2026-08-24** — see "Web capability inventory" below | direct code/table inspection |
| Mock-by-mock mapping (6 mocks) | **DONE 2026-08-24** — see "Mock mapping" below; native mocks mapped against post-cleanup main | cleanup train landed first |

## Web capability inventory (Plan 20.1-01 Task 1 — swept 2026-08-24, `origin/main d528663af`)

| Capability | Existing files/tables | Current behavior | Similarity | Reuse decision | Gap | Risk / inflight owner |
|---|---|---|---|---|---|---|
| Beach favorites (account) | `favorite_beaches` table; `app/api/beaches/[id]/favorite/toggle`, `app/api/beaches/favorites`, `actions/beach/beach-favorite-actions.ts`, `components/favorite-button.tsx`, `lib/services/beach-query-service.ts` | Auth-gated (`withAuth`); **free tier capped at `FREE_FAVORITES_LIMIT = 3`**; read by alerts rules, similarity/streak/trial crons, best-days | strong (account side) | **Extend** — this is the durable-beach store for signed-in follows | No anonymous path; no topic dimension | Free-tier cap collides with BFR-04 "merge without entitlement changes" — anonymous follows >3 need a contract decision in 20.1-02. Crons/emails read this table: follow-driven inserts change email/alert audiences — must stay intentional |
| Anonymous action continuation | `PendingAction` pattern in `favorite-button.tsx`, `alert-capture-cta.tsx`, `beach-alert-cta.tsx` | Anonymous click stores a pending action, opens auth modal, resumes after sign-in | partial | **Reuse the pattern**, but it is single-action deferral, NOT local-first storage | BFR-03 needs versioned bounded local follow state that succeeds WITHOUT auth; no such store exists anywhere (localStorage sweep: no follow/beach store) | New primitive required (`create` with justification: no equivalent exists) |
| Anonymous↔account merge | none | — | none | **Create** (bounded) | BFR-04 idempotent merge does not exist; PendingAction resumes one action only | Must not auto-opt-in notifications or change entitlements |
| Intent/page-type classification | `lib/analytics/web-context.ts` (`SeoPageType`, `SeoQueryIntent` — e.g. `beach_water_temp`/`water_temp`), `lib/constants/intent-definitions.ts`, `surf-intents.ts` | Every SEO surface already emits typed page_type + query_intent | strong | **Reuse** as the BFR-01 evidence vocabulary | Explicit/inferred/unknown visitor-intent state machine is new, but its inputs exist | Do not invent a second page-type taxonomy |
| Utility answer-first templates | `components/intent/*` (TidePageContent, TideHeroSection, WaterTempOverviewSection, etc.), `/tide/[city]`, `/[intent]/[city]`, `app/beach/[slug]/water-temp` | Hero answer first; CTA slots (`InlineSignupCta`, `StickySignupBar`, `CTASection`, `AlertCaptureCta`) come after | strong | **Reuse**; `Follow this beach` slots into the existing CTA position | none structural | Preserve canonical/schema/answer-first (BFR-02) |
| Exact-window handoff | `app/app/spot/[slug]` (noindex, force-dynamic, `loadForecastWindowShareMetadata`, `ShareLinkOpenTracker`), `lib/share/forecast-window-share`, `app/app/handoff`, `components/intent/session-intelligence-intent-handoff.tsx` | Exact beach/window share+handoff route already resolves window metadata | strong | **Extend** for BFR-06 (`Open this exact call in Quiver`) | Prior-recommendation context + truthful-current-fallback copy | One router only; no second deep-link path (#174 native side) |
| Install attribution / first open | `install_attribution_tokens` (opaque token hash), `app/api/install-attribution/issue`+`redeem`, `app/api/android-tester-roster/first-open`, `app/api/events/link` | Token issue → store URL → redeem/join on first open | strong | **Reuse** for handoff→native join | iOS coverage vs Android roster asymmetry to verify in 20.1-04 | Phase 20 foundation; keep one joined identity path |
| Analytics taxonomy | `lib/analytics/event-taxonomy.ts` (`app_handoff_*` funnel, `ios_app_cta_*`, `seo_intent_page_window_clicked`; signed-out allowlist section) | Canonical event list with explicit anonymous allowlist | strong | **Extend** with mock-listed events via 20.1-02 reconciliation | `beach_follow_*`/`my_coast_*`/`watched_call_*` names absent (expected) | No parallel transport; anonymous events must join the signed-out allowlist |
| Experiments/holdouts | `lib/experiments/assignment-hash.ts`, `assignments.ts` | Deterministic assignment hashing | strong | **Reuse** for BFR-12 holdout/segmented measurement | — | — |
| Signup/alert capture CTAs | `components/seo/alert-capture-cta.tsx`, `signup-conversion-tracking` | Tracked view/click, PendingAction, auth modal | strong | **Preserve**; follow is additive, not a replacement | — | Do not turn follow into a signup wall (mock 01) |
| Legacy email prefs | `app/api/prefs/set` | Graceful no-op (link-expired page); `user_email_prefs` writes removed | n/a | **Leave alone** | — | Do not resurrect for follow topics |

## Mock mapping (matches / adapt / blocked)

| Mock | Mapping | Maps to | Deviations/notes |
|---|---|---|---|
| 01 web utility follow | **adapt** | Intent/utility templates + existing CTA slot; `Follow this beach` is a NEW bounded local-first primitive (no existing equivalent — justified create); topic default from `SeoQueryIntent` | Favorites API is auth-only, so mock's account-less follow cannot reuse it directly; sync step maps to PendingAction/auth modal |
| 02 web My Coast | **adapt** | New route composed of existing cards: `TideOverviewSection`, `WaterTempOverviewSection`, WQ/wind equivalents from intent/city components; "changed since last visit" is new bounded state | Surf rankings stay conditional on explicit/inferred intent (BFR-05); page itself is new but every data card has a live owner |
| 03 web surf handoff | **adapt** | `session-intelligence-intent-handoff.tsx` + `app/app/spot/[slug]` + `forecast-window-share` + `app_handoff_*` funnel + install-attribution join | CTA copy change to `Open this exact call in Quiver`; add `As of <forecast time>` freshness label; `Other opportunities` label rule (#147 semantics) |
| 04 native Home continuity | **adapt** | Post-cleanup `home.tsx`, `beach-hero.tsx`, `forecast-timeline.tsx`, `my-surf-list-ranking.ts`, `recommendation-hold.ts`, `week-scout-home-adapter.ts`, `stability.ts` | Change-cause attribution vocabulary (8 causes) is new copy/state on existing hold semantics; no state machine; provisional-vs-settled hero rule per #155 |
| 05 native Watch this call | **adapt** | `alert-prefill.ts` → `beach-detail-best-window.ts` → `alert-rule-seed.ts` → existing alert creation (Beach Detail); Home entry reuses the same seed path | One-tap = seeded creation WITHOUT opening the composer; idempotent duplicate-watch check is a new bounded guard on the existing rules store |
| 06 native watch return | **adapt** | Alert Center/watched history + notification routing + existing deep-link resolver; canonical verdict copy (#440) | `Still on`/`Call changed`/`Better nearby` eligibility+dedupe logic is new on the EXISTING delivery pipeline (no second sender); no session prompt in the return path (BFR-11) |

No mock is `blocked`: after the cleanup train every mocked native surface maps to a live owner, and
every web surface maps to an existing template/route plus explicitly-justified new bounded
primitives (local follow store, merge, My Coast route, watch eligibility).

## Anti-duplication verdicts

- One nearest-beach resolver (`use-nearest-beach.ts`). One startup data path (React Query). One
  boat payoff owner. One forecast rendering stack. One PMNS explanation projection. One alert
  seed path (`alert-rule-seed.ts`) — `Watch this call` extends it, never forks it.
- No new outcome/session flow, prompt, receipt, reminder, or store — Quick Log and session logging
  are baseline behavior (X-03/BFR-11).
- The audit branch is reference-only; never a merge base.
