# Phase 20.1 Locked Patterns

**Locked:** 2026-08-24 (Plan 20.1-01 execution session)
**Scope:** cleanup-train sequencing, native file ownership, and anti-duplication rules are LOCKED.
Web ownership/handoff pattern selection remains bounded-open (see Blockers) pending the full web
capability sweep; nothing below may be contradicted by that sweep without a recorded decision.

## Cleanup-train sequence (X-04) — LOCKED

```text
TD-00 freeze + exact-path inventory ........ DONE 2026-08-24 (codex1-audit-report.md)
TD-01 nearest-beach ........................ LAND  branch cleanup/td-01-nearest-beach
TD-02 startup prefetch ..................... LAND  branch cleanup/td-02-startup-prefetch
TD-03 payoff card (boat-only, disjoint) .... LAND  branch cleanup/td-03-payoff-card   (e2e gate: documented deferral)
TD-04 forecast prototype ................... SUPERSEDED/NO-OP (main f9e15d47) — no branch
TD-05 PMNS policy stack .................... LAND  branch cleanup/td-05-pmns-policy
```

- Every slice: one branch off `quiver-native` `origin/main` `1323c2cb`, one isolated commit,
  before/after reference scans, focused suites, typecheck, rollback = `git revert <commit>`.
- No Phase 20.1 product code in cleanup branches; no cleanup code in product branches.
- Phase 20.1 native implementation branches (20.1-05) start from main **after** the cleanup train
  merges, and rebase rather than duplicating deletions.
- Cleanup effects and retention treatment effects stay separately attributable (separate PRs,
  separate release notes).

## Native file ownership — LOCKED

| Surface | Canonical owner (post-cleanup) | Prohibited |
|---|---|---|
| Nearest-beach | `src/hooks/use-nearest-beach.ts` | any local recompute twin |
| Startup warmup | React Query via `use-profile.ts` / `use-current-location.ts` | imperative prefetch helpers |
| Boat payoff/completion | `boat-completion-overlay.tsx`; `review-panel.tsx` → `payoff-step.tsx` | resurrecting `PayoffValueCard` |
| Home rendering/ranking | `beach-hero.tsx`, `forecast-timeline.tsx`, `home/my-surf-list-ranking.ts`, `week-scout-home-adapter.ts` | second ranking store or state machine |
| Beach Detail forecast | `looking-ahead-card.tsx`, `synced-forecast-chart-stack.tsx`, `synced-forecast-charts.ts` | reintroducing the retired chart prototype |
| Swell visualization | `swell-field-hero.tsx` + `src/lib/swell-field/**` | — |
| PMNS explanation | `beach-detail-best-window.ts`, `recommendation-explanation.ts`, 2 components | reviving `rank-recommendations`/`candidate-sources`/`reason-mapping`/`source-flags` |
| Watch/alert seam | `alert-prefill.ts` → `alert-rule-seed.ts` → existing alert creation | a second alert builder for `Watch this call` |
| Hold/stability | `recommendation-hold.ts`; `week-scout/stability.ts` behind `EXPO_PUBLIC_WEEK_SCOUT_STABILITY_ENABLED` | a second resolver/hold store |
| Quick log/sessions | existing flow, untouched | any new outcome/session surface, prompt, receipt, reminder, store, or metric (BFR-11) |

## Mock interpretation — LOCKED rule, mapping OPEN

Mocks lock hierarchy/state/copy/continuity, not pixels. After TD-04's no-op confirmation, native
mocks 04-06 may map ONLY to the live stack above; any mock element implying the retired prototype
or a new session surface is a deviation to record, not to build. Full mock-by-mock mapping runs
against post-cleanup main (open item for 20.1-01 completion).

## Anti-duplication constraints — LOCKED

One favorites/follow model, one alert/watch path, one deep-link dispatcher/resolver, one analytics
taxonomy, one recommendation store, one session system (the existing one). Every 20.1-02+ `create`
decision must name the strong/partial equivalent it extends or say why none exists.

## Blockers / bounded-open questions

1. **Web follow/anon-ownership storage sweep** (Plan 20.1-01 Task 1) — required before 20.1-02
   contracts and 20.1-03 implementation. Not started in depth this session.
2. **N-01 device truth** — installed build/runtime/channel/update-id/flag from a physical device
   before any native release lane (does NOT block the cleanup train: code-only, unreleased).
3. **W-02 redeem branch** — retire-vs-cherry-pick call owed by the 20.1-03/04 lane before touching
   utility/app-CTA surfaces.
4. **N-03 unique branches** — re-judge `dazzling-chaplygin` (4 commits) and `blissful-torvalds`
   (1 commit) against post-cleanup main before 20.1-05 Home/Looking-Ahead work.
5. **TD-03 e2e** — `npm run e2e:boat:gate` deferred (no safe simulator/backend lane reserved this
   session); run it on the next approved E2E lane before a native release train.
