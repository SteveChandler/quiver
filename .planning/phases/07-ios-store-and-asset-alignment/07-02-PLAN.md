# Plan 07-02: Update iOS Constants And Stale Copy

## Goal

Make public iOS CTAs use one conservative source of truth while Apple's live page remains in preorder state.

## Scope

- Update `lib/constants/app-store.ts` so CTA text does not promise an immediate download.
- Keep App Store and TestFlight paths separate.
- Remove stale preorder variant usage from landing CTA component wiring.
- Add focused unit coverage for the shared iOS constants.
- Ensure hero visible CTA copy follows the shared constant even when the media asset has baked-in historical button text.

## Verification

- No active app code contains `Download Quiver`, `app-store-preorder`, or `app_store_listing`.
- App Store CTA tests read from the shared constants.
- Constants test locks current URL, CTA, status, smart banner argument, and TestFlight URL.
