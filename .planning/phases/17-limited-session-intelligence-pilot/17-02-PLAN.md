---
phase: 17-limited-session-intelligence-pilot
plan: 17-02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - components/beach-detail.tsx
  - components/beach-detail/session-intelligence-pilot.tsx
  - __tests__/components/beach-detail/session-intelligence-pilot.test.tsx
  - e2e/guest-spot-surf-report.spec.ts
autonomous: true
requirements:
  - SI-04
  - SI-07
---

# 17-02: Add Spot Page Pilot

<objective>
Show the top three upcoming surf windows on one existing major spot page surface
without removing beach hero, forecast, reviews, local intel, sessions, signup,
or nearby-spot content.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add the spot pilot wrapper</name>
  <files>components/beach-detail/session-intelligence-pilot.tsx</files>
  <read_first>
    - components/beach-detail/ARCHITECTURE.md
    - components/session-intelligence/best-surf-windows.tsx
    - lib/recommendations/session-intelligence-surface-adapters.ts
    - .planning/phases/17-limited-session-intelligence-pilot/17-PATTERNS.md
  </read_first>
  <action>
    Create a `SessionIntelligencePilot` spot wrapper that accepts `beach`,
    `forecasts`, and optional `beachTimezone`. Build recommendations from the
    already-loaded forecast rows and render `BestSurfWindows` with real spot-page
    copy. Return `null` when rows or recommendations are unavailable.
  </action>
  <verify>
    <automated>rg -n "SessionIntelligencePilot|BestSurfWindows|forecasts" components/beach-detail/session-intelligence-pilot.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The component performs no client fetch.
    - The component renders at most three windows.
    - Empty data returns no visual block.
    - Phase 16 Brand-Vault sticker treatment is preserved through `BestSurfWindows`.
  </acceptance_criteria>
  <done>The spot page has a reusable pilot module.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the pilot near top-level beach tabs</name>
  <files>components/beach-detail.tsx</files>
  <read_first>
    - components/beach-detail.tsx
    - components/beach-detail/ARCHITECTURE.md
    - components/beach-detail/beach-tabs.tsx
  </read_first>
  <action>
    Render the spot pilot after existing signup/alert/nudge surfaces and before
    `BeachTabs`. Pass the existing `beach` and `forecasts` from
    `useBeachDetailData()`. Keep Forecast, Reviews, Local Intel, Sessions,
    nearby spots, and the existing authenticated daily `BestSurfWindow` in place
    unless QA proves an unavoidable duplicate.
  </action>
  <verify>
    <automated>rg -n "SessionIntelligencePilot|BeachTabs|ForecastTab|ReviewsTab|IntelTab|SessionsTab" components/beach-detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The pilot appears before top-level tabs.
    - The existing tabs are still rendered.
    - No SEO metadata, route, schema, or server fetch code is changed.
  </acceptance_criteria>
  <done>The pilot is additive to the existing spot page.</done>
</task>

<task type="auto">
  <name>Task 3: Keep guest behavior useful and CTA-safe</name>
  <files>components/beach-detail/session-intelligence-pilot.tsx, components/beach-detail.tsx</files>
  <read_first>
    - components/session-intelligence/app-deep-link-cta.tsx
    - components/session-intelligence/why-this-call.tsx
    - CLAUDE.md
  </read_first>
  <action>
    Ensure the pilot is not wrapped in a signup gate, does not emit pre-auth
    funnel events, and preserves `WhyThisCall` plus app CTA anchors for every
    recommendation.
  </action>
  <verify>
    <automated>rg -n "PublicContentGate|signup_cta|auth_modal|app-deep-link-cta|WhyThisCall" components/beach-detail/session-intelligence-pilot.tsx components/beach-detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The pilot content is visible to anonymous visitors when data exists.
    - No pre-auth-only event name is emitted by the pilot.
    - App CTA anchors remain real links.
  </acceptance_criteria>
  <done>The spot pilot is public and CTA-safe.</done>
</task>

<task type="auto">
  <name>Task 4: Add spot tests</name>
  <files>__tests__/components/beach-detail/session-intelligence-pilot.test.tsx, e2e/guest-spot-surf-report.spec.ts</files>
  <read_first>
    - __tests__/components/beach-detail/tabs/forecast-tab.test.tsx
    - e2e/guest-spot-surf-report.spec.ts
    - e2e/utils/error-detection.ts
  </read_first>
  <action>
    Add component tests for populated render, empty suppression, and valid CTA
    links. Extend guest Playwright coverage so `/ca/san-diego/blacks` still
    exposes the Forecast/Reviews/Local Intel tab surface after the pilot renders.
  </action>
  <verify>
    <automated>yarn test:unit __tests__/components/beach-detail/session-intelligence-pilot.test.tsx --runInBand</automated>
    <automated>npx playwright test e2e/guest-spot-surf-report.spec.ts --project=guest</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if the pilot renders without recommendations.
    - Tests fail if CTA links are missing.
    - E2E confirms existing tab navigation remains available.
  </acceptance_criteria>
  <done>Spot pilot behavior is covered.</done>
</task>

</tasks>

<verification>

```bash
yarn test:unit __tests__/components/beach-detail/session-intelligence-pilot.test.tsx --runInBand
npx eslint --max-warnings=0 components/beach-detail.tsx components/beach-detail/session-intelligence-pilot.tsx __tests__/components/beach-detail/session-intelligence-pilot.test.tsx e2e/guest-spot-surf-report.spec.ts
npx playwright test e2e/guest-spot-surf-report.spec.ts --project=guest
```

</verification>

<success_criteria>

- `/ca/san-diego/blacks` shows up to three upcoming surf windows.
- Forecast, Reviews, Local Intel, and Sessions content remains reachable.
- Each window has `Why this call?` and a valid app CTA anchor.
- Anonymous users can view the pilot content.
</success_criteria>
