---
phase: 21-multi-forecaster-forecast-adjustment-and-production-ingestio
plan: 21-03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - lib/services/forecast/trusted-forecast-policy.ts
  - lib/services/forecast/trusted-forecast-adjustment.ts
  - lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts
  - lib/services/forecast/__tests__/trusted-forecast-contract.test.ts
autonomous: true
requirements:
  - MFA-03
  - MFA-04
  - MFA-05
must_haves:
  truths:
    - "Authority selection dedupes provider lineage and excludes model/buoy/evidence-only rows before voting (D-05 through D-08)."
    - "Fresh compatible spot WaveCast wins, then regional WaveCast, then configured validated regional authority; one valid authority activates without universal consensus (D-09, D-10)."
    - "Independent separation above 1.00 ft blocks and creates an alert; at or below 1.00 ft the primary range remains unchanged (D-11)."
    - "Spot supersedes regional, exposure must match, and NNW/SSW are never unioned (D-12)."
    - "Exactly one durable decision exists per beach/IANA local day and each forecast slot belongs to at most one decision (D-13)."
    - "Signed adjustment bands are exactly 0, 0.25, or 0.50 ft and only raw horizons 0 through 168 hours are eligible (D-14, D-15, D-16)."
  artifacts:
    - path: "lib/services/forecast/trusted-forecast-policy.ts"
      provides: "Versioned coverage, lineage, evidence, precedence, exposure, freshness, and stable tie-break policy."
    - path: "lib/services/forecast/trusted-forecast-adjustment.ts"
      provides: "Pure local-day authority, conflict, alert, and signed adjustment decision engine."
    - path: "lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts"
      provides: "Boundary matrix for policy, local day/DST, conflicts, bands, and horizons."
    - path: "lib/services/forecast/__tests__/trusted-forecast-contract.test.ts"
      provides: "Cross-language normalized issue compatibility across Seaside, SQL, and TypeScript."
  key_links:
    - from: "lib/services/forecast/trusted-forecast-adjustment.ts"
      to: "lib/services/forecast/trusted-forecast-policy.ts"
      via: "Validated issue filtering and deterministic primary/corroborator selection."
      pattern: "select.*Authority"
    - from: "TrustedForecastDecision"
      to: "public.trusted_forecast_decisions"
      via: "Fields align with the 21-02 migration/RPC contract."
      pattern: "localDate|policyVersion|appliedDeltaFt"
    - from: "/Users/stevenchandler/Desktop/dev/seaside/tests/fixtures/trusted_forecasts/corpus.json"
      to: "public.trusted_forecast_issues and TrustedForecastIssueInput"
      via: "Representative normalized issue contract test."
      pattern: "provider_lineage|valid_local_date|measurement_basis|revision_hash"
---

# 21-03: Coverage-Aware Decision Engine

<objective>
Implement the deterministic coverage-aware authority and bounded signed adjustment engine over immutable private issues.

Purpose: Convert normalized evidence into one reproducible beach/local-day decision without false consensus, incompatible exposure, threshold drift, or horizon leakage, implementing D-05 through D-16.
Output: Versioned policy, pure decision engine, durable-decision reuse contract, alert payloads, and exhaustive Jest tests.
</objective>

<execution_context>
@/Users/stevenchandler/.codex/get-shit-done/workflows/execute-plan.md
@/Users/stevenchandler/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@docs/README.md
@lib/services/ARCHITECTURE.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-CONTEXT.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-RESEARCH.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-PATTERNS.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode versioned coverage, lineage, and authority precedence</name>
  <files>lib/services/forecast/trusted-forecast-policy.ts, lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</files>
  <read_first>
    - lib/services/ARCHITECTURE.md
    - CLAUDE.md
    - docs/README.md
    - lib/services/forecast/trusted-forecast-adjustment.ts
    - lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-CONTEXT.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-RESEARCH.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
  </read_first>
  <behavior>
    - "Same-lineage rows never create corroboration; NJ mirrors and all Stormsurf endpoints count once (D-05, D-06)."
    - "Model/buoy and evidence-only rows never become human authority (D-07, D-08)."
    - "Precedence is compatible spot WaveCast, regional WaveCast, then configured validated regional authority, with freshness and stable tie-breaks (D-09, D-10)."
    - "Spot/regional and NNW/SSW inputs are not unioned; exposure compatibility is mandatory (D-12)."
  </behavior>
  <action>
    Write table-driven policy tests first, then extract policy from the current adjustment draft into a framework-free `trusted-forecast-policy.ts`. Define explicit server-private issue/evidence/scope types and a versioned immutable coverage policy that maps beaches/regions/exposures to approved authority priority. Filter invalid/stale/superseded/authority-ineligible rows, dedupe by provider lineage, prefer spot within WaveCast, and use stable issue-key tie-breaks after configured priority and freshness. Accept a single valid configured authority immediately. Do not retain the two-provider enum, infer exposure from display text, average/union ranges, or let provider mirrors/model pages vote.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</automated>
    <automated>npx eslint --max-warnings=0 lib/services/forecast/trusted-forecast-policy.ts lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover every provider lineage/evidence class and precedence tier.
    - One authority works without a consensus precondition.
    - Same-lineage disagreement and incompatible exposure cannot block or corroborate.
    - The policy version is carried into every new decision payload.
  </acceptance_criteria>
  <done>Versioned policy tests prove deterministic authority selection without false lineage, evidence, or exposure votes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the Seaside-to-SQL-to-TypeScript issue contract</name>
  <files>lib/services/forecast/__tests__/trusted-forecast-contract.test.ts, lib/services/forecast/trusted-forecast-policy.ts</files>
  <read_first>
    - CLAUDE.md
    - docs/README.md
    - /Users/stevenchandler/Desktop/dev/seaside/trusted_forecasts/models.py
    - /Users/stevenchandler/Desktop/dev/seaside/trusted_forecasts/parsers.py
    - /Users/stevenchandler/Desktop/dev/seaside/tests/fixtures/trusted_forecasts/corpus.json
    - supabase/migrations/20260727231500_create_trusted_external_forecast_adjustments.sql
    - lib/services/forecast/trusted-forecast-policy.ts
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-01-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
  </read_first>
  <behavior>
    - "One representative Seaside normalized issue fixture contains every required MFA-02 field with matching snake_case names, nullability, fixed-scale numeric ranges, enum vocabulary, timezone/date/timestamp semantics, and SHA-256 formats."
    - "The representative row is accepted by the exported strict TypeScript `TrustedForecastIssueInput` parser used by policy loading."
    - "The same row's required fields and constraints are present in `public.trusted_forecast_issues`; any drift in Seaside, SQL, or TypeScript fails the test."
  </behavior>
  <action>
    Add a strict exported TypeScript input schema/type at the policy boundary, then write a Jest contract test that reads one named representative normalized issue from the bounded Seaside corpus by fully qualified path. Validate it with the actual TypeScript parser and assert its field/enum/hash/nullability contract against the Quiver migration's `trusted_forecast_issues` definition. The test must prove one concrete row, not only compare independent lists of field names. Keep Seaside snake_case as the database boundary and perform any camelCase mapping only after strict validation. Do not duplicate a second schema ledger or loosen a field to make the test pass.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath lib/services/forecast/__tests__/trusted-forecast-contract.test.ts</automated>
    <automated>npx eslint --max-warnings=0 lib/services/forecast/trusted-forecast-policy.ts lib/services/forecast/__tests__/trusted-forecast-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test imports the production TypeScript schema and reads a production-parser corpus row.
    - Removing or renaming a required field in any of the three contracts fails.
    - Enum, numeric, timestamp/local-date/timezone, and hash constraints are asserted.
  </acceptance_criteria>
  <done>A representative normalized issue is proven compatible end-to-end across Seaside output, Quiver SQL storage, and TypeScript policy input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build one local-day decision with exact conflict and adjustment boundaries</name>
  <files>lib/services/forecast/trusted-forecast-adjustment.ts, lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</files>
  <read_first>
    - lib/services/forecast/trusted-forecast-policy.ts
    - lib/services/forecast/__tests__/trusted-forecast-contract.test.ts
    - lib/services/forecast/trusted-forecast-adjustment.ts
    - lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts
    - docs/archive/superpowers/specs/2026-07-17-canonical-session-decision-engine-design.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
  </read_first>
  <behavior>
    - "An existing durable `(beach_id, local_date)` decision is reused exactly; a new calculation cannot supersede it (D-13, D-18)."
    - "The primary local-day maximum range stays one row's min/max pair; independent nearest-edge separation 1.00 does not block and 1.001 blocks with one alert (D-11, D-14)."
    - "Inside range and gaps 0.499 are no-op; signed gaps ±0.500/±0.749 apply ±0.25; ±0.750 or larger apply ±0.50 (D-15)."
    - "Raw horizon exactly 0 or 168 is eligible; below 0 or above 168 is ineligible (D-16)."
    - "Spring-forward and fall-back days group by the beach's IANA local date, not fixed UTC windows."
  </behavior>
  <action>
    Replace the current UTC-window, midpoint-spread, union-range, subtraction-oriented offset, and `Math.round()` behavior. Accept validated issues, baseline slots after beach offset, beach timezone, build anchor, and existing durable decisions. Group by IANA local date, preserve the primary row associated with the highest primary daily maximum, compute independent nearest-edge separation, create one blocked decision plus one durable-alert payload only when separation is greater than 1.00, and keep corroborators from modifying the primary range. Compute intuitive addition-oriented `appliedDeltaFt` with explicit magnitude branches and a hard ±0.50 cap. Filter by raw elapsed hours before rounding. Return deterministic decisions/applications/alerts compatible with 21-02; never write the database or public DTOs from this pure module.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</automated>
    <automated>npx eslint --max-warnings=0 lib/services/forecast/trusted-forecast-policy.ts lib/services/forecast/trusted-forecast-adjustment.ts lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The complete decision boundary matrix in 21-VALIDATION.md is asserted for both signs.
    - Tests cover 23/25-hour local days, existing-decision reuse, one decision/day, one slot claim, and stable ordering.
    - `appliedDeltaFt` has addition semantics and cannot exceed ±0.50.
    - No generic confidence UI, public disagreement label, model training, or horizon beyond 168 hours is added.
  </acceptance_criteria>
  <done>The pure engine passes local-day, conflict, signed-band, horizon, reuse, uniqueness, and deterministic-order tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private issue rows -> authority policy | Stored rows remain untrusted until validated for lineage, evidence class, freshness, coverage, exposure, basis, and revision status. |
| Decision engine -> persistence payload | A deterministic pure result must carry enough identity/version data for database uniqueness and replay enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T21-03 | Spoofing | Provider corroboration | mitigate | Lineage dedupe, evidence-class filtering, approved policy, stable precedence/tie-break tests. |
| T21-10 | Tampering | Exposure and primary range | mitigate | Exact coverage map, incompatible-exposure exclusion, no union/average, primary-row preservation. |
| T21-11 | Repudiation | Daily decision identity | mitigate | Existing durable decision reuse, one local-date key, policy version and evidence IDs in private payload. |
| T21-12 | Tampering | Threshold/horizon math | mitigate | Explicit signed bands and raw-hour comparisons with complete boundary tests. |
| T21-SC | Tampering | Dependencies | mitigate | No package installation; use existing TypeScript/Date/Intl facilities and repository helpers. |
</threat_model>

<verification>

```bash
source ~/.nvm/nvm.sh
nvm use 22
yarn test:unit --runInBand --runTestsByPath \
  lib/services/forecast/__tests__/trusted-forecast-contract.test.ts \
  lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts
npx eslint --max-warnings=0 \
  lib/services/forecast/trusted-forecast-policy.ts \
  lib/services/forecast/trusted-forecast-adjustment.ts \
  lib/services/forecast/__tests__/trusted-forecast-contract.test.ts \
  lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts
```

</verification>

<success_criteria>

- MFA-03 lineage/evidence rules and MFA-04 coverage/conflict behavior are deterministic and fully tested.
- MFA-05 exact signed bands and 0-168 raw-hour eligibility pass.
- D-05 through D-16 are each represented by named test cases.
- The engine remains private, pure, framework-free, and contains no database writes or UI work.

</success_criteria>

<output>
Create `.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-03-SUMMARY.md` when done.
</output>
