---
phase: 21-multi-forecaster-forecast-adjustment-and-production-ingestio
plan: 21-05
type: execute
wave: 4
depends_on:
  - 21-01
  - 21-02
  - 21-03
  - 21-04
files_modified:
  - /Users/stevenchandler/Desktop/dev/seaside/scripts/verify_trusted_forecast_ingestion.py
  - /Users/stevenchandler/Desktop/dev/seaside/tests/test_fetch_trusted_forecasts.py
  - scripts/trusted-forecast-production-smoke.ts
  - scripts/__tests__/trusted-forecast-production-smoke.test.ts
  - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
autonomous: false
requirements:
  - MFA-08
must_haves:
  truths:
    - "A read-only live verifier enumerates exactly 17 sources, reports sanitized contract/health evidence, and exits nonzero for any enabled-source failure."
    - "Focused and full Seaside, Quiver, database, privacy, and build gates are green before any default-on production serving action."
    - "Rollout order is schema, production Seaside ingestion, live parser/parity verification, Quiver serving, audit verification, then local launchd retirement (D-25)."
    - "Ingestion and serving remain independent default-on/explicit-false controls; normal rollout performs no env mutation, and any future env/secret/enable/disable change requires fresh authorization outside this plan (D-24, D-26)."
    - "Seaside deploy and Quiver forecast/audit writes run only from clean detached worktrees at authorized SHAs; Quiver writes also require exact READY production Vercel deployment/project/commit provenance."
    - "The exact local launchd service is removed only after production parity and separate approval; prior snapshots and plist recovery evidence remain preserved."
  artifacts:
    - path: "/Users/stevenchandler/Desktop/dev/seaside/scripts/verify_trusted_forecast_ingestion.py"
      provides: "Read-only sanitized fixture/live verifier for all 17 sources."
    - path: "scripts/trusted-forecast-production-smoke.ts"
      provides: "Exact beach/local-date/build-key preflight and production-confirmed forecast/audit execution."
    - path: ".planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md"
      provides: "Exact local, live, privacy, audit, approval, rollout, and remaining-risk evidence."
  key_links:
    - from: "21-VERIFICATION.md schema evidence"
      to: "production Seaside approval"
      via: "D-25 ordered rollout gate."
      pattern: "schema.*Seaside.*parity.*Quiver.*audit.*launchd"
    - from: "production receipt audit"
      to: "launchd retirement approval"
      via: "Parity and complete durable attribution must pass first."
      pattern: "receipt|parity|launchd"
    - from: "Tasks 7 and 11 exact target authorization"
      to: "scripts/trusted-forecast-production-smoke.ts"
      via: "Mandatory beach ID, local date, expected build key, mode, and production confirmation arguments."
      pattern: "beach-id.*local-date.*expected-build-key.*confirm-production"
    - from: "Vercel deployment API provenance"
      to: "Tasks 10 and 12 write-capable runner"
      via: "Exact deployment ID, project/team, production target, READY state, commit SHA, clean detached worktree HEAD/status."
      pattern: "projectId.*readyState.*gitSource"
---

# 21-05: Verification and Default-On Rollout

<objective>
Prove the complete private ingestion/decision/persistence path locally and read-only live, then execute production transitions only through explicit ordered approvals.

Purpose: Complete MFA-08 without premature default-on serving, private-data leakage, unverified production writes, or early local scheduler retirement, implementing D-23 through D-26.
Output: Read-only verifier, full validation evidence, staged approval gates, production parity/audit evidence when approved, and recoverable launchd retirement only after parity.
</objective>

<execution_context>
@/Users/stevenchandler/.codex/get-shit-done/workflows/execute-plan.md
@/Users/stevenchandler/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@docs/README.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-CONTEXT.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-RESEARCH.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-PATTERNS.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-01-SUMMARY.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-03-SUMMARY.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-04-SUMMARY.md
@/Users/stevenchandler/Desktop/dev/seaside/AGENTS.md
@/Users/stevenchandler/Desktop/dev/surf-forecast-ingestion/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitized ingestion verification and exact-target production smoke tooling</name>
  <files>/Users/stevenchandler/Desktop/dev/seaside/scripts/verify_trusted_forecast_ingestion.py, /Users/stevenchandler/Desktop/dev/seaside/tests/test_fetch_trusted_forecasts.py, scripts/trusted-forecast-production-smoke.ts, scripts/__tests__/trusted-forecast-production-smoke.test.ts</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/seaside/AGENTS.md
    - /Users/stevenchandler/Desktop/dev/seaside/CLAUDE.md
    - /Users/stevenchandler/Desktop/dev/seaside/docs/README.md
    - /Users/stevenchandler/Desktop/dev/seaside/scripts/gfs_wave_comparison_report.py
    - /Users/stevenchandler/Desktop/dev/seaside/trusted_forecasts/sources.py
    - /Users/stevenchandler/Desktop/dev/seaside/crons/fetch_trusted_forecasts.py
    - scripts/regenerate-enhanced-forecasts.ts
    - lib/services/forecast/forecast-builder.ts
    - /Users/stevenchandler/Desktop/dev/quiver/.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
  </read_first>
  <behavior>
    - "`--live --no-write` enumerates exactly the 17 configured source keys in deterministic order."
    - "Output contains source key, sanitized final host/status, parser version, issue count, freshness, evidence class, and failure class only."
    - "Any enabled source fetch/parser failure exits nonzero; no Supabase write method is called."
    - "Narratives, source body, full source URLs/query strings, source hashes, issue/decision IDs, and credentials never appear (D-23)."
    - "The production smoke runner requires `--beach-id`, `--local-date`, `--expected-build-key`, an explicit mode, and `--confirm-production`."
    - "It resolves the actual beach/local-date/build-key target before mutation and exits without any write when any authorized value differs."
  </behavior>
  <action>
    Write CLI/no-write/sanitization tests first, then create an argparse-based async verifier that reuses production source/fetch/parser contracts without invoking persistence. Require both `--live` and `--no-write`, deterministic source order, bounded network behavior, and nonzero exit on any enabled failure. Also create `scripts/trusted-forecast-production-smoke.ts` with focused argument/preflight tests. Require `--mode=forecast|audit`, `--beach-id`, `--local-date`, `--expected-build-key`, and `--confirm-production`; reject missing/duplicate/malformed arguments. Before calling any write-capable builder path, load the explicit beach, derive its IANA local date/build key from the same production calculation, print only sanitized target metadata, and byte-compare all three values. On mismatch, missing beach, ambiguous target, or preflight read failure, exit nonzero before a write-capable dependency is invoked. Inject read/write dependencies so tests prove abort-before-write. Do not retain count-based target selection, add dependencies, or expose private content.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 --with-requirements requirements.txt --with pytest==8.3.5 python -m pytest -p no:cacheprovider tests/test_fetch_trusted_forecasts.py tests/test_scheduler_registration.py -q</automated>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 --with-requirements requirements.txt --with pytest==8.3.5 python scripts/verify_trusted_forecast_ingestion.py --help</automated>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath scripts/__tests__/trusted-forecast-production-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove no-write behavior even on mixed success/failure.
    - The verifier reports exactly 17 configured results and fails closed for enabled-source failure.
    - Output negative assertions cover every D-23 private field category.
    - Smoke tests prove exact target success and zero write calls for missing arguments, target mismatch, missing beach, and preflight failure.
    - Both forecast and audit modes use the same mandatory exact-target preflight.
    - No live call is required for the automated unit gate.
  </acceptance_criteria>
  <done>Read-only source verification and exact-target production smoke tooling are deterministic, sanitized, and fail closed before unintended writes.</done>
</task>

<task type="auto">
  <name>Task 2: Run complete local gates and record privacy/audit evidence</name>
  <files>.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md
    - docs/README.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-01-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-03-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-04-SUMMARY.md
    - package.json
    - /Users/stevenchandler/Desktop/dev/seaside/requirements.txt
  </read_first>
  <action>
    Run the focused Quiver, exact-target smoke-runner, Seaside, and local Supabase/pgTAP commands first, then the broader gates from 21-VALIDATION.md. Require the completed Wave-0 uv and Docker preflights; start `/Applications/Docker.app` and wait for `docker info` when needed. Use the exact isolated Python 3.11 uv command with repository requirements and `pytest==8.3.5`; do not create a repository venv or change manifests. Record every command/result, blocker, test count, final default-on/explicit-false flag contract, and actionable fix in `21-VERIFICATION.md`. Verify cross-language contract, exact-target abort-before-write behavior, public negative serialization, privileges, append-only behavior, canonical replay/rollback, receipt reconciliation, durable reuse, uniqueness races, first-write snapshots, source inventory, and scheduler. Normal default-on rollout performs no production env mutation. Any future enable/disable/secret/env change is outside this rollout and requires a fresh, separately scoped authorization. No browser E2E is required unless route/UI scope changes.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn typecheck &amp;&amp; yarn typecheck:forecast-gate &amp;&amp; NODE_OPTIONS="--max-old-space-size=8192" yarn lint &amp;&amp; yarn test:unit --bail=0 &amp;&amp; VERCEL_ENV=preview yarn build</automated>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 --with-requirements requirements.txt --with pytest==8.3.5 python -m pytest -p no:cacheprovider tests/ -v --tb=short</automated>
    <automated>docker info &gt;/dev/null &amp;&amp; yarn db:reset &amp;&amp; supabase test db</automated>
    <manual>Review `21-VERIFICATION.md` and confirm every MFA-01 through MFA-08 row has exact passing evidence or an explicit blocker; do not advance rollout with any blocker.</manual>
  </verify>
  <acceptance_criteria>
    - All focused and full gates pass, or rollout remains blocked with exact evidence.
    - Verification records no browser E2E requirement because no UI/route work occurred.
    - Privacy and first-write assertions are behavior-based, not only source-text checks.
    - Exact-target runner tests prove no write-capable dependency is called before beach/local-date/build-key equality.
    - Default-on rollout requires no env mutation; future env/secret/enable/disable changes are explicitly outside this execution.
    - No production write, deploy, flag/env mutation, or launchd removal occurs in this task.
  </acceptance_criteria>
  <done>All local focused/full gates are recorded green, or rollout is explicitly blocked with exact failing evidence and no production mutation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Authorize ongoing default-on production ingestion writes</name>
  <files>none; authorization record only</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/seaside/AGENTS.md
    - /Users/stevenchandler/Desktop/dev/seaside/crons/fetch_trusted_forecasts.py
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Before Seaside deploy because ingestion is default-on, present the exact Seaside commit SHA, Fly production app `quiver-ml`, trusted-ingestion job ID and six-hour schedule, fixed 17-source scope, target private tables, expected append-only run/source/issue rows per run, indefinite recurring production write effect beginning after deploy, one controlled immediate run, aggregate unhealthy behavior, and explicit-false rollback. Request authorization for exactly that ongoing default-on six-hour write scope plus the controlled immediate run. Do not bundle Seaside deployment authorization, perform an env mutation, deploy, or write in this task.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; git rev-parse HEAD &amp;&amp; fly status --app quiver-ml</automated>
    <manual>Confirm authorization names the SHA/app, 17-source tables, six-hour recurring scope, controlled immediate run, append-only side effects, and rollback.</manual>
  </verify>
  <acceptance_criteria>
    - Ongoing six-hour and controlled immediate-run side effects are explicit before default-on code can deploy.
    - Authorization is for the exact recurring scope, not a one-time or “first write” proxy.
    - No deploy or write runs in this task.
  </acceptance_criteria>
  <done>The exact ongoing default-on six-hour ingestion scope plus one immediate run is authorized, or rollout remains blocked.</done>
  <resume-signal>Reply `authorize ongoing trusted ingestion: quiver-ml, repo [SHA], 17 sources every six hours plus one immediate run` or deny.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: [BLOCKING] Authorize the exact Seaside production deploy</name>
  <files>none; authorization record only</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/seaside/AGENTS.md
    - /Users/stevenchandler/Desktop/dev/seaside/scripts/deploy.sh
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Present the exact Seaside commit SHA, clean owned diff, Fly app/region/machine targets resolved by `scripts/deploy.sh`, image/build side effects, process restart, scheduler registration change, approved ongoing-write prerequisite from Task 3, health checks, and rollback revision. Request deploy authorization only; do not deploy or change env here.
  </action>
  <verify>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; git rev-parse HEAD &amp;&amp; git status --short &amp;&amp; fly status --app quiver-ml</automated>
    <manual>Confirm approval names the SHA, `quiver-ml` production environment, deploy command, restart/scheduler effects, and rollback.</manual>
  </verify>
  <acceptance_criteria>
    - Seaside deploy authorization is separate from ongoing-write authorization; env changes are outside this rollout.
    - The ongoing six-hour write-scope authorization is already recorded.
  </acceptance_criteria>
  <done>The exact Seaside production deployment is authorized or remains blocked.</done>
  <resume-signal>Reply `authorize Seaside deploy: quiver-ml, repo [SHA]` or deny.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Deploy the authorized Seaside SHA and verify scheduler health</name>
  <files>none; Fly production deployment only</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/seaside/scripts/deploy.sh
    - /Users/stevenchandler/Desktop/dev/seaside/scheduler.py
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Abort if Task 4's Seaside SHA/app differs from Task 3's ongoing-write authorization. From `/Users/stevenchandler/Desktop/dev/seaside` create a temporary dedicated detached worktree at the exact authorized SHA; never deploy the current dirty directory. In the same guarded shell, require `AUTHORIZED_FLY_APP=quiver-ml`, the write-scope SHA/app to match the deploy authorization, HEAD to equal the authorized SHA, and `git status --porcelain` to be empty before invoking `scripts/deploy.sh` from inside that worktree. Capture image/revision/restart, then verify `/health`, `/crons/status`, and exact six-hour registration. Do not mutate env or perform unrelated actions.
  </action>
  <verify>
    <automated>zsh -lc 'set -euo pipefail; : "${AUTHORIZED_SEASIDE_SHA:?}"; : "${AUTHORIZED_INGEST_SHA:?}"; : "${AUTHORIZED_FLY_APP:?}"; : "${AUTHORIZED_INGEST_APP:?}"; test "$AUTHORIZED_FLY_APP" = "quiver-ml"; test "$AUTHORIZED_INGEST_APP" = "quiver-ml"; test "$AUTHORIZED_INGEST_SHA" = "$AUTHORIZED_SEASIDE_SHA"; RELEASE_ROOT="$(mktemp -d /tmp/seaside-phase21-deploy.XXXXXX)"; RELEASE_WORKTREE="$RELEASE_ROOT/worktree"; trap '\''git -C /Users/stevenchandler/Desktop/dev/seaside worktree remove --force "$RELEASE_WORKTREE" &gt;/dev/null 2&gt;&amp;1 || true; rmdir "$RELEASE_ROOT" &gt;/dev/null 2&gt;&amp;1 || true'\'' EXIT; git -C /Users/stevenchandler/Desktop/dev/seaside worktree add --detach "$RELEASE_WORKTREE" "$AUTHORIZED_SEASIDE_SHA"; test "$(git -C "$RELEASE_WORKTREE" rev-parse HEAD)" = "$AUTHORIZED_SEASIDE_SHA"; test -z "$(git -C "$RELEASE_WORKTREE" status --porcelain)"; cd "$RELEASE_WORKTREE"; ./scripts/deploy.sh; curl -fsS https://quiver-ml.fly.dev/health; curl -fsS https://quiver-ml.fly.dev/crons/status'</automated>
  </verify>
  <acceptance_criteria>
    - Deployed revision matches the authorized SHA.
    - Deploy command runs only from a clean detached Seaside worktree at that SHA with app `quiver-ml` and matching ongoing-write authorization.
    - Health and exact six-hour scheduler registration pass.
  </acceptance_criteria>
  <done>The authorized Seaside revision is live and healthy with the trusted-ingestion scheduler registered.</done>
</task>

<task type="auto">
  <name>Task 6: Execute and verify the authorized immediate trusted-ingestion run</name>
  <files>none; append-only production ingest run/source/issue rows</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/seaside/crons/fetch_trusted_forecasts.py
    - /Users/stevenchandler/Desktop/dev/seaside/scripts/verify_trusted_forecast_ingestion.py
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Abort unless Task 3's ongoing six-hour-plus-immediate-run authorization and Task 5's deployed SHA/app/scheduler all match. Execute the one authorized immediate production run on `quiver-ml`, then run the read-only 17-source verifier and private count/freshness/lineage audit. Confirm the recurring six-hour scheduler is active under the same authorization. Persist partial success but treat any enabled source failure as rollout-blocking. Redact narratives, URLs, hashes, and IDs.
  </action>
  <verify>
    <automated>fly ssh console --app quiver-ml -C "python -c 'import asyncio; from crons.fetch_trusted_forecasts import run; asyncio.run(run())'"</automated>
    <automated>cd /Users/stevenchandler/Desktop/dev/seaside &amp;&amp; PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 --with-requirements requirements.txt --with pytest==8.3.5 python scripts/verify_trusted_forecast_ingestion.py --live --no-write</automated>
  </verify>
  <acceptance_criteria>
    - One controlled immediate run writes append-only private evidence and the same authorization covers subsequent six-hour runs.
    - 17/17 live contracts and aggregate health pass before Quiver rollout.
  </acceptance_criteria>
  <done>The immediate run and authorized ongoing six-hour production ingestion scope are healthy, private, and parity-verified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 7: [BLOCKING] Authorize default-on Quiver forecast writes before deploy</name>
  <files>none; authorization record only</files>
  <read_first>
    - AGENTS.md
    - CLAUDE.md
    - docs/GIT_WORKFLOW.md
    - scripts/regenerate-enhanced-forecasts.ts
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Before Quiver deploy, present `AUTHORIZED_MAIN_SHA`, `AUTHORIZED_PREVIOUS_PROD_SHA`, the expected merge tree hash derived from those exact tips, production Vercel project ID/team ID/environment, default-on recurring forecast-generation write scope after deploy, private read/RPC/write tables, and the controlled smoke runner's exact beach ID, IANA local date, and expected build key. Include privacy/receipt safeguards, explicit-false rollback, and confirmed Seaside parity. Request forecast-write authorization for only a deployed prod merge whose two authorized tips are ancestors and whose tree matches the presented expected tree. Do not bundle deploy/audit authorization, mutate env, or write here.
  </action>
  <verify>
    <automated>git rev-parse main &amp;&amp; git rev-parse prod &amp;&amp; git merge-tree --write-tree prod main &amp;&amp; vercel project ls</automated>
    <manual>Confirm authorization names both input SHAs, expected merge tree, project/team IDs, recurring default-on write scope, exact beach/local-date/build-key smoke target, and rollback.</manual>
  </verify>
  <acceptance_criteria>
    - Forecast-write authorization precedes Quiver deploy.
    - No deploy or forecast write runs in this task.
  </acceptance_criteria>
  <done>Default-on production forecast writes are explicitly authorized or Quiver deploy remains blocked.</done>
  <resume-signal>Reply `authorize Quiver forecast writes: production, main [SHA], previous prod [SHA], expected tree [hash], beach [ID], local date [date], build [key]` or deny.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 8: [BLOCKING] Authorize the exact Quiver production deploy</name>
  <files>none; authorization record only</files>
  <read_first>
    - AGENTS.md
    - CLAUDE.md
    - docs/GIT_WORKFLOW.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Present `AUTHORIZED_MAIN_SHA`, `AUTHORIZED_PREVIOUS_PROD_SHA`, the expected merge tree hash already authorized in Task 7, production Vercel project ID/team ID, regular `main` to `prod` merge/push commands, automatic production deployment side effect, expected default-on write behavior already authorized in Task 7, local gate evidence, and rollback commit. Explain that the merge creates a new `DEPLOYED_PROD_SHA`, which must have both authorized tips as ancestors and the authorized expected tree. Request deploy authorization only; do not merge/push/deploy here.
  </action>
  <verify>
    <automated>git rev-parse main &amp;&amp; git rev-parse prod &amp;&amp; git status --short &amp;&amp; vercel project ls</automated>
    <manual>Confirm approval names both input SHAs, expected tree, production project, merge/push side effect, resulting deployed-SHA rule, and rollback.</manual>
  </verify>
  <acceptance_criteria>
    - Deploy approval is separate from forecast-write approval; env changes are outside this rollout.
    - Main SHA, previous prod SHA, and expected tree equal Task 7's authorized forecast-write provenance.
  </acceptance_criteria>
  <done>The exact Quiver production deploy is authorized or remains blocked.</done>
  <resume-signal>Reply `authorize Quiver deploy: main [SHA] + previous prod [SHA] -> expected tree [hash], project [name]` or deny.</resume-signal>
</task>

<task type="auto">
  <name>Task 9: Promote the authorized inputs and record the deployed prod merge SHA</name>
  <files>none; prod branch and Vercel production deployment only</files>
  <read_first>
    - docs/GIT_WORKFLOW.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Abort if Task 7/8 `AUTHORIZED_MAIN_SHA`, `AUTHORIZED_PREVIOUS_PROD_SHA`, expected tree, branch tips, project/team IDs, forecast-write scope, or local gate evidence differ. Use a dedicated clean release worktree, fetch origin, require `origin/main` and `origin/prod` to equal the authorized tips, check out `prod`, regular-merge the exact authorized main SHA, and compute `DEPLOYED_PROD_SHA`. Before push, require both authorized tips to be ancestors of `DEPLOYED_PROD_SHA` and require its tree to equal the authorized expected tree. Push only that prod merge per `docs/GIT_WORKFLOW.md`; do not use the dirty planning worktree. Inspect the Vercel production deployment until terminal, require its commit to equal `DEPLOYED_PROD_SHA`, and record that SHA, immutable deployment ID, and project/team IDs in `21-VERIFICATION.md` for Tasks 10/12 provenance guards. Do not mutate env or run a controlled write here.
  </action>
  <verify>
    <automated>zsh -lc 'set -euo pipefail; : "${AUTHORIZED_MAIN_SHA:?}"; : "${AUTHORIZED_PREVIOUS_PROD_SHA:?}"; : "${AUTHORIZED_EXPECTED_TREE:?}"; git fetch origin main prod; test "$(git rev-parse origin/main)" = "$AUTHORIZED_MAIN_SHA"; test "$(git rev-parse origin/prod)" = "$AUTHORIZED_PREVIOUS_PROD_SHA"; test "$(git merge-tree --write-tree "$AUTHORIZED_PREVIOUS_PROD_SHA" "$AUTHORIZED_MAIN_SHA")" = "$AUTHORIZED_EXPECTED_TREE"'</automated>
    <manual>Verify `DEPLOYED_PROD_SHA` contains both authorized tips, has the authorized tree, and is the commit reported by the terminal Vercel production deployment.</manual>
  </verify>
  <acceptance_criteria>
    - Only a prod merge with the authorized main/previous-prod ancestry and expected tree is promoted through the documented one-way branch flow.
    - `DEPLOYED_PROD_SHA`, production deployment ID, project/team IDs, and terminal status are recorded.
  </acceptance_criteria>
  <done>The exact authorized Quiver revision is deployed to production without using the dirty planning worktree.</done>
</task>

<task type="auto">
  <name>Task 10: Execute the authorized exact-target production forecast write</name>
  <files>none; one controlled production forecast generation and its atomic audit rows</files>
  <read_first>
    - scripts/trusted-forecast-production-smoke.ts
    - lib/services/forecast/trusted-forecast-persistence.ts
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Abort unless Task 9 recorded an exact `DEPLOYED_PROD_SHA` and READY production deployment ID whose project/team IDs and extracted commit SHA match it. Reverify that `AUTHORIZED_MAIN_SHA` and `AUTHORIZED_PREVIOUS_PROD_SHA` are ancestors of `DEPLOYED_PROD_SHA` and that its tree equals `AUTHORIZED_EXPECTED_TREE`. Create a temporary dedicated detached Quiver worktree at `DEPLOYED_PROD_SHA`; never run the writer from the dirty planning directory. In one guarded shell, require clean worktree status/HEAD, call the Vercel deployment API for the exact recorded deployment ID, compare `projectId`, `target=production`, `readyState=READY`, and `gitSource.sha`/`meta.githubCommitSha`, then invoke the runner from that worktree with the exact authorized beach/local-date/build-key. Any provenance or target mismatch aborts before the runner/write. Read back receipt/decision/application/snapshot counts and verify ambiguity or uniqueness races never degrade to baseline.
  </action>
  <verify>
    <automated>zsh -lc 'set -euo pipefail; : "${VERCEL_TOKEN:?}"; : "${AUTHORIZED_MAIN_SHA:?}"; : "${AUTHORIZED_PREVIOUS_PROD_SHA:?}"; : "${AUTHORIZED_EXPECTED_TREE:?}"; : "${DEPLOYED_PROD_SHA:?}"; : "${AUTHORIZED_VERCEL_PROJECT_ID:?}"; : "${AUTHORIZED_VERCEL_TEAM_ID:?}"; : "${AUTHORIZED_PRODUCTION_DEPLOYMENT_ID:?}"; : "${AUTHORIZED_BEACH_ID:?}"; : "${AUTHORIZED_LOCAL_DATE:?}"; : "${AUTHORIZED_BUILD_KEY:?}"; git -C /Users/stevenchandler/Desktop/dev/quiver merge-base --is-ancestor "$AUTHORIZED_MAIN_SHA" "$DEPLOYED_PROD_SHA"; git -C /Users/stevenchandler/Desktop/dev/quiver merge-base --is-ancestor "$AUTHORIZED_PREVIOUS_PROD_SHA" "$DEPLOYED_PROD_SHA"; test "$(git -C /Users/stevenchandler/Desktop/dev/quiver rev-parse "$DEPLOYED_PROD_SHA^{tree}")" = "$AUTHORIZED_EXPECTED_TREE"; DEPLOYMENT_JSON="$(curl -fsS -H "Authorization: Bearer $VERCEL_TOKEN" "https://api.vercel.com/v13/deployments/$AUTHORIZED_PRODUCTION_DEPLOYMENT_ID?teamId=$AUTHORIZED_VERCEL_TEAM_ID")"; test "$(jq -er .projectId &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "$AUTHORIZED_VERCEL_PROJECT_ID"; test "$(jq -er .target &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "production"; test "$(jq -er .readyState &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "READY"; test "$(jq -er ".gitSource.sha // .meta.githubCommitSha" &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "$DEPLOYED_PROD_SHA"; RELEASE_ROOT="$(mktemp -d /tmp/quiver-phase21-forecast.XXXXXX)"; RELEASE_WORKTREE="$RELEASE_ROOT/worktree"; trap '\''git -C /Users/stevenchandler/Desktop/dev/quiver worktree remove --force "$RELEASE_WORKTREE" &gt;/dev/null 2&gt;&amp;1 || true; rmdir "$RELEASE_ROOT" &gt;/dev/null 2&gt;&amp;1 || true'\'' EXIT; git -C /Users/stevenchandler/Desktop/dev/quiver worktree add --detach "$RELEASE_WORKTREE" "$DEPLOYED_PROD_SHA"; test "$(git -C "$RELEASE_WORKTREE" rev-parse HEAD)" = "$DEPLOYED_PROD_SHA"; test -z "$(git -C "$RELEASE_WORKTREE" status --porcelain)"; cd "$RELEASE_WORKTREE"; yarn install --frozen-lockfile; test -z "$(git status --porcelain)"; yarn tsx scripts/trusted-forecast-production-smoke.ts --mode=forecast --beach-id="$AUTHORIZED_BEACH_ID" --local-date="$AUTHORIZED_LOCAL_DATE" --expected-build-key="$AUTHORIZED_BUILD_KEY" --confirm-production'</automated>
    <manual>Verify recorded worktree HEAD/status and Vercel deployment/project/team/production/READY/commit provenance, then exact beach, receipt counts, durable reuse, and privacy result.</manual>
  </verify>
  <acceptance_criteria>
    - Authorized main/previous-prod ancestry and expected tree resolve to `DEPLOYED_PROD_SHA`; exact Vercel deployment ID/project/team/production/READY/commit and clean detached-worktree HEAD/status match that SHA before runner invocation.
    - Exactly the authorized beach/local-date/build-key is processed; mismatch aborts before any write.
    - Matching durable receipt/counts authorize any adjusted output.
    - Ambiguous or mismatched durability returns retriable, never baseline.
  </acceptance_criteria>
  <done>The authorized controlled forecast write is durably attributed and privacy-safe.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 11: [BLOCKING] Authorize one exact production audit-write exercise</name>
  <files>none; authorization record only</files>
  <read_first>
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
    - scripts/trusted-forecast-production-smoke.ts
  </read_first>
  <action>
    Present one exact production beach ID/local day/build key, `DEPLOYED_PROD_SHA` with its authorized main/previous-prod ancestry and tree proof, immutable Vercel deployment ID, project ID/team ID, expected append/reuse counts, purpose of the replay/audit write, read-back queries, privacy limits, and rollback/kill switch. Request audit-write authorization only; do not bundle launchd/deploy approval or write here.
  </action>
  <verify>
    <automated>git rev-parse HEAD &amp;&amp; vercel project ls</automated>
    <manual>Confirm authorization names the exact SHA/environment/beach/local day/build key/count side effects.</manual>
  </verify>
  <acceptance_criteria>
    - Audit scope is one exact build exercise.
    - No audit write runs in this task.
  </acceptance_criteria>
  <done>The exact audit-write exercise is authorized or remains pending.</done>
  <resume-signal>Reply `authorize audit write: deployed prod [SHA], beach [ID], local day [date], build [key]` or deny.</resume-signal>
</task>

<task type="auto">
  <name>Task 12: Execute the authorized exact-target audit write and read back invariants</name>
  <files>.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
    - scripts/trusted-forecast-production-smoke.ts
    - supabase/migrations/20260727231500_create_trusted_external_forecast_adjustments.sql
  </read_first>
  <action>
    Abort on any mismatch with Task 11. Reverify the authorized main/previous-prod ancestry and expected tree of `DEPLOYED_PROD_SHA`, then create a temporary dedicated detached Quiver worktree at that exact SHA. In one guarded shell, require clean HEAD/status, query the exact authorized Vercel deployment ID, and compare project ID/team scope, production target, READY state, and extracted commit SHA before invoking the runner with exact beach/local-date/build-key in audit mode. Any provenance/target mismatch aborts before the runner/write. Then use read-only queries to verify receipt counts, original decision reuse, one-slot claims, append-only history, unchanged pre-existing `ml_predictions_log` bytes, and D-23 privacy. Ambiguity or unreadable receipt blocks retirement.
  </action>
  <verify>
    <automated>zsh -lc 'set -euo pipefail; : "${VERCEL_TOKEN:?}"; : "${AUTHORIZED_MAIN_SHA:?}"; : "${AUTHORIZED_PREVIOUS_PROD_SHA:?}"; : "${AUTHORIZED_EXPECTED_TREE:?}"; : "${DEPLOYED_PROD_SHA:?}"; : "${AUTHORIZED_VERCEL_PROJECT_ID:?}"; : "${AUTHORIZED_VERCEL_TEAM_ID:?}"; : "${AUTHORIZED_PRODUCTION_DEPLOYMENT_ID:?}"; : "${AUTHORIZED_BEACH_ID:?}"; : "${AUTHORIZED_LOCAL_DATE:?}"; : "${AUTHORIZED_BUILD_KEY:?}"; git -C /Users/stevenchandler/Desktop/dev/quiver merge-base --is-ancestor "$AUTHORIZED_MAIN_SHA" "$DEPLOYED_PROD_SHA"; git -C /Users/stevenchandler/Desktop/dev/quiver merge-base --is-ancestor "$AUTHORIZED_PREVIOUS_PROD_SHA" "$DEPLOYED_PROD_SHA"; test "$(git -C /Users/stevenchandler/Desktop/dev/quiver rev-parse "$DEPLOYED_PROD_SHA^{tree}")" = "$AUTHORIZED_EXPECTED_TREE"; DEPLOYMENT_JSON="$(curl -fsS -H "Authorization: Bearer $VERCEL_TOKEN" "https://api.vercel.com/v13/deployments/$AUTHORIZED_PRODUCTION_DEPLOYMENT_ID?teamId=$AUTHORIZED_VERCEL_TEAM_ID")"; test "$(jq -er .projectId &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "$AUTHORIZED_VERCEL_PROJECT_ID"; test "$(jq -er .target &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "production"; test "$(jq -er .readyState &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "READY"; test "$(jq -er ".gitSource.sha // .meta.githubCommitSha" &lt;&lt;&lt; "$DEPLOYMENT_JSON")" = "$DEPLOYED_PROD_SHA"; RELEASE_ROOT="$(mktemp -d /tmp/quiver-phase21-audit.XXXXXX)"; RELEASE_WORKTREE="$RELEASE_ROOT/worktree"; trap '\''git -C /Users/stevenchandler/Desktop/dev/quiver worktree remove --force "$RELEASE_WORKTREE" &gt;/dev/null 2&gt;&amp;1 || true; rmdir "$RELEASE_ROOT" &gt;/dev/null 2&gt;&amp;1 || true'\'' EXIT; git -C /Users/stevenchandler/Desktop/dev/quiver worktree add --detach "$RELEASE_WORKTREE" "$DEPLOYED_PROD_SHA"; test "$(git -C "$RELEASE_WORKTREE" rev-parse HEAD)" = "$DEPLOYED_PROD_SHA"; test -z "$(git -C "$RELEASE_WORKTREE" status --porcelain)"; cd "$RELEASE_WORKTREE"; yarn install --frozen-lockfile; test -z "$(git status --porcelain)"; yarn tsx scripts/trusted-forecast-production-smoke.ts --mode=audit --beach-id="$AUTHORIZED_BEACH_ID" --local-date="$AUTHORIZED_LOCAL_DATE" --expected-build-key="$AUTHORIZED_BUILD_KEY" --confirm-production'</automated>
    <manual>Verify recorded worktree/Vercel provenance, match exact authorized build key/beach/day, and record receipt, decision/application/snapshot, first-write, and privacy evidence.</manual>
  </verify>
  <acceptance_criteria>
    - Authorized main/previous-prod ancestry and expected tree resolve to `DEPLOYED_PROD_SHA`; exact Vercel deployment ID/project/team/production/READY/commit and clean detached-worktree HEAD/status match it before runner invocation.
    - Audit write matches the authorized beach/local-date/build-key exactly; mismatch causes zero writes.
    - All durable and privacy invariants pass before retirement authorization.
  </acceptance_criteria>
  <done>The exact audit write and read-back prove production durability, reuse, first-write immutability, and privacy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 13: [BLOCKING] Authorize exact local launchd retirement</name>
  <files>none; authorization record only</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/surf-forecast-ingestion/README.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Only after every production parity/audit gate passes, present the exact service `gui/501/com.quiver.surf-forecast-ingest`, plist path, current loaded state, recoverable archive destination, preserved runtime/snapshot paths, removal commands, side effects, and reload rollback. Request launchd retirement authorization only; do not unload or move files here.
  </action>
  <verify>
    <automated>launchctl print gui/501/com.quiver.surf-forecast-ingest &amp;&amp; shasum -a 256 /Users/stevenchandler/Library/LaunchAgents/com.quiver.surf-forecast-ingest.plist</automated>
    <manual>Confirm authorization names the exact service, plist checksum/archive target, preserved snapshots, and rollback.</manual>
  </verify>
  <acceptance_criteria>
    - Retirement is separately authorized after complete parity/audit.
    - Runtime data and snapshots remain out of deletion scope.
  </acceptance_criteria>
  <done>Exact recoverable launchd retirement is authorized or remains pending.</done>
  <resume-signal>Reply `authorize launchd retirement: gui/501/com.quiver.surf-forecast-ingest, plist sha256 [hash], archive [path]` or deny.</resume-signal>
</task>

<task type="auto">
  <name>Task 14: Retire only the authorized launchd service recoverably</name>
  <files>none; exact launchd service and plist archive target only</files>
  <read_first>
    - /Users/stevenchandler/Desktop/dev/surf-forecast-ingestion/README.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md
  </read_first>
  <action>
    Recompute service/plist checksum/archive target and abort on any mismatch with Task 13. Run `launchctl bootout gui/501/com.quiver.surf-forecast-ingest`, move only the exact plist to the approved recoverable target, verify `launchctl print` fails for the service, and verify no later local snapshot appears. Preserve the local runtime, logs, and all historical snapshots. Record the exact reload command as rollback.
  </action>
  <verify>
    <automated>if launchctl print gui/501/com.quiver.surf-forecast-ingest; then exit 1; fi</automated>
    <manual>Verify the approved archive contains the exact plist and historical runtime/snapshots remain intact.</manual>
  </verify>
  <acceptance_criteria>
    - Only the exact authorized service/plist changes.
    - Retirement is verified and recoverable.
    - No local ingestion data is deleted.
  </acceptance_criteria>
  <done>The local scraper is unloaded and its plist archived recoverably after proven production parity.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | ID | Plan | Status | Notes |
|--------|----|------|--------|-------|
| GOAL | Phase 21 outcome | 21-01 through 21-05 | COVERED | Production ingestion, bounded adjustment, atomic audit, privacy, and ordered rollout are all planned. |
| REQ | MFA-01 | 21-01 | COVERED | Fixed 17-source six-hour ingestion, parsers, retry/redirect, and aggregate health. |
| REQ | MFA-02 | 21-01, 21-02 | COVERED | Complete normalized issue contract plus immutable database revisions. |
| REQ | MFA-03 | 21-01, 21-03 | COVERED | Provider lineage/evidence assignment and vote dedupe. |
| REQ | MFA-04 | 21-03 | COVERED | Coverage precedence, exposure compatibility, and >1.00-ft block. |
| REQ | MFA-05 | 21-03, 21-04 | COVERED | Exact signed bands, cap, horizon, and builder integration. |
| REQ | MFA-06 | 21-02, 21-04 | COVERED | Atomic RPC, receipt reconciliation, and first-write history. |
| REQ | MFA-07 | 21-02, 21-04 | COVERED | Database privileges and public serialization/analytics privacy. |
| REQ | MFA-08 | 21-05 | COVERED | Focused/full/live gates, approvals, parity, and launchd retirement. |
| CONTEXT | D-01 through D-04 | 21-01 | COVERED | Inventory, source parsers, rejection, six-hour partial success/health. |
| CONTEXT | D-05 through D-08 | 21-01, 21-03 | COVERED | Lineage and evidence classes assigned and enforced. |
| CONTEXT | D-09 through D-13 | 21-03, 21-02 | COVERED | Precedence, single authority, conflict, exposure, decision/slot uniqueness. |
| CONTEXT | D-14 through D-17 | 21-03, 21-04 | COVERED | Local-day baseline, exact bands, horizon, feedback suppression. |
| CONTEXT | D-18 through D-22 | 21-02, 21-04 | COVERED | Append-only/first-write, atomic RPC, DB hash/counts, ambiguity, acknowledgement. |
| CONTEXT | D-23 through D-26 | 21-04, 21-05 | COVERED | Privacy, flags, rollout order, and explicit approvals. |
| RESEARCH | Hardened fetch/parser architecture | 21-01 | COVERED | Existing stack only; manual redirects, bounded retry, fixtures, IANA local days. |
| RESEARCH | Database-owned receipt protocol | 21-02, 21-04 | COVERED | Strict canonical RPC plus client ambiguity reconciliation. |
| RESEARCH | Validation architecture and runtime state | 21-05 | COVERED | Jest/pytest/pgTAP/full gates, live verifier, flags, and launchd target. |

Deferred ideas and sources beyond the fixed 17-source inventory are excluded per 21-CONTEXT.md and are not gaps.

</source_coverage_audit>

<validation_alignment>

`21-VALIDATION.md` is synchronized to this final task map and exact command set:

| Wave | Plan/task coverage |
|------|--------------------|
| 0 | 21-01 Task 0 isolated Python 3.11/uv preflight; 21-02 Task 0 Docker start/readiness plus local reset/pgTAP preflight |
| 1 | 21-01 Tasks 1-2 source/parser contract and ingestion scheduler; 21-02 Tasks 1-4 immutable schema/RPC plus separate exact-set schema authorization and guarded execution |
| 2 | 21-03 Tasks 1-3 policy contract, cross-language fixture conformance, and exact adjustment engine |
| 3 | 21-04 Tasks 1-2 persistence/server-only reads plus receipt-backed builder, first-write privacy, and changelog |
| 4 | 21-05 Tasks 1-14 verification tooling/evidence, ongoing ingestion-write authorization before separate Seaside deploy authorization, clean authorized-SHA Seaside deployment, Vercel-provenance-guarded exact-target forecast/audit execution from clean Quiver worktrees, and recoverable launchd retirement |

Validation evidence must cite these plan/task identifiers and exact commands; stale task references are blockers.

</validation_alignment>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live third-party endpoints -> verifier | Mutable external markup is inspected without writes or content leakage. |
| Verified local artifacts -> production services/data | Deploys and write exercises cross explicit approval gates; normal rollout does not mutate env flags. |
| Production parity -> local launchd state | A recoverable local scheduler mutation is allowed only after complete parity/audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T21-01 | Spoofing | Live verifier | mitigate | Reuse fixed HTTPS source contract and parser policy; deterministic 17-key results. |
| T21-07 | Information Disclosure | Verifier/verification record | mitigate | Sanitized fields only; negative output tests; no narratives, URLs with queries, hashes, or IDs. |
| T21-08 | Tampering / Elevation | Production rollout and launchd | mitigate | Ordered independent approvals, exact targets/commands, read-only checks between mutations, recoverable retirement. |
| T21-15 | Repudiation | Verification evidence | mitigate | Exact commands/results, approval text, timestamps, target IDs, counts, and blockers in 21-VERIFICATION.md. |
| T21-SC | Tampering | Tooling dependencies | mitigate | Change no manifests; detached Quiver worktrees use only `yarn install --frozen-lockfile`, Seaside uses exact isolated uv Python 3.11 plus pinned pytest, and existing Supabase CLI/pgTAP/Docker remain fixed. |
</threat_model>

<verification>

```bash
# Quiver
source ~/.nvm/nvm.sh
nvm use 22
yarn typecheck
yarn typecheck:forecast-gate
NODE_OPTIONS="--max-old-space-size=8192" yarn lint
yarn test:unit --runInBand --runTestsByPath scripts/__tests__/trusted-forecast-production-smoke.test.ts
yarn test:unit --bail=0
VERCEL_ENV=preview yarn build
open /Applications/Docker.app
for attempt in {1..30}; do docker info >/dev/null 2>&1 && break; sleep 2; done
docker info >/dev/null
yarn db:reset
supabase test db

# Seaside
cd /Users/stevenchandler/Desktop/dev/seaside
PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 \
  --with-requirements requirements.txt --with pytest==8.3.5 \
  python -m pytest -p no:cacheprovider \
  tests/ -v --tb=short
PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/uv run --isolated --python 3.11 \
  --with-requirements requirements.txt --with pytest==8.3.5 python \
  scripts/verify_trusted_forecast_ingestion.py --live --no-write
```

Production deploy/write/launchd commands are excluded from the broader automatic gate and remain blocking authorization tasks. Normal default-on rollout has no env mutation; any future env/secret/enable/disable change is outside this plan and requires fresh authorization.
</verification>

<success_criteria>

- MFA-01 through MFA-08 have exact local evidence and all 17 live parser contracts pass before rollout.
- D-23 privacy holds across database roles, DTOs, analytics, logs, verifier output, and verification artifacts.
- D-24 default-on/explicit-false controls are independently verified.
- D-25 rollout order is followed without skipping a gate.
- D-26 approval evidence precedes every production deploy/write and exact launchd removal; no env mutation occurs in normal rollout.
- No UI work, new provider, new dependency, model training, or public attribution is introduced.

</success_criteria>

<output>
Create `.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-05-SUMMARY.md` and finalize `.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VERIFICATION.md` when done.
</output>
