---
phase: 21-multi-forecaster-forecast-adjustment-and-production-ingestio
plan: 21-04
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - CHANGELOG.md
  - lib/services/forecast/trusted-forecast-repository.ts
  - lib/services/forecast/trusted-forecast-persistence.ts
  - lib/services/forecast/forecast-builder.ts
  - lib/services/forecast/log-display-prediction.ts
  - lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts
  - lib/services/forecast/__tests__/forecast-builder.height-offset.test.ts
  - lib/services/forecast/__tests__/log-display-prediction.test.ts
autonomous: true
requirements:
  - MFA-05
  - MFA-06
  - MFA-07
must_haves:
  truths:
    - "The builder compares trusted local-day ranges only after base face transform, handoff blend, and beach offset (D-14)."
    - "Trusted decisions use only raw 0-168-hour snapshot-eligible slots and suppress session-feedback adjustment on claimed slots (D-16, D-17)."
    - "One RPC persists decisions, applications, alerts, missing first-write snapshots, and a matching receipt before adjusted output can escape (D-18, D-19, D-20)."
    - "Definite rejection may serve baseline; transport ambiguity requires a matching durable receipt or throws a retriable generation error (D-21)."
    - "Eligible issue and durable daily-decision reads are server-only; repeated builds reuse the original decision, and a decision uniqueness race can never degrade to baseline."
    - "Private forecaster and internal audit fields never enter public forecast DTOs, UI payloads, analytics, or public logs (D-23)."
    - "Serving defaults enabled while explicit false remains independent of ingestion (D-24)."
  artifacts:
    - path: "lib/services/forecast/trusted-forecast-persistence.ts"
      provides: "Server-only strict payload/receipt schemas and ambiguity reconciliation."
    - path: "lib/services/forecast/trusted-forecast-repository.ts"
      provides: "Server-only eligible-issue and existing daily-decision reads."
    - path: "lib/services/forecast/forecast-builder.ts"
      provides: "Ordered trusted decision, persistence, feedback suppression, and receipt-gated serving."
    - path: "lib/services/forecast/log-display-prediction.ts"
      provides: "Unmodified legacy first-write snapshot path with no trusted sidecar mutation."
    - path: "lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts"
      provides: "Transport and receipt-state matrix."
  key_links:
    - from: "lib/services/forecast/forecast-builder.ts"
      to: "lib/services/forecast/trusted-forecast-repository.ts"
      via: "Load validated eligible issues and existing durable local-day decisions before computation."
      pattern: "loadEligibleTrustedForecastIssues|loadTrustedForecastDecisions"
    - from: "lib/services/forecast/forecast-builder.ts"
      to: "lib/services/forecast/trusted-forecast-persistence.ts"
      via: "Complete build payload committed before applying public wave_height deltas."
      pattern: "persistTrustedForecastBuild"
    - from: "lib/services/forecast/trusted-forecast-persistence.ts"
      to: "public.persist_trusted_forecast_build"
      via: "Service-role RPC plus receipt lookup after ambiguous transport."
      pattern: "persist_trusted_forecast_build"
---

# 21-04: Forecast Builder Integration and Privacy

<objective>
Integrate trusted decisions into the active forecast builder with atomic receipt-gated persistence and a strict private/public boundary.

Purpose: Ensure bounded adjustments occur at the approved layer, never stack feedback, never mutate first-write history, and never escape without durable attribution, implementing D-14 through D-24.
Output: Server-only persistence client, corrected builder ordering, preserved snapshot writer, independent default-on serving flag, and focused Jest coverage.
</objective>

<execution_context>
@/Users/stevenchandler/.codex/get-shit-done/workflows/execute-plan.md
@/Users/stevenchandler/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@docs/README.md
@lib/services/ARCHITECTURE.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-CONTEXT.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-RESEARCH.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-PATTERNS.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
@.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build server-only persistence, reconciliation, and durable reads</name>
  <files>lib/services/forecast/trusted-forecast-persistence.ts, lib/services/forecast/trusted-forecast-repository.ts, lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts</files>
  <read_first>
    - lib/recommendations/major-event-hold/control.ts
    - CLAUDE.md
    - docs/README.md
    - __tests__/lib/recommendations/major-event-hold/control.test.ts
    - lib/services/forecast/trusted-forecast-policy.ts
    - supabase/migrations/20260727231500_create_trusted_external_forecast_adjustments.sql
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-02-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-03-SUMMARY.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
  </read_first>
  <behavior>
    - "Matching RPC receipt permits adjusted output."
    - "A structured definite SQL rejection permits baseline output."
    - "Timeout/reset/abort/missing response/uncertain exception triggers receipt lookup; matching hash and exact counts permits adjusted output."
    - "Missing, mismatched, or unreadable receipt after ambiguity throws a typed retriable forecast-generation error (D-21)."
    - "Decision/application uniqueness collisions are reconciliation-required, not definite baseline-safe rejections."
    - "All returned receipt and payload data is strictly validated in a server-only module."
    - "Only eligible `trusted_forecast_issues` rows and exact existing `(beach_id, local_date)` decisions are returned through strict validation."
    - "Malformed/unreadable private rows fail with typed retriable errors and never masquerade as no evidence."
  </behavior>
  <action>
    Write the complete transport/read-state matrix first. Create `server-only` persistence and repository modules with strict Zod schemas, injectable store interfaces, service-role RPC invocation, receipt lookup by build key, eligible issue reads, and exact durable daily-decision reads. Repository projections are explicit, paginated where needed, parsed through the policy input schema, and private. Classify only known structured errors unrelated to uniqueness as definite rejection. Treat transport uncertainty and daily-decision/application uniqueness errors as reconciliation-required: reload matching durable decision/receipt state and continue only when hash, decision identity, and every exact count match; otherwise throw retriable. A query failure or malformed row must never become an empty evidence result. Never trust a client hash, expose private repository types through client/public surfaces, or collapse ambiguity/read failure/uniqueness races into baseline success.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts lib/services/forecast/__tests__/trusted-forecast-contract.test.ts</automated>
    <automated>npx eslint --max-warnings=0 lib/services/forecast/trusted-forecast-persistence.ts lib/services/forecast/trusted-forecast-repository.ts lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The eight-state transport matrix in 21-VALIDATION.md is table-driven.
    - Only returned/matched receipts authorize adjusted output.
    - Definite rejection is distinguishable from a retriable ambiguous failure.
    - Daily-decision/application uniqueness errors never produce a baseline-safe outcome.
    - Tests prove explicit issue/decision projections, eligible filtering, exact local-date lookup, strict parsing, and retriable read failure.
    - Empty results mean successful reads with no matching rows, never failed or unreadable queries.
    - Error/log values contain build status/counts only, not source ranges, URLs, hashes, evidence, or internal IDs.
  </acceptance_criteria>
  <done>Server-only persistence and repository tests cover receipt reconciliation, eligible reads, durable reuse, and fail-closed ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate receipt-backed serving, first-write privacy, and changelog</name>
  <files>CHANGELOG.md, lib/services/forecast/forecast-builder.ts, lib/services/forecast/log-display-prediction.ts, lib/services/forecast/__tests__/forecast-builder.height-offset.test.ts, lib/services/forecast/__tests__/log-display-prediction.test.ts</files>
  <read_first>
    - CLAUDE.md
    - docs/README.md
    - lib/services/forecast/forecast-builder.ts
    - lib/services/forecast/__tests__/forecast-builder.height-offset.test.ts
    - lib/services/forecast/trusted-forecast-policy.ts
    - lib/services/forecast/trusted-forecast-adjustment.ts
    - lib/services/forecast/trusted-forecast-persistence.ts
    - lib/services/forecast/trusted-forecast-repository.ts
    - lib/services/forecast/log-display-prediction.ts
    - lib/services/forecast/__tests__/log-display-prediction.test.ts
    - CHANGELOG.md
    - .planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-VALIDATION.md
  </read_first>
  <behavior>
    - "Baseline is base face transform -> handoff blend -> beach offset before trusted comparison (D-14)."
    - "Raw horizons exactly 0 and 168 may be claimed; negative or greater than 168 cannot be claimed (D-16)."
    - "A trusted-applied slot suppresses session feedback; an unclaimed/no-op slot preserves existing feedback behavior (D-17)."
    - "Forecast wave_height is not mutated until the complete build receipt is returned/reconciled (D-19 through D-21)."
    - "A second build loads and reuses the first durable local-day decision even when newer issues exist."
    - "A decision uniqueness race is reconciled to the matching durable decision/receipt or throws retriable; it never serves baseline."
    - "TRUSTED_FORECAST_ADJUSTMENTS_ENABLED defaults on and explicit false disables serving independently (D-24)."
    - "The legacy snapshot writer performs no trusted UPDATE and private ranges/narratives/URLs/attribution/hashes/metadata/evidence/internal IDs never enter public DTOs, analytics, or logs (D-18, D-23)."
  </behavior>
  <action>
    Expand integration/privacy tests before production edits. Preserve base face transform, handoff blend, and beach offset order. Load eligible issues and all existing durable decisions through the repository; existing decisions always win over later guidance. Collect raw 0-168-hour slots, suppress feedback only for claimed slots, build one persistence payload, and await a matching receipt before public mutation. A known structured rejection unrelated to uniqueness may return byte-identical baseline; any uniqueness collision or transport ambiguity must reconcile identically or throw retriable, never baseline. Keep serving server-side, default-on, explicit-false, and independent of ingestion; record the final flag contract in `21-VERIFICATION.md`, not a deployed env mutation. Reverse the test that permits trusted sidecar UPDATE, remove `syncTrustedForecastApplications` and trusted mutation fields while preserving unrelated first-write telemetry, and use sentinel negative tests for every D-23 category plus the reviewed negative source scan. Append one concise `[Unreleased]` changelog bullet for local private ingestion/receipt-backed adjustment/privacy, preserving user edits and making no production claim.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh &amp;&amp; nvm use 22 &amp;&amp; yarn test:unit --runInBand --runTestsByPath lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts lib/services/forecast/__tests__/forecast-builder.height-offset.test.ts lib/services/forecast/__tests__/log-display-prediction.test.ts</automated>
    <automated>if rg -n "provider_lineage|source_hash|parser_version|evidence_ids|trusted_forecast_(issue|decision|application|receipt)_id" app components hooks lib --glob '!**/*.test.*' --glob '!**/__tests__/**' --glob '!lib/services/forecast/trusted-forecast-policy.ts' --glob '!lib/services/forecast/trusted-forecast-adjustment.ts' --glob '!lib/services/forecast/trusted-forecast-persistence.ts' --glob '!lib/services/forecast/trusted-forecast-repository.ts' --glob '!lib/services/forecast/forecast-builder.ts' --glob '!lib/services/forecast/log-display-prediction.ts'; then exit 1; fi</automated>
    <automated>rg -n "^## \\[Unreleased\\]|trusted|forecaster|receipt|privacy" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove builder order, both horizon boundaries, feedback suppression/non-suppression, definite rejection baseline, matching receipt adjusted output, and unresolved ambiguity rejection.
    - Repeated-build tests introduce newer issue guidance and prove the original durable decision/range/delta is reused end-to-end.
    - Uniqueness-race tests prove matching reconciliation continues with attributed output and mismatched/unreadable state throws retriable rather than baseline.
    - Adjusted forecasts cannot be observed before persistence resolves.
    - Existing beach-offset and feedback behavior remains unchanged outside trusted-applied slots.
    - No route, component, public UI field, confidence label, or analytics event is added.
    - Tests fail if any trusted UPDATE targets `ml_predictions_log`; existing first-write behavior remains green.
    - Public negative tests cover every D-23 class and the reviewed source scan has no unapproved client/public/analytics match.
    - One preserved `[Unreleased]` entry describes local implementation without claiming rollout.
  </acceptance_criteria>
  <done>The builder serves only receipt-backed output while first-write, privacy, flag, feedback, and changelog contracts remain correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forecast builder -> private persistence RPC | Adjusted values may escape only after complete durable proof. |
| Private decision state -> public forecast DTO/analytics/logs | Only the final height effect may cross; evidence and internal IDs stay server-private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T21-06 | Repudiation / Tampering | Persistence state machine | mitigate | Matching receipt and exact counts required; uncertainty is ambiguous; typed retriable failure blocks output. |
| T21-13 | Tampering | Builder layer ordering | mitigate | Focused tests lock base -> handoff -> beach offset -> trusted -> conditional feedback order. |
| T21-14 | Tampering | First-write snapshots | mitigate | Remove trusted UPDATE path; preserve ignore-duplicates; integration and pgTAP tests prove immutability. |
| T21-07 | Information Disclosure | DTOs, analytics, logs | mitigate | Server-only module, public allowlist behavior, sentinel negative tests, scoped source scan. |
| T21-SC | Tampering | Dependencies | mitigate | No packages installed; use existing Zod, Supabase client, Jest, and server-only facilities. |
</threat_model>

<verification>

```bash
source ~/.nvm/nvm.sh
nvm use 22
yarn test:unit --runInBand --runTestsByPath \
  lib/services/forecast/__tests__/trusted-forecast-adjustment.test.ts \
  lib/services/forecast/__tests__/trusted-forecast-persistence.test.ts \
  lib/services/forecast/__tests__/forecast-builder.height-offset.test.ts \
  lib/services/forecast/__tests__/log-display-prediction.test.ts \
  __tests__/migrations/trusted-external-forecast-adjustments.test.ts
npx eslint --max-warnings=0 \
  lib/services/forecast/trusted-forecast-policy.ts \
  lib/services/forecast/trusted-forecast-adjustment.ts \
  lib/services/forecast/trusted-forecast-repository.ts \
  lib/services/forecast/trusted-forecast-persistence.ts \
  lib/services/forecast/forecast-builder.ts \
  lib/services/forecast/log-display-prediction.ts
```

</verification>

<success_criteria>

- MFA-05 builder integration preserves the exact bounded decision contract.
- MFA-06 adjusted output is receipt-gated and ambiguous commits fail retriably.
- MFA-07 private source and audit data is absent from public/API/UI/analytics payloads.
- D-14 through D-24 are covered by focused unit/integration tests.
- No UI work, dependency install, production deploy, production write, or env mutation occurs.

</success_criteria>

<output>
Create `.planning/phases/21-multi-forecaster-forecast-adjustment-and-production-ingestio/21-04-SUMMARY.md` when done.
</output>
