# Plan 04-04: Metadata, Sitemap, And Landing Integration

## Goal

Make the pricing surface discoverable and connect it to the landing page without weakening the primary iOS CTA.

## Scope

- Add `/pricing` metadata through `buildPageMetadata`.
- Add conservative WebPage schema without Offer/Product purchase schema.
- Include `/pricing` in the flat sitemap.
- Add a landing-page teaser near the final CTA.
- Document the new pricing component rules.

## Verification

- Unit tests cover metadata/schema output and sitemap inclusion.
- E2E covers the landing teaser link.
