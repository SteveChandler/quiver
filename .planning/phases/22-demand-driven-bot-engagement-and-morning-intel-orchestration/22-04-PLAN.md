---
phase: 22
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified:
  - scripts/bot-engagement-report.ts
  - __tests__/scripts/bot-engagement-report.test.ts
  - docs/features/NPC_INTEL_BOTS.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operators can compare clean web demand, bot supply, target coverage, feedback, and downstream session intent for a fixed window."
    - "Rollout is staged and reversible, with dry-run/shadow/live controls and explicit production approval gates."
    - "Documentation describes quality and trust outcomes rather than rewarding post volume."
  artifacts:
    - path: "scripts/bot-engagement-report.ts"
      provides: "Read-only machine-readable and human-readable supply/demand report"
    - path: "docs/features/NPC_INTEL_BOTS.md"
      provides: "Current demand-driven bot policy and operator runbook"
  key_links:
    - from: "scripts/bot-engagement-report.ts"
      to: "user_events, intel_posts, beach_reviews, bot_engagement_assignments"
      via: "same clean filters and aggregate joins as the production ranker"
    - from: "docs/features/NPC_INTEL_BOTS.md"
      to: "NPC_ENGAGEMENT_MODE"
      via: "documented dry-run, shadow, live, and rollback controls"
---

<objective>
Make the demand-driven system measurable and safely operable, with a repeatable report that proves whether bot supply is reaching real web demand and whether people find it useful.
</objective>

<tasks>

<task type="execute">
<name>Build a read-only supply/demand and quality report</name>
<files>scripts/bot-engagement-report.ts</files>
<read_first>
- `scripts/session-acquisition-funnel-report.ts`
- `lib/npc/demand-ranking.ts`
- `supabase/migrations/20260803120000_create_bot_engagement_audit.sql`
- `AGENTS.md`
- `docs/features/NPC_INTEL_BOTS.md`
</read_first>
<action>Implement a service-role read-only CLI that accepts an explicit rolling window and optional `--mode`/`--format` flags, applies the same clean-demand exclusions as the ranker, and reports total bot posts/reviews, active supply by beach, clean views/sessions by beach, top demand gaps, top oversupply, target coverage, demand-vs-exploration share, suppression reasons, source-attribution completeness, confirmations/helpful feedback, and downstream `plan_session_from_intel`/session-log intent. Never insert, update, delete, or mutate production data; output JSON and human-readable markdown with the window and filter definition.</action>
<verify>Run the report against a fixture client and a local/preview read-only environment; inspect that it cannot call `insert`, `update`, `upsert`, `delete`, or `rpc` mutation paths.</verify>
<acceptance_criteria>
- The report can reproduce the prior 30-day comparison: bot supply, clean demand, top uncovered demand, and oversupply concentration.
- The report explicitly distinguishes zero feedback from unavailable instrumentation.
- The report emits a stable machine-readable shape suitable for a later dashboard without exposing user identifiers or raw event metadata.
- The CLI exits nonzero for invalid dates or missing read-only configuration and never falls back to mutation credentials.
</acceptance_criteria>
<done>Report fixtures prove clean filtering, read-only behavior, stable output, and invalid-input failures.</done>
</task>

<task type="execute">
<name>Test report filters and operating metrics</name>
<files>__tests__/scripts/bot-engagement-report.test.ts</files>
<read_first>
- `__tests__/scripts/beach-photo-review.test.ts`
- `scripts/bot-engagement-report.ts`
- `lib/analytics/event-taxonomy.ts`
- `supabase/migrations/20260313210000_fix_p0_bot_flagged_and_auth_events.sql`
</read_first>
<action>Fixture bot posts, reviews, clean/flagged/mock/system beach views, feedback rows, and downstream intent. Assert exact date-window normalization, exclusion rules, top-gap/oversupply ordering, target coverage, demand/exploration percentages, and zero-vs-unavailable feedback reporting. Add a source assertion that the script has no mutation method calls.</action>
<verify>Run `yarn test:unit --runInBand __tests__/scripts/bot-engagement-report.test.ts`.</verify>
<acceptance_criteria>
- Tests fail if any bot/mock/system view is counted as human demand.
- Tests fail if active supply is joined to the wrong beach or deleted reviews are counted.
- Tests fail if the report calls a mutating Supabase method.</acceptance_criteria>
<done>The focused report test command exits 0.</done>
</task>

<task type="execute">
<name>Document AAA operating rules and rollout gates</name>
<files>docs/features/NPC_INTEL_BOTS.md, docs/ARCHITECTURE.md</files>
<read_first>
- `docs/features/NPC_INTEL_BOTS.md`
- `docs/ARCHITECTURE.md`
- `22-RESEARCH.md`
- `22-01-PLAN.md`
- `22-02-PLAN.md`
- `22-03-PLAN.md`
</read_first>
<action>Update the NPC feature and architecture documentation to describe the demand-gap ranking, 85–90% demand/10–15% exploration policy, home-region-as-context rule, Morning Intel vs Quiver identity boundary, derived-content attribution, default review prohibition, dry-run/shadow/live modes, audit tables, and the staged rollout gates. Include the exact report command, the 3-day/7-day/28-day review cadence, and rollback/kill-switch conditions: oversupply concentration worsens, attribution is incomplete, confirmations/helpful or downstream intent is absent after instrumentation is verified, or cron errors exceed the agreed threshold.</action>
<verify>Search both docs for the policy constants, identity boundary, report command, rollout modes, and rollback conditions; run markdown/source formatting checks used by the repository if available.</verify>
<acceptance_criteria>
- The docs no longer describe home/secondary percentages as the live policy.
- The docs clearly state that bots are derived/forecast content, not eyewitness community reports.
- Operators can find the dry-run, shadow, live, report, and rollback procedure without relying on conversation history.</acceptance_criteria>
<done>Documentation assertions find the policy constants, identity boundary, operator command, and rollback gates.</done>
</task>

<task type="execute">
<name>Run the final release-readiness checks</name>
<files>scripts/bot-engagement-report.ts, docs/features/NPC_INTEL_BOTS.md, docs/ARCHITECTURE.md</files>
<read_first>
- `AGENTS.md`
- `.planning/phases/22-demand-driven-bot-engagement-and-morning-intel-orchestration/22-VALIDATION.md`
- all `22-*-PLAN.md` files
</read_first>
<action>Run the focused phase test set, full typecheck, scoped ESLint on every changed TypeScript/route file, and preview build because `vercel.json` and cron routes changed. Run the read-only report in dry-run against the selected verification window, save the summary as execution evidence, and record any production schema push/deploy/cron activation as approval-gated rather than claiming completion.</action>
<verify>Commands exit 0 except explicitly approval-gated production actions, which must be reported as not run. Review `git diff --check` and the final diff for unrelated changes and accidental secrets.</verify>
<acceptance_criteria>
- Focused tests, `yarn typecheck`, scoped lint, and preview build pass before phase closeout.
- The report output includes demand coverage, oversupply, quality feedback, and downstream intent for a fixed window.
- No production deploy, schema push, cron activation, or content mutation is claimed without a recorded successful command and approval.</acceptance_criteria>
<done>All local gates pass and any production approval-gated actions are explicitly reported.</done>
</task>

</tasks>

<verification>
- `yarn test:unit --runInBand __tests__/lib/npc/demand-ranking.test.ts __tests__/lib/npc/npc-selection.test.ts __tests__/lib/npc/morning-intel-orchestrator.test.ts __tests__/app/api/cron/npc-activity.test.ts __tests__/app/api/cron/morning-intel-bot.test.ts __tests__/app/api/cron/morning-forecast-bot.test.ts __tests__/scripts/bot-engagement-report.test.ts`
- `yarn test:unit --bail=0`
- `yarn typecheck`
- `npx eslint --max-warnings=0` on all touched TypeScript files
- `VERCEL_ENV=preview yarn build`
- `git diff --check`
</verification>

<success_criteria>
- Operators can measure whether bot supply follows clean web demand and whether it produces downstream value.
- Rollout is staged, reversible, and evidence-driven.
- Documentation and verification make AAA engagement a quality/feedback objective rather than a post-count objective.
</success_criteria>

<threat_model>
- T-22-10: the system optimizes for post volume instead of usefulness; mitigation is the read-only report and quality/downstream-intent gates.
- T-22-11: an operator enables production writes without evidence; mitigation is dry-run/shadow modes, explicit approval gates, and documented rollback conditions.
- T-22-12: documentation drifts from implementation; mitigation is source assertions, exact commands, and final diff review.
</threat_model>
