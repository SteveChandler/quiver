---
phase: 22
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - lib/npc/morning-intel-orchestrator.ts
  - app/api/cron/morning-intel-bot/route.ts
  - vercel.json
  - __tests__/lib/npc/morning-intel-orchestrator.test.ts
  - __tests__/app/api/cron/morning-intel-bot.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Morning Intel Bot publishes at most 3–5 concise demand-ranked briefings with derived-source attribution."
    - "Quiver Surf Forecast remains a separate structured regional forecast publisher."
    - "Morning Intel cron retries are idempotent and can be disabled or shadowed without content writes."
  artifacts:
    - path: "lib/npc/morning-intel-orchestrator.ts"
      provides: "Bounded demand-ranked editorial briefing generation"
    - path: "app/api/cron/morning-intel-bot/route.ts"
      provides: "Authenticated Morning Intel system-account cron route"
  key_links:
    - from: "app/api/cron/morning-intel-bot/route.ts"
      to: "Morning Intel Bot profile"
      via: "stable id plus system-account identity check"
    - from: "app/api/cron/morning-intel-bot/route.ts"
      to: "app/api/cron/morning-forecast-bot/route.ts"
      via: "separate profile and unchanged forecast route contract"
---

<objective>
Bring Morning Intel Bot into the production engagement loop as a small, attributed editorial orchestrator while leaving Quiver Surf Forecast as the structured regional forecast publisher.
</objective>

<tasks>

<task type="execute">
<name>Extract the bounded Morning Intel editorial service</name>
<files>lib/npc/morning-intel-orchestrator.ts</files>
<read_first>
- `scripts/morningIntel.ts`
- `lib/npc/demand-ranking.ts`
- `lib/npc/template-hydration.ts`
- `lib/utils/intel-dedupe.ts`
- `types/morning-intel.ts`
- `lib/scoring/index.ts`
</read_first>
<action>Export typed functions that accept a demand target snapshot and produce at most 3–5 concise editorial briefings. Select high-intent/stale gaps, use existing forecast/conditions helpers for derived facts, include source kind, generated-at, expiry, confidence, and `is_eyewitness: false`, and build stable dedupe fingerprints. Apply one-post-per-beach-per-morning and material-change rules. Keep the service independent of HTTP and resolve the stable Morning Intel Bot identity in the route, not by reusing the Quiver Surf Forecast identity.</action>
<verify>Run the pure orchestrator tests with fixed time, target fixtures, missing forecast data, duplicate targets, and fewer-than-three eligible targets.</verify>
<acceptance_criteria>
- The service returns no more than five briefings and never more than one briefing per beach per morning.
- Selection is driven by demand score/staleness and does not hard-code Ocean Beach, California regions, or NPC home-region quotas.
- Every briefing is explicitly derived/forecast content and includes source, freshness, expiry, and confidence fields.
- Missing or stale source data produces a suppressed target with a reason rather than invented observation text.</acceptance_criteria>
<done>Pure orchestrator tests prove target bounds, attribution, dedupe, and missing-data suppression.</done>
</task>

<task type="execute">
<name>Add the Morning Intel cron route</name>
<files>app/api/cron/morning-intel-bot/route.ts</files>
<read_first>
- `app/api/cron/morning-forecast-bot/route.ts`
- `lib/npc/morning-intel-orchestrator.ts`
- `lib/npc/demand-ranking.ts`
- `scripts/morningIntel.ts`
- `__tests__/app/api/cron/morning-forecast-bot.test.ts`
- `AGENTS.md`
</read_first>
<action>Implement `GET /api/cron/morning-intel-bot` with `validateCronRequest`, service-role Supabase access, `withObservedCron`, and the repository response wrappers. Look up profile id `f2472229-100e-4a8a-ae6e-bc8b23d7cf87` with `is_system_account = true` and `full_name = 'Morning Intel Bot'`, load one demand snapshot, publish only the orchestrator’s 3–5 briefings in live mode, and write run/assignment audit rows in all modes. Preserve unauthorized/missing-profile/error isolation semantics and return additive counts for eligible targets, published briefings, suppressed targets, and follow-up opportunities. Do not modify the existing `morning-forecast-bot` route’s Quiver Surf Forecast lookup or three-region behavior.</action>
<verify>Run the route test and assert unauthorized requests return 401 before Supabase access, Morning Intel Bot is queried by stable identity, Quiver Surf Forecast is never queried, dry-run/shadow have no content inserts, and one target failure does not fail the whole run.</verify>
<acceptance_criteria>
- The new route can publish a bounded Morning Intel set without creating a Quiver Surf Forecast post.
- The existing `/api/cron/morning-forecast-bot` contract and identity remain unchanged.
- Live mode is idempotent by bot/beach/morning fingerprint and dry-run/shadow are mutation-free for content tables.
- Response payload exposes selected/published/suppressed counts and no raw user/event data.</acceptance_criteria>
<done>Route tests prove identity separation, mode safety, auth behavior, and partial-failure isolation.</done>
</task>

<task type="execute">
<name>Schedule the orchestrator without changing the forecast schedule</name>
<files>vercel.json</files>
<read_first>
- `vercel.json`
- `app/api/cron/morning-forecast-bot/route.ts`
- `app/api/cron/morning-intel-bot/route.ts`
- `AGENTS.md`
</read_first>
<action>Add one explicit Vercel cron entry for `/api/cron/morning-intel-bot` at the intended morning UTC schedule, documenting the PT/DST assumption in the plan or adjacent code comment. Leave `/api/cron/morning-forecast-bot` at its existing `30 13 * * *` schedule so the two system identities remain separate. Keep any rollout environment flag outside the client bundle.</action>
<verify>Parse `vercel.json` as JSON and assert both cron paths exist exactly once with their intended schedules; run the focused route tests.</verify>
<acceptance_criteria>
- `vercel.json` contains one new Morning Intel cron entry and the existing Quiver Surf Forecast entry remains present and unchanged.
- No cron path is duplicated and no schedule relies on a local-time string unsupported by Vercel.
- The route can be disabled by the server-side rollout flag without editing the schedule.</acceptance_criteria>
<done>Vercel JSON parsing and cron schedule assertions pass.</done>
</task>

<task type="execute">
<name>Cover Morning Intel orchestration and identity separation</name>
<files>__tests__/lib/npc/morning-intel-orchestrator.test.ts, __tests__/app/api/cron/morning-intel-bot.test.ts</files>
<read_first>
- `__tests__/app/api/cron/morning-forecast-bot.test.ts`
- `__tests__/lib/utils/morning-intel-recommendation.test.ts`
- `lib/npc/morning-intel-orchestrator.ts`
- `app/api/cron/morning-intel-bot/route.ts`
- `vercel.json`
</read_first>
<action>Test target bounds (3–5), stale/high-intent prioritization, one-per-beach dedupe, derived-source wording, missing data suppression, auth rejection, stable Morning Intel Bot lookup, no Quiver lookup, dry-run/shadow no-content writes, partial failures, and schedule JSON integrity.</action>
<verify>Run `yarn test:unit --runInBand __tests__/lib/npc/morning-intel-orchestrator.test.ts __tests__/app/api/cron/morning-intel-bot.test.ts`.</verify>
<acceptance_criteria>
- Tests fail if Morning Intel Bot resolves to the Quiver Surf Forecast profile.
- Tests fail if the route publishes more than five briefings or posts the same beach twice in one run.
- Tests fail if derived content is labeled as eyewitness or if missing source data is filled with fabricated conditions.</acceptance_criteria>
<done>The focused Morning Intel test command exits 0.</done>
</task>

</tasks>

<verification>
- `yarn test:unit --runInBand __tests__/lib/npc/morning-intel-orchestrator.test.ts __tests__/app/api/cron/morning-intel-bot.test.ts __tests__/app/api/cron/morning-forecast-bot.test.ts`
- `yarn typecheck`
- `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"`
- `VERCEL_ENV=preview yarn build` because cron/config behavior changed.
</verification>

<success_criteria>
- Morning Intel Bot publishes a bounded, demand-ranked, attributed morning briefing.
- Quiver Surf Forecast remains a separate structured forecast publisher.
- Morning Intel can be enabled, shadowed, or disabled without content identity collisions.
</success_criteria>

<threat_model>
- T-22-07: two system bots collide or identity is misattributed; mitigation is stable Morning Intel identity lookup and regression tests against the existing forecast route.
- T-22-08: morning content fabricates observations when forecast data is missing; mitigation is source gating, explicit derived metadata, and suppression tests.
- T-22-09: a cron retry creates duplicate briefings; mitigation is one-per-beach morning fingerprints and audit-backed idempotency.
</threat_model>
