---
phase: 22
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - lib/npc/npc-selection.ts
  - lib/npc/beach-selection.ts
  - app/api/cron/npc-activity/route.ts
  - lib/npc/template-hydration.ts
  - __tests__/lib/npc/npc-selection.test.ts
  - __tests__/app/api/cron/npc-activity.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hourly NPC activity follows global demand gaps; home region is used only for personality/context."
    - "Default bot content is capped, deduped, explicitly derived, and does not create synthetic reviews."
    - "Dry-run and shadow modes make no content-table mutations."
  artifacts:
    - path: "app/api/cron/npc-activity/route.ts"
      provides: "Demand-aware, mode-gated NPC publishing cron"
    - path: "lib/npc/npc-selection.ts"
      provides: "NPC eligibility and global ranked-target assignment"
  key_links:
    - from: "app/api/cron/npc-activity/route.ts"
      to: "lib/npc/demand-ranking.ts"
      via: "one demand snapshot per cron run"
    - from: "app/api/cron/npc-activity/route.ts"
      to: "intel_posts/beach_reviews"
      via: "mode, cap, fingerprint, and attribution guards before insert"
---

<objective>
Make hourly NPC activity follow the demand-gap ranking while preserving personality voice, adding trust and freshness guardrails, and materially reducing bot-generated reviews.
</objective>

<tasks>

<task type="execute">
<name>Replace regional allocation with demand-target assignment</name>
<files>lib/npc/npc-selection.ts, lib/npc/beach-selection.ts</files>
<read_first>
- `AGENTS.md`
- `lib/npc/npc-selection.ts`
- `lib/npc/beach-selection.ts`
- `lib/npc/posting-windows.ts`
- `.planning/phases/22-demand-driven-bot-engagement-and-morning-intel-orchestration/22-01-PLAN.md`
- `lib/npc/demand-ranking.ts`
</read_first>
<action>Keep `fetchEligibleNPCs` and posting-window/activity filtering intact, but change beach assignment to accept a globally ranked target pool from `selectDemandTargets`. Remove the 50%/35%/15% home/secondary/adventure allocation from the live path. Use `home_region` only to provide personality context and prefer a compatible voice when multiple NPCs can cover the same target. Preserve a bounded exploration fallback when demand data is empty and return an explicit suppression reason instead of silently selecting a home-region beach.</action>
<verify>Run the selection unit tests with fixtures containing multiple regions and assert assignment is driven by target score, not the NPC’s home-region slug list.</verify>
<acceptance_criteria>
- No live selection path uses a home-region percentage or `HOME_BEACH_COUNT` as a posting allocation.
- A high-gap beach outside an NPC’s home region can be selected when it is in the demand pool.
- Home region/personality remain available to template selection/context and never change the 85–90% demand-gap allocation.
- Empty or stale demand data produces a safe no-post result or explicitly bounded exploration, not an unbounded random fallback.
</acceptance_criteria>
<done>Selection tests prove demand-driven global assignment and context-only home region behavior.</done>
</task>

<task type="execute">
<name>Add content trust, cap, and material-change guards</name>
<files>app/api/cron/npc-activity/route.ts, lib/npc/template-hydration.ts</files>
<read_first>
- `app/api/cron/npc-activity/route.ts`
- `lib/npc/template-hydration.ts`
- `lib/utils/intel-dedupe.ts`
- `app/api/cron/morning-forecast-bot/route.ts`
- `supabase/migrations/20250817120001_create_intel_and_reviews_tables.sql`
- `22-RESEARCH.md`
</read_first>
<action>Before inserting, enforce a configurable per-beach rolling cap, a duplicate/material-change fingerprint, and a freshness check against active `intel_posts` and non-deleted `beach_reviews`. Set the default review weight to zero or a clearly bounded low ceiling, never create synthetic review ratings unless an explicit rollout flag enables them, and record a suppression reason in the audit result. Add structured derived-content metadata to intel posts where the current schema supports it: source kind, generated-at, expires-at, confidence, and `is_eyewitness: false`. Remove or rewrite random crowd/experience language that implies a bot personally observed conditions; keep personality language as tone only.</action>
<verify>Run focused cron tests and inspect the inserted payloads for `is_eyewitness: false`, expiry, dedupe/material-change behavior, cap suppression, and zero/default review writes.</verify>
<acceptance_criteria>
- Re-running the same hourly inputs does not create a second active post for the same bot/beach/content fingerprint.
- A beach at its cap is suppressed and counted in the response/audit summary without failing the entire cron run.
- Default configuration cannot create a bot review; enabling any review experiment requires an explicit environment flag and a separate cap.
- Stored bot intel does not claim first-person presence and includes generated/expiry/confidence metadata.
</acceptance_criteria>
<done>Focused cron tests prove cap, fingerprint, derived-source, and review guards.</done>
</task>

<task type="execute">
<name>Wire demand ranking, audit writes, and rollout modes into the cron</name>
<files>app/api/cron/npc-activity/route.ts</files>
<read_first>
- `app/api/cron/npc-activity/route.ts`
- `lib/npc/demand-ranking.ts`
- `lib/npc/npc-selection.ts`
- `supabase/migrations/20260803120000_create_bot_engagement_audit.sql`
- `__tests__/app/api/cron/npc-activity.test.ts`
</read_first>
<action>Load one demand snapshot per cron run, assign selected NPCs to ranked targets, and write `bot_engagement_runs` plus aggregate assignment rows in `dry_run`, `shadow`, or `live` mode. In dry-run/shadow, return planned targets and suppression counts without `intel_posts`/`beach_reviews` mutations; in live, retain `validateCronRequest` and `withObservedCron`, preserve existing response fields, and add additive counters for demand targets, exploration targets, suppressed posts, reviews blocked, and audit persistence. Replace the fixed PT offset with the repository timezone helper or a documented UTC-derived hour.</action>
<verify>Run the focused cron test suite and assert unauthorized requests make no reads/writes, dry-run makes no content inserts, shadow mode writes only audit records, live mode writes only approved content types, and a partial beach failure does not abort other assignments.</verify>
<acceptance_criteria>
- `NPC_ENGAGEMENT_MODE=dry_run` and `shadow` produce zero content-table mutations.
- Live mode reads demand once per run and never performs a per-NPC full demand query.
- Existing summary keys remain present and new counters are additive.
- The route never routes the `forecaster` or system profiles through the NPC publisher.
</acceptance_criteria>
<done>Dry-run, shadow, live, auth, and partial-failure cron tests pass.</done>
</task>

<task type="execute">
<name>Strengthen selection and cron regression coverage</name>
<files>__tests__/lib/npc/npc-selection.test.ts, __tests__/app/api/cron/npc-activity.test.ts</files>
<read_first>
- `__tests__/app/api/cron/npc-activity.test.ts`
- `__tests__/app/api/cron/morning-forecast-bot.test.ts`
- `__tests__/lib/npc/forecast-formatter.test.ts`
- `lib/npc/npc-selection.ts`
- `app/api/cron/npc-activity/route.ts`
</read_first>
<action>Add tests for global cross-region target selection, 85–90% demand-gap allocation, exploration bounds, home-region context-only behavior, review blocking, dedupe/material-change suppression, per-beach caps, dry-run/shadow no-content writes, and additive response counters. Keep exact HTTP 401 assertions for invalid cron authentication.</action>
<verify>Run `yarn test:unit --runInBand __tests__/lib/npc/npc-selection.test.ts __tests__/app/api/cron/npc-activity.test.ts`.</verify>
<acceptance_criteria>
- Tests fail if `getBeachesForNPC` silently falls back to home-region allocation in live mode.
- Tests fail if dry-run or shadow inserts into `intel_posts` or `beach_reviews`.
- Tests fail if duplicate fingerprints or per-beach caps are ignored.
- Tests preserve the existing unauthorized and empty-selection behavior.</acceptance_criteria>
<done>The focused NPC selection and cron regression command exits 0.</done>
</task>

</tasks>

<verification>
- `yarn test:unit --runInBand __tests__/lib/npc/npc-selection.test.ts __tests__/app/api/cron/npc-activity.test.ts`
- `yarn typecheck`
- Scoped ESLint on `lib/npc/npc-selection.ts`, `lib/npc/beach-selection.ts`, `lib/npc/template-hydration.ts`, and `app/api/cron/npc-activity/route.ts`.
</verification>

<success_criteria>
- Hourly NPC activity follows demand gaps globally, with home region used only for voice/context.
- Default bot output is capped, deduped, non-eyewitness, and review-free.
- Dry-run/shadow modes make rollout measurable without content mutations.
</success_criteria>

<threat_model>
- T-22-04: synthetic content is mistaken for eyewitness intel; mitigation is explicit derived-source metadata and removal of first-person observation claims.
- T-22-05: a cron retry creates a content burst; mitigation is per-beach caps, material-change fingerprints, and idempotent audit writes.
- T-22-06: rollout mode is bypassed; mitigation is server-side environment parsing, no-content writes in dry-run/shadow, and route tests that assert table calls.
</threat_model>
