---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260803120000_create_bot_engagement_audit.sql
  - lib/npc/demand-ranking.ts
  - __tests__/lib/npc/demand-ranking.test.ts
  - __tests__/migrations/bot-engagement-audit.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clean human web demand is ranked globally with an 85–90% demand-gap allocation and 10–15% exploration, with no home-region quota."
    - "Each run can be audited from aggregate score components without storing raw user identities or event payloads."
  artifacts:
    - path: "lib/npc/demand-ranking.ts"
      provides: "Typed demand signal loading, deterministic gap ranking, and bounded target selection"
    - path: "supabase/migrations/20260803120000_create_bot_engagement_audit.sql"
      provides: "Private indexed audit tables for engagement runs and assignments"
  key_links:
    - from: "lib/npc/demand-ranking.ts"
      to: "user_events, intel_posts, beach_reviews"
      via: "service-role aggregate queries with clean-signal exclusions"
    - from: "bot_engagement_assignments"
      to: "beaches"
      via: "beach_id foreign key and indexed run history"
---

<objective>
Create the deterministic, server-only demand-gap ranking and an auditable run/assignment record so bot targeting can follow clean web demand without using home-region quotas.
</objective>

<tasks>

<task type="execute">
<name>Define the bot engagement audit schema</name>
<files>supabase/migrations/20260803120000_create_bot_engagement_audit.sql</files>
<read_first>
- `AGENTS.md`
- `docs/MIGRATION_SAFETY.md`
- `supabase/ARCHITECTURE.md`
- `supabase/migrations/20250817120001_create_intel_and_reviews_tables.sql`
- `supabase/migrations/20260313210000_fix_p0_bot_flagged_and_auth_events.sql`
</read_first>
<action>In one transactional migration, create private service-role audit tables `bot_engagement_runs` and `bot_engagement_assignments` with UUID primary keys, run/window timestamps, mode (`dry_run`, `shadow`, or `live`), beach and optional NPC foreign keys, demand/supply/feedback numeric inputs, selection bucket, suppression reason, and a JSONB reason summary that contains no raw user identifiers or event payloads. Enable RLS with no public/authenticated policies, add indexes on `(window_end DESC)`, `(beach_id, window_end DESC)`, and `(run_id)`, and make reruns safe with `IF NOT EXISTS`. Keep the migration additive; do not alter `intel_posts`, `beach_reviews`, or generated database types.</action>
<verify>Read the migration and confirm it is wrapped in `BEGIN`/`COMMIT`, has no bulk user-data deletion, enables RLS, defines the documented status/mode checks, and contains all three indexes.</verify>
<acceptance_criteria>
- `bot_engagement_runs` and `bot_engagement_assignments` are created idempotently in `supabase/migrations/20260803120000_create_bot_engagement_audit.sql`.
- No policy grants public or authenticated users access to either table; service-role access remains available through RLS bypass.
- Audit columns store aggregate counts and score components, not `user_id`, session identifiers, or raw `user_events.metadata`.
</acceptance_criteria>
<done>The migration source assertions pass and the focused migration test exits 0.</done>
</task>

<task type="execute">
<name>Implement clean demand-gap ranking</name>
<files>lib/npc/demand-ranking.ts</files>
<read_first>
- `AGENTS.md`
- `lib/npc/npc-selection.ts`
- `lib/npc/beach-selection.ts`
- `lib/utils/intel-dedupe.ts`
- `supabase/migrations/20260313210000_fix_p0_bot_flagged_and_auth_events.sql`
- `22-RESEARCH.md`
</read_first>
<action>Export typed functions `loadDemandSignals`, `rankDemandGaps`, and `selectDemandTargets`. Aggregate `user_events` using `event_type = 'beach_view'` plus existing downstream intent events, require `beach_id`, exclude `bot_flagged` rows and mock/system/test identities, join active non-deleted `intel_posts` and `beach_reviews`, calculate freshness and feedback terms, and return stable beach-id/name/coordinate records with demand, supply, feedback, gap, freshness, and suppression fields. Rank globally with explicit constants for an 85–90% demand-gap bucket and a 10–15% exploration bucket, deterministic score/tie ordering, and no region partitioning. Accept an injected `now` and window options for deterministic tests; keep all Supabase access inside the server-side loader.</action>
<verify>Run the focused ranking unit test and inspect the exported types to confirm the selector can be called by both NPC activity and Morning Intel without importing a route or client-only module.</verify>
<acceptance_criteria>
- Clean demand rows with bot/mock/system/test markers never contribute to a target score.
- A beach with high views but already-heavy fresh bot supply ranks below an equally visited beach with a larger supply gap.
- `selectDemandTargets` returns 85–90% demand-gap targets and 10–15% exploration targets within the configured batch, with deterministic tie-breaking.
- The result contains enough score components to write `bot_engagement_assignments` without raw event metadata.
</acceptance_criteria>
<done>The ranking unit tests prove clean filtering, deterministic ordering, bounded allocation, and global selection.</done>
</task>

<task type="execute">
<name>Lock ranking and migration assertions</name>
<files>__tests__/lib/npc/demand-ranking.test.ts, __tests__/migrations/bot-engagement-audit.test.ts</files>
<read_first>
- `__tests__/lib/npc/forecast-formatter.test.ts`
- `__tests__/actions/intel-actions-dedupe.test.ts`
- `__tests__/migrations/coach-picks-daily-intel.test.ts`
- `lib/npc/demand-ranking.ts`
- `supabase/migrations/20260803120000_create_bot_engagement_audit.sql`
</read_first>
<action>Write unit fixtures for demand, supply, feedback, stale content, exploration, deterministic ties, and excluded traffic. Assert the 85–90/10–15 allocation, global selection across regions, and score suppression. Add migration source assertions for transactional safety, RLS, aggregate-only audit fields, mode checks, and required indexes.</action>
<verify>Run `yarn test:unit --runInBand __tests__/lib/npc/demand-ranking.test.ts __tests__/migrations/bot-engagement-audit.test.ts`.</verify>
<acceptance_criteria>
- The tests fail if the implementation reintroduces a home-region allocation or counts bot/mock/system traffic.
- The tests fail if a rerun changes target order for the same `now` and inputs.
- The migration assertions fail if public read policies or raw identity/event-payload columns are introduced.
</acceptance_criteria>
<done>The focused ranking and migration test command exits 0.</done>
</task>

<task type="execute">
<name>Run the blocking schema push checkpoint</name>
<files>supabase/migrations/20260803120000_create_bot_engagement_audit.sql</files>
<read_first>
- `AGENTS.md`
- `docs/MIGRATION_SAFETY.md`
- `supabase/migrations/20260803120000_create_bot_engagement_audit.sql`
</read_first>
<action>[BLOCKING] After the migration file and source tests are complete, run the repository-approved Supabase schema push command only in the authorized target environment. If production credentials or approval are unavailable, stop at the migration/source-test checkpoint and record the exact manual push command as a release gate; never push production implicitly.</action>
<verify>For an authorized environment, `supabase db push` exits 0 and a read-only schema check confirms both audit tables, RLS, checks, and indexes. Otherwise, report `NOT RUN — approval-gated` and keep implementation verification green against the migration source.</verify>
<acceptance_criteria>
- No later plan assumes the audit tables exist in production unless this checkpoint has a recorded successful push.
- The implementation remains runnable in dry-run mode when the audit schema is unavailable, with an explicit error/kill-switch path for live writes.</acceptance_criteria>
<done>The authorized push succeeds, or the approval-gated push is explicitly recorded as not run.</done>
</task>

</tasks>

<verification>
- `yarn test:unit --runInBand __tests__/lib/npc/demand-ranking.test.ts __tests__/migrations/bot-engagement-audit.test.ts`
- `yarn typecheck`
- Review the migration for RLS, no raw identity payloads, and approval-gated push behavior.
</verification>

<success_criteria>
- Demand ranking is deterministic, globally demand-driven, and feedback/supply-aware.
- Audit records are aggregate-only, private, indexed, and safe to rerun.
- No production schema push is claimed without an explicit successful command result.
</success_criteria>

<threat_model>
- T-22-01: bot/mock/system traffic contaminates demand; mitigation is centralized exclusion in the loader and exclusion fixtures.
- T-22-02: audit data leaks identity or event payloads; mitigation is aggregate-only columns, RLS without public policies, and migration source assertions.
- T-22-03: a partial schema deployment makes live writes unauditable; mitigation is dry-run/shadow modes and a blocking push checkpoint.
</threat_model>
