diff --git a/node_modules/@capgo/capacitor-social-login/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java b/node_modules/@capgo/capacitor-social-login/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
index 5cfaa74..7c04ce6 100644
--- a/node_modules/@capgo/capacitor-social-login/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
+++ b/node_modules/@capgo/capacitor-social-login/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
@@ -569,7 +569,6 @@ public class GoogleProvider implements SocialProvider {
                                         if (result.getAccessToken() != null) {
                                             JSObject accessTokenObj = new JSObject();
                                             accessTokenObj.put("token", result.getAccessToken());
-                                            // accessTokenObj.put("userId", accessToken.userId);
 
                                             resultObj.put("accessToken", accessTokenObj);
                                             resultObj.put("profile", user);
@@ -579,7 +578,14 @@ public class GoogleProvider implements SocialProvider {
                                             persistState(googleIdTokenCredential.getIdToken(), result.getAccessToken());
                                             call.resolve(response);
                                         } else {
-                                            call.reject("Failed to get access token");
+                                            // Access token is null — fall back to idToken-only
+                                            Log.w(LOG_TAG, "Access token is null, falling back to idToken-only response");
+                                            resultObj.put("profile", user);
+                                            resultObj.put("idToken", googleIdTokenCredential.getIdToken());
+                                            resultObj.put("responseType", "online");
+                                            response.put("result", resultObj);
+                                            persistState(googleIdTokenCredential.getIdToken(), null);
+                                            call.resolve(response);
                                         }
                                     } else {
                                         if (result.getServerAuthCode() != null) {
@@ -592,7 +598,20 @@ public class GoogleProvider implements SocialProvider {
                                         }
                                     }
                                 } catch (Exception e) {
-                                    call.reject("Error retrieving access token: " + e.getMessage());
+                                    // Authorization API failed — fall back to idToken-only.
+                                    // This is non-fatal: Supabase only needs the idToken.
+                                    Log.w(LOG_TAG, "Authorization API failed, falling back to idToken-only: " + e.getMessage());
+                                    try {
+                                        resultObj.put("profile", user);
+                                        resultObj.put("idToken", googleIdTokenCredential.getIdToken());
+                                        resultObj.put("responseType", "online");
+                                        response.put("result", resultObj);
+                                        persistState(googleIdTokenCredential.getIdToken(), null);
+                                        call.resolve(response);
+                                    } catch (Exception fallbackError) {
+                                        Log.e(LOG_TAG, "Fallback response construction also failed", fallbackError);
+                                        call.reject("Error retrieving access token: " + e.getMessage());
+                                    }
                                 } finally {
                                     executor.shutdown();
                                 }
